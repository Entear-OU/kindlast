{
  "info": {
    "name": "Kindlast",
    "description": "The Kindlast HTTP surface, against the local compose stack (deploy/compose.yaml).\n\nThe collection is committed and reviewed alongside the code it describes, so it grows with the build order rather than drifting in one person's workspace.\n\n**Two halves, with different futures.** The `auth` folder describes endpoints Zitadel serves and `web` redirects to. Those will never appear in a proto file, so this collection stays their source of truth. The `Core API v1` folder describes the proto surface, and once buf emits OpenAPI (design doc §23.2) those requests should be generated rather than hand-written, or the two will disagree.\n\n**Requests marked NOT YET IMPLEMENTED** are here on purpose. They document the contract that the issue named in each description will deliver, so the collection tracks the plan rather than only the past.\n\nNo secrets live in this file. The client secret is generated by the seed job and belongs in a Postman environment or a vault, never in the repo.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "auth_base_url",
      "value": "http://localhost:8300",
      "description": "Zitadel. Published by compose on KINDLAST_AUTH_PORT, default 8300."
    },
    {
      "key": "edge_base_url",
      "value": "http://localhost:8000",
      "description": "Caddy edge. Published on KINDLAST_EDGE_PORT, default 8000."
    },
    {
      "key": "web_base_url",
      "value": "http://localhost:3000",
      "description": "The Next.js app, run with `bun run dev`."
    },
    {
      "key": "api_base_url",
      "value": "http://localhost:8000",
      "description": "core-api, through the Caddy edge (ENT-197). It still binds no published port of its own, so the edge is the only way in: the same door from the host as from another container, with no development-only shortcut. Paths are the Connect procedures, which are fully qualified proto names."
    },
    {
      "key": "client_id",
      "value": "",
      "description": "The web OIDC client, created by the seed job. Read it with:\n\ndocker run --rm -v kindlast_zitadel-machinekey:/k alpine cat /k/web-client.json"
    },
    {
      "key": "client_secret",
      "value": "",
      "description": "Seed-generated. Keep it in an environment, never in this file."
    },
    {
      "key": "access_token",
      "value": "",
      "description": "Set by the token request's test script."
    },
    {
      "key": "org_id",
      "value": "a0000000-0000-4000-8000-000000000001",
      "description": "Alpha Compliance GmbH, from deploy/seed/seed.sql."
    }
  ],
  "item": [
    {
      "name": "Stack health",
      "description": "The two checks that tell you whether the stack is actually up, and the ones CI runs.",
      "item": [
        {
          "name": "Edge healthz",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{edge_base_url}}/healthz",
              "host": ["{{edge_base_url}}"],
              "path": ["healthz"]
            },
            "description": "Returns `ok`. The edge also routes `/kindlast.core.v1.*` to core-api as of ENT-197; web claims its own routes later in build-order step 1."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('edge is up', () => pm.response.to.have.status(200));",
                  "pm.test('returns ok', () => pm.expect(pm.response.text()).to.eql('ok'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Auth (Zitadel)",
      "description": "Consumed through discovery, never by hard-coded path (§18.2), so a self-hoster can point at their own provider. Every URL below is taken from the discovery document rather than assumed.",
      "item": [
        {
          "name": "OIDC discovery",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{auth_base_url}}/.well-known/openid-configuration",
              "host": ["{{auth_base_url}}"],
              "path": [".well-known", "openid-configuration"]
            },
            "description": "The document every other request here is derived from. Run this first: if it does not answer, nothing else will."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('discovery answers', () => pm.response.to.have.status(200));",
                  "const doc = pm.response.json();",
                  "pm.test('issuer matches the configured base url', () => {",
                  "  pm.expect(doc.issuer).to.eql(pm.variables.get('auth_base_url'));",
                  "});",
                  "// Store the endpoints so later requests use the advertised paths",
                  "// rather than paths copied into this file by hand.",
                  "pm.collectionVariables.set('token_endpoint', doc.token_endpoint);",
                  "pm.collectionVariables.set('userinfo_endpoint', doc.userinfo_endpoint);",
                  "pm.collectionVariables.set('jwks_uri', doc.jwks_uri);",
                  "pm.collectionVariables.set('revocation_endpoint', doc.revocation_endpoint);",
                  "pm.collectionVariables.set('end_session_endpoint', doc.end_session_endpoint);"
                ]
              }
            }
          ]
        },
        {
          "name": "JWKS",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{auth_base_url}}/oauth/v2/keys",
              "host": ["{{auth_base_url}}"],
              "path": ["oauth", "v2", "keys"]
            },
            "description": "The signing keys core-api verifies against, locally and cached, rather than introspecting per request (§1.4).\n\n**This is `{\"keys\": []}` on a freshly seeded stack.** Zitadel generates a signing key lazily, on the first token it issues, so an empty set is correct rather than broken. It matters for ENT-195: a JWKS cache populated at boot would cache nothing and then reject every token, which is exactly why the acceptance criterion is that an unknown `kid` triggers a refetch."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('serves a jwks document', () => {",
                  "  pm.response.to.have.status(200);",
                  "  pm.expect(pm.response.json().keys).to.be.an('array');",
                  "});",
                  "// Deliberately not asserting a non-empty set: see the description.",
                  "if (pm.response.json().keys.length === 0) {",
                  "  console.log('JWKS is empty, which is expected until the first token is issued.');",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Token, client credentials",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/x-www-form-urlencoded" }
            ],
            "body": {
              "mode": "urlencoded",
              "urlencoded": [
                { "key": "grant_type", "value": "client_credentials" },
                { "key": "client_id", "value": "{{client_id}}" },
                { "key": "client_secret", "value": "{{client_secret}}" },
                { "key": "scope", "value": "openid profile urn:zitadel:iam:org:project:id:{{project_id}}:aud" }
              ]
            },
            "url": {
              "raw": "{{auth_base_url}}/oauth/v2/token",
              "host": ["{{auth_base_url}}"],
              "path": ["oauth", "v2", "token"]
            },
            "description": "A machine token, for exercising the API without a browser. The seed job creates the `core-api-client` service user this needs (ENT-195), so it works against any stack brought up with `docker compose -f deploy/compose.yaml up -d`.\n\nFill `client_id`, `client_secret` and `project_id` from the shared volume rather than guessing:\n\n```bash\ndocker run --rm -v kindlast_zitadel-machinekey:/k alpine cat /k/core-api-client.json\ndocker run --rm -v kindlast_zitadel-machinekey:/k alpine cat /k/core-api-audience.txt\n```\n\nTwo details were measured against this stack rather than taken from a specification, and both cost an afternoon to rediscover.\n\n**`client_id` is the service user's username, not its id.** The secret response carries its own `clientId`, and that is the one to send. Passing the numeric user id returns `invalid_client: client not found`.\n\n**The audience is Zitadel's project id**, which is why the reserved scope `urn:zitadel:iam:org:project:id:{project}:aud` is in the scope list. Without it the token comes back scoped to the client itself, and core-api refuses it, correctly, as minted for someone else.\n\nThe scope story, which changed in ENT-197 and is worth reading before a `permission_denied` sends you looking in the wrong place. Zitadel's access tokens carry no `scope` and no `scp` claim whatever is requested here. With role assertion enabled they carry project roles under `urn:zitadel:iam:org:project:roles`, which `KINDLAST_OIDC_SCOPE_CLAIMS` points at, and that is what satisfies an endpoint declaring a real permission. `openid` is the exception: it is not a permission, nothing grants it, and verification asserts it, so an endpoint declaring it is reachable by any valid token. See `docs/core-api-configuration.md`.\n\nThe **user** flow is authorization code with PKCE and cannot be driven from Postman end to end, because it involves a login form and a redirect back to web. Use a browser for that."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// The service user is seeded (ENT-195), so a failure here is a",
                  "// real failure rather than an expected gap, and is asserted.",
                  "pm.test('token issued', () => pm.response.to.have.status(200));",
                  "",
                  "if (pm.response.code === 200) {",
                  "  const body = pm.response.json();",
                  "  pm.collectionVariables.set('access_token', body.access_token);",
                  "",
                  "  const claims = JSON.parse(atob(body.access_token.split('.')[1]",
                  "    .replace(/-/g, '+').replace(/_/g, '/')));",
                  "",
                  "  // core-api accepts one audience and only one. If this fails, the",
                  "  // reserved aud scope is missing or project_id is unset, and every",
                  "  // core-api call will read as an audience mismatch.",
                  "  pm.test('aud carries the project id', () =>",
                  "    pm.expect(String(claims.aud)).to.include(pm.environment.get('project_id')));",
                  "",
                  "  // Recorded, not asserted: this is the known gap, and failing here",
                  "  // would report a design decision as a defect.",
                  "  if (!claims.scope && !claims.scp) {",
                  "    console.log('No scope claim, as measured. core-api refuses this at the',",
                  "      'scope stage until ENT-197 enables role assertion.');",
                  "  }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Userinfo",
          "request": {
            "method": "GET",
            "header": [
              { "key": "Authorization", "value": "Bearer {{access_token}}" }
            ],
            "url": {
              "raw": "{{auth_base_url}}/oidc/v1/userinfo",
              "host": ["{{auth_base_url}}"],
              "path": ["oidc", "v1", "userinfo"]
            },
            "description": "The IdP's view of the subject. Note that core-api does not call this per request: it verifies the token locally (§1.4), so auth being down does not take the API down with it."
          }
        },
        {
          "name": "Revoke token",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/x-www-form-urlencoded" }
            ],
            "body": {
              "mode": "urlencoded",
              "urlencoded": [
                { "key": "token", "value": "{{access_token}}" },
                { "key": "client_id", "value": "{{client_id}}" },
                { "key": "client_secret", "value": "{{client_secret}}" }
              ]
            },
            "url": {
              "raw": "{{auth_base_url}}/oauth/v2/revoke",
              "host": ["{{auth_base_url}}"],
              "path": ["oauth", "v2", "revoke"]
            },
            "description": "Revocation reaches the refresh token. Access tokens already issued stay valid until they expire, which is why sign-out also adds the `jti` to a Redis deny-list (§1.7). Without that second step, sign-out looks like it works and does not, for up to ten minutes."
          }
        }
      ]
    },
    {
      "name": "web (OAuth client)",
      "description": "Browser-facing redirect endpoints on the Next.js app, not `/api/v1`. They are listed for completeness: most cannot be exercised from Postman, because they end in a redirect to a login form.\n\nNOT YET IMPLEMENTED: ENT-197.",
      "item": [
        {
          "name": "Login (NOT YET IMPLEMENTED, ENT-197)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{web_base_url}}/auth/login",
              "host": ["{{web_base_url}}"],
              "path": ["auth", "login"]
            },
            "description": "Builds the authorization URL with a PKCE verifier and state, then 302s. `?idp=google` adds an `idp_hint`.\n\nFollow redirects off to see the 302 rather than the login page."
          }
        },
        {
          "name": "Logout (NOT YET IMPLEMENTED, ENT-197)",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{web_base_url}}/auth/logout",
              "host": ["{{web_base_url}}"],
              "path": ["auth", "logout"]
            },
            "description": "**POST with a CSRF token, never GET.** A GET logout can be triggered by a prefetch or an image tag, which signs the user out without them acting. ENT-197 asserts a GET is rejected."
          }
        }
      ]
    },
    {
      "name": "Core API v1",
      "description": "The proto surface, reachable from here as of ENT-197. core-api still binds no published port of its own, and CI asserts that absence rather than trusting it; the Caddy edge routes `/kindlast.core.v1.*` to it instead. That is the same door a container uses, which is the point: there is no development-only shortcut that stops existing in production.\n\nThese are Connect calls. Every one is a POST with a JSON body, including the ones that only read, and the path is the fully qualified proto name rather than a REST-style noun. An empty body is a parse error; the empty request message is `{}`.\n\nThe interceptor chain in front of these endpoints is live and tested as of ENT-195, and it is client-agnostic by construction: it verifies every token itself rather than trusting which door a request arrived through. `./scripts/validate-core-api-auth.sh` drives the whole chain and explains what each result proves.\n\nOne asymmetry to expect between this folder and a browser. The client-credentials token from the Auth folder is a machine token: it carries the project roles, so it passes endpoints declaring a real permission. A human token from the authorization-code flow cannot be obtained here at all, because that flow involves a login form and a redirect back to web. Use a browser for that, or `bun run --cwd apps/web test:e2e`, which drives it end to end.\n\nOnce buf emits OpenAPI (§23.2), these requests should be generated from it rather than maintained here by hand.",
      "item": [
        {
          "name": "Me (GetCurrentUser)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              {
                "key": "Kindlast-Org-Id",
                "value": "{{org_id}}",
                "description": "The active organisation travels per request rather than in the token, so switching organisation needs no re-minting (§20.1).\n\nDisable this header to exercise the bootstrap path: a caller who has never signed in has no organisation id to send, and the call has to work without one or a new user could never obtain one."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.SessionService/GetCurrentUser",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.SessionService", "GetCurrentUser"]
            },
            "description": "The console bootstrap call: user, memberships with roles, active organisation, plan.\n\nOn a subject it has never seen it provisions a personal organisation and an owner membership in one transaction, idempotent on the `sub` claim. Required scope: `openid`, declared on the proto method.\n\n**Answerable from here as of ENT-197**, now that the edge routes `/kindlast.core.v1.*` to core-api. It is a Connect call, so POST with a JSON body even though it reads: `{}` is the empty request message, and an empty body is a parse error rather than the same thing.\n\nWhat `openid` means here is worth knowing before it surprises you. It is not a permission and no authorization server grants it: verification asserts it, so a token that passed signature, issuer, audience and expiry carries it whether or not the IdP said anything about scopes. Measured on this stack, a real Zitadel access token carries seven project roles and no `openid` at all. That is why this endpoint is reachable by a caller holding no grants, which it has to be, because this is where their first grant comes from.\n\nTwo behaviours worth knowing before you read a response. **The organisation header is optional on the first call** and must be: a client that has never signed in cannot know an organisation id, because none exists yet, so omitting it resolves the caller's default. And a brand-new organisation has no subscription row, so `plan` comes back `free` rather than empty.\n\nHow the personal organisation gets its name is worth knowing, because it involves a call you cannot see from here. It is named from the token's display name or email, and Zitadel puts neither in an **access** token, so core-api asks the provider's userinfo endpoint with your own access token and names it from that. Only on the request that provisions, never on later calls. If userinfo is unreachable or the provider declares none, the name falls back to the raw subject id, which looks like `386250729179840515` and is the symptom to recognise.\n\nEach membership carries an `orgSlug` as of ENT-198, and that is what the console routes on: every authenticated page lives under `/o/{orgSlug}/`. It is derived from the name when the organisation is created and never recomputed, so it does not follow a rename. Use `orgName` for a label and `orgSlug` for a link; the two drift apart the moment anyone renames an organisation, and that is by design, because slugs live in bookmarks and in emailed approval links."
          }
        },
        {
          "name": "Create organisation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Acme Compliance GmbH\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/CreateOrganisation",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "CreateOrganisation"]
            },
            "description": "Creates an organisation and makes you its owner. **Implemented as of ENT-202.** Required scope: `org:manage`.\n\n**No organisation header, deliberately.** This is one of two calls that carry none, the other being Accept invitation: in both you are establishing membership rather than exercising it, so there is no active organisation to name. Omit the header rather than sending it empty (§1.8) — an empty value is a client bug and should be refused, while its absence is a statement.\n\nThe slug is derived from the name by `org_slug()` at insert time and is immutable from then on. Send a name with umlauts to watch the transliteration: `Müller GmbH` becomes `mueller-gmbh`, not `m-ller-gmbh`. If the slug is already taken, the collision retry appends an ordinal and you get `acme-compliance-gmbh-2`, which is the correct answer rather than an error.\n\nCopy the `orgId` from the response into the `org_id` environment variable to act inside the new organisation with the requests below."
          }
        },
        {
          "name": "Rename organisation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Acme Holdings GmbH\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/UpdateOrganisation",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "UpdateOrganisation"]
            },
            "description": "Renames the organisation the header names. **Implemented as of ENT-202.** Required scope: `org:manage`, and owner-only on top of that.\n\n**The slug does not follow the rename**, and the response returns it so you can see that rather than assume it. This is a product decision, not an oversight: slugs live in bookmarks and in emailed capability-token links, which are exactly the links a compliance product has to keep working. Rename a customer after an acquisition and every existing link still resolves.\n\nUse `name` for a label and `slug` for a link. They drift apart the moment anyone renames, by design.\n\nA member or viewer gets `permission_denied` rather than a call that appears to succeed and changes nothing."
          }
        },
        {
          "name": "List members",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/ListMembers",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "ListMembers"]
            },
            "description": "Everyone in the organisation the header names. **Implemented as of ENT-202.** Required scope: `org:read`, not `org:manage` — seeing who your colleagues are is not an administrative act, and requiring the management scope would mean a viewer could not render a page they are entitled to see.\n\n`displayName` and `email` are readable by every member regardless of role, which is a decision the product owner took explicitly on 2026-08-15 rather than a default nobody examined. Before that, `user_identities` was self-only and this endpoint could only have returned uuids. The reversal is narrow: it grants nothing outside a shared membership, and nothing outside the organisation the header names. See `db/migrations/00005_co_member_identity.sql`.\n\nBoth fields can be empty and a client must cope. `displayName` is absent when the authorization server returned no name claim, and both are absent for someone invited who has not yet signed in.\n\nCopy a `userId` from the response into the `member_user_id` environment variable for the two requests below."
          }
        },
        {
          "name": "Update member role",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"userId\": \"{{member_user_id}}\",\n  \"role\": \"member\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/UpdateMemberRole",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "UpdateMemberRole"]
            },
            "description": "Changes one member's role. **Implemented as of ENT-202.** Required scope: `org:manage`, owner-only.\n\nThree roles exist and no more: `owner`, `member`, `viewer`. Anything else answers `invalid_argument` naming the three, rather than letting the database's check constraint reply with `memberships_role_check`, which is a correct answer to the wrong audience.\n\n**Demoting the last owner is refused** with `failed_precondition`, not only removing them. Guarding removal alone would be theatre: a last owner who cannot be removed could be demoted to viewer and then removed, reaching the same ownerless organisation in two steps. An organisation with no owner has nobody who can invite, change roles or manage billing, and no way back short of an operator opening a database session.\n\nThe response re-reads the member rather than echoing what was sent, so it describes what the database holds."
          }
        },
        {
          "name": "Remove member",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"userId\": \"{{member_user_id}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/RemoveMember",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "RemoveMember"]
            },
            "description": "Removes someone from the organisation the header names. **Implemented as of ENT-202.** Required scope: `org:manage`, owner-only.\n\nRemoving the last owner answers `failed_precondition`; see Update member role for why that rule covers demotion too.\n\nA user id that is not a member of this organisation answers `not_found`, and so does one that does not exist at all. That ambiguity is deliberate: distinguishing them would turn this endpoint into a way of asking whether a given person belongs to a given organisation."
          }
        },
        {
          "name": "Invite member",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"colleague@example.com\",\n  \"role\": \"member\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/InviteMember",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "InviteMember"]
            },
            "description": "Creates an invitation to the organisation the header names. **Implemented as of ENT-202.** Required scope: `org:manage`, owner-only. Invitations expire after seven days.\n\n**The response does not contain the token, and never will.** It carries `invitationId` and `expiresAt` only. The raw token exists in exactly two places: the email sent to the invitee, and a SHA-256 hash stored against the row. Returning it here would let anyone who can invite also redeem on the invitee's behalf, which defeats the point of a capability sent to an address.\n\nA practical consequence for anyone testing from here: you cannot chain this into Accept invitation. To exercise acceptance, write a fixture invitation with a token you already know, which is what the Go tests do via the store's `CreateInvitation`.\n\n**Email delivery is ENT-202 PR 2.** As of PR 1 the invitation is created, hashed and stored, and nothing is sent. Until then the invitee learns of it out of band.\n\nEmail validation is deliberately shallow — non-empty and containing an `@`. Anything stricter rejects addresses that are valid under RFC 5321, and the authoritative test of an address is whether mail to it arrives."
          }
        },
        {
          "name": "Accept invitation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"token\": \"{{invitation_token}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/AcceptInvitation",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "AcceptInvitation"]
            },
            "description": "Ordering matters and is easy to get wrong: this must run **before** the first GetCurrentUser. Get it backwards and every invited user ends up owning a personal organisation they never asked for, alongside the one they were invited to. `web`'s callback enforces that ordering (ENT-197); a direct caller has to keep it themselves.\n\n**Implemented as of ENT-196**, and reachable from here as of ENT-197. Required scope is `openid`, not `org:manage`: the caller is redeeming a capability they were sent and holds no role anywhere until it succeeds, so an organisation scope would make a first invitation impossible to accept.\n\nThe token is sent in the clear here and stored hashed, so the value in the database never matches this one. **Issuing invitations exists as of ENT-202** (see Invite member above), but you still cannot chain the two from here: that endpoint deliberately never returns the token, so a fixture written with a token you already know remains the only way to exercise acceptance, which is what the Go tests do.\n\nExpired, already accepted and never existed all answer `not_found` alike, deliberately, so this cannot be used to discover which tokens are real.\n\nThe response carries `orgSlug` as of ENT-198, so a client can redirect straight into `/o/{orgSlug}/` rather than making a second call to find out where the organisation it just joined actually lives."
          }
        },
        {
          "name": "List findings",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"pageSize\": 20\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.FindingsService/ListFindings",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.FindingsService", "ListFindings"]
            },
            "description": "The feed: findings for the organisation the header names, newest first. Required scope: `findings:read`.\n\n**Implemented as of ENT-203.**\n\n**It will still not work from here yet, and the reason is not this endpoint.** No human token carries `findings:read` at all: `deploy/seed/seed.sh` creates all nineteen project roles and grants them to nobody, so a browser session's token has an empty role set (ENT-221). The client-credentials token above has the same problem. Until ENT-221 lands, the only callers that reach a scoped endpoint are the Go tests, which mint their own tokens against the stack's JWKS. Expect `permission_denied` here until then, from the scope interceptor rather than from the handler.\n\nPagination is keyset, not offset, over the `(org_id, created_at desc)` index 00002 built for this query. `pageToken` is opaque: do not parse or construct one. Send `pageSize: 0` (or omit it) to let the server choose. Treat a non-empty `nextPageToken` as the only signal that more exist — a full last page is ordinary and would otherwise read as another page waiting.\n\n`status` filters to one of `pending`, `approved`, `rejected`, `snoozed`. Omit it for every status, which is what the feed's default view wants: a finding somebody already dealt with is still part of the compliance record."
          }
        },
        {
          "name": "Get finding",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"findingId\": \"{{finding_id}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.FindingsService/GetFinding",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.FindingsService", "GetFinding"]
            },
            "description": "One finding plus the quoted regulatory text behind it. Required scope: `findings:read`. **Implemented as of ENT-203.** See List findings for why a real token still cannot reach it (ENT-221).\n\nThe `supporting` array comes from `finding_supporting_chunks`, which is org-gated in the database as well as in the handler and returns no rows rather than raising when the finding belongs to someone else.\n\n**An empty `supporting` array is normal and is not an error.** The corpus does not cover every instrument an obligation can cite. Empty means \"we cannot show you the text here\", which is honest; it is not the same as the citation being absent, and a client renders the citation either way.\n\n**On the `citation` object, which is the part worth reading carefully.** Every field in it is a value the database already stored, assembled by `analyst_citation_label` and `analyst_citation_url` when the finding was created. Render `label` and link `url`. Do **not** rebuild the label from `celex` and `article`: a second assembler is a second thing that can be wrong, and the product's whole value is that a human can check the claim against the law."
          }
        },
        {
          "name": "Approve finding",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"findingId\": \"{{finding_id}}\",\n  \"reviewed\": true\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.FindingsService/ApproveFinding",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.FindingsService", "ApproveFinding"]
            },
            "description": "The act path. Required scope: `findings:act`, which is deliberately **not** `findings:read`: rendering the console and approving on a human's behalf are separately dangerous, and a display-only integration should not be one credential leak away from the second.\n\n**Implemented as of ENT-203**, database half included (`00006_act_path_audit.sql`, which is what makes the audit row exist at all). See List findings for why a real token still cannot reach it (ENT-221).\n\n**Plan gating.** With `KINDLAST_BILLING_ENABLED` set, an organisation not on an active `pro` subscription gets `resource_exhausted` here rather than `permission_denied`: the two mean different things to the person reading the console, and only one of them has a button under it. Unset, which is the self-hosted default, the act path is ungated (\u00a718.1).\n\n**There is no actor field, and there will not be one.** The approving human is read from the session GUC the tenancy interceptor set, so a caller cannot name someone else as the approver. `approve_finding` lost its acting-user parameter in the ENT-192 rewrite for exactly this reason.\n\n**Idempotent.** Approving an already-approved finding returns `applied: false`, writes nothing and writes no second audit row. The usual cause is a double submit or a retry, neither of which is a problem worth showing a customer.\n\n`applied: false` also covers a finding that is unknown or belongs to another organisation. All three answer alike on purpose — telling them apart is what probing for a tenancy leak looks like.\n\n**`createdRecordId` is populated as of ENT-203, and which findings populate it is a data question rather than a code one.** 00007 gave obligations an `action_type` and made `analyst_convert_signal` copy it onto the finding; 00009 classified the three obligations where approving means something other than \"a human read it\". So approving a `gdpr-art-30-ropa` finding returns a `processing_activities` id, an Annex III or Article 26 finding returns an `ai_systems` id, and the other twelve obligations stay `review` and create nothing — which is the right outcome for an obligation whose remedy is not a row. Verified end to end through the console, not inferred from the migrations.\n\n**No obligation maps to `create_dsar`, and 00009 asserts that rather than leaving it to chance:** a data-subject request arrives from a person, so an obligation that manufactured one would be inventing the requester.\n\n`createdRecordId` is empty on a repeat approval even though the record exists, because nothing was created by that call. This field reports what this request did; `RecordsService` answers what is on file.\n\n`reviewed` records whether a human actually read the supporting text rather than approving from the feed card. \"Approved\" and \"approved having read the regulation\" are different claims and the audit trail can tell them apart."
          }
        },
        {
          "name": "Reject finding",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"findingId\": \"{{finding_id}}\",\n  \"reason\": \"Not applicable: we process no special-category data\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.FindingsService/RejectFinding",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.FindingsService", "RejectFinding"]
            },
            "description": "Rejects a finding, optionally saying why. Required scope: `findings:act`. **Implemented as of ENT-203**; see List findings for the ENT-221 caveat.\n\n**The reason is worth sending even though it is optional.** Three rejections of the same obligation on the same profile raise a `product_review_flags` row, carrying the distinct reasons given. That is the mechanism by which a bad rule gets noticed rather than being quietly dismissed by every customer in turn, and it only works if the reasons are there.\n\nIdempotent in the same way as approve: rejecting an already-rejected finding returns `applied: false`, keeps the first reason, and writes no second audit row."
          }
        },
        {
          "name": "Snooze finding",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"findingId\": \"{{finding_id}}\",\n  \"days\": 14\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.FindingsService/SnoozeFinding",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.FindingsService", "SnoozeFinding"]
            },
            "description": "Defers a finding. Required scope: `findings:act`. **Implemented as of ENT-203**; see List findings for the ENT-221 caveat.\n\n`days` is clamped to 1..365 by the database; `0` or an omitted value means the default of 7. A snoozed finding returns to `pending` when `expire_snoozed_findings()` next runs, and nothing schedules that yet — the pg_cron jobs were dropped in 00001 and the schedules move to Temporal at build-order step 8.\n\n**Unlike approve and reject, this is deliberately not idempotent.** Snoozing an already-snoozed finding writes a second audit row, because deferring again is a second decision with a new date. A trail that recorded only the first would say a finding was deferred once when a human deferred it four times, which is precisely what an auditor is looking for."
          }
        },
        {
          "name": "Run sweep",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"detectOnly\": false\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.platform.v1.SweepService/RunSweep",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.platform.v1.SweepService", "RunSweep"]
            },
            "description": "Runs the Watcher and then the Analyst for the organisation the header names. **Implemented as of ENT-203.** Required scope: `internal:ingest`.\n\n**This is the one request here the client-credentials token above is actually for.** Every other Core API request needs a human scope nobody is granted yet (ENT-221); this one needs a machine scope. It still needs that role granted to the service user, which the seed does not do yet.\n\n**Why this endpoint exists at all.** Nothing runs the sweeps on a schedule. 00001 dropped the three pg_cron jobs when the schema moved off Supabase, and the schedules return with Temporal at build-order step 8. Between those points a perfectly correct feed shows an empty state forever, because nothing has ever looked. This is how you make something happen now, and when Temporal lands it becomes the thing Temporal calls rather than being replaced.\n\n**It runs as a different database role.** `core-api` serves requests as `kindlast_app`, which deliberately cannot create findings: the thing exposed to the world should not be able to fabricate a claim about a customer's legal exposure. Sweeps run on a second pool as `kindlast_agent` (00008). If `KINDLAST_AGENT_DATABASE_URL` is unset the service is not served at all and this answers `unimplemented`, which is a supported configuration rather than a fault.\n\n**Per-organisation, never global.** A request with no `Kindlast-Org-Id` is refused with `invalid_argument` rather than defaulting. Doing nothing would look like success, and sweeping everyone has every customer in its blast radius, so sweeping the estate is a loop somebody writes deliberately.\n\n`detectOnly` runs the Watcher and skips the Analyst. Use it when the feed is empty and you need to know which half is quiet: the Watcher finding nothing wrong is a different failure from the Analyst failing to resolve an obligation.\n\n`findings` lower than `signals` is normal, not an error. A signal whose obligation slug resolves to nothing is skipped and logged, because a finding with no obligation behind it could not cite anything."
          }
        },
        {
          "name": "Dashboard",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.DashboardService/GetDashboard",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.DashboardService", "GetDashboard"]
            },
            "description": "Posture, open counts by severity, and when the agents last ran. Required scope: `dashboard:read`, kept separate from `findings:read` so a viewer can be shown the summary without being entitled to every finding behind it. **Implemented as of ENT-203**; see List findings for the ENT-221 caveat.\n\n**`posture` has four values, and the fourth is the point:** `green`, `amber`, `red`, `not_assessed`. Before the Watcher has ever run there are no findings, and a posture derived by counting them reports green — telling a customer they are on track when the product has not looked (ENT-161). `not_assessed` is the only honest answer at that point.\n\n`pipeline` is what the agent rail (ENT-222) renders. `watcherLastRunAt` is absent when the Watcher has never run for this organisation, read from `compliance_profiles.watcher_last_run_at`. `profileExists` separates \"the agents have not run\" from \"there is nothing for them to run against\", which are different problems with different fixes.\n\n**Nothing currently runs the sweeps**, and that is deliberate rather than broken: 00001 dropped the three pg_cron jobs and the schedules move to Temporal at build-order step 8. So even a correct dashboard shows `not_assessed` until something invokes `run_watcher()` explicitly."
          }
        },
        {
          "name": "List processing activities (ROPA)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"pageSize\": 20\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/ListProcessingActivities",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "ListProcessingActivities"]
            },
            "description": "The Article 30 record of processing activities. Required scope: `records:read`.\n\n**CONTRACT ONLY, NOT YET SERVED (ENT-200).** The proto and generated code exist; no handler is registered, so this answers `unimplemented` rather than data. It is in the collection deliberately, per the repo rule that requests documenting the plan track where the build order is going. The row it will return already exists in the database: approving a `create_ropa` finding writes one today.\n\nPagination is keyset over `(org_id, created_at desc)`, not offset, for the reason List findings gives. `pageToken` is opaque: do not parse or construct one. Treat a non-empty `nextPageToken` as the only signal that more exist.\n\n**Ordered by creation, not by `updated_at`**, which is a deliberate change from the legacy console. Ordering a paginated list by a column that changes under it lets a row edited between two page requests be skipped or returned twice. \"What did I touch last\" is a sort a client can offer within a page.\n\n**`completeness` is computed server-side**, one of `complete`, `incomplete`, `review_needed`. `review_needed` means the Executor pre-filled the entry and no human has edited it since, which is a different state from incomplete and wants a different prompt. `complete` additionally requires the row to have been updated after it was created, which is what stops a stub from ever reading as complete without a human having looked.\n\n**`manualQuota`** carries the Free-tier cap on manually-created activities (ENT-84). `limit: 0` means unlimited, which is what a paid plan and a billing-disabled self-hosted deployment both report — check for zero rather than rendering \"3 of 0 used\". Executor-created rows never count against it: a record created from an approved finding is part of the compliance record and is not withheld behind a plan."
          }
        },
        {
          "name": "Get processing activity",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"processingActivityId\": \"{{processing_activity_id}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/GetProcessingActivity",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "GetProcessingActivity"]
            },
            "description": "One entry in the Article 30 record. Required scope: `records:read`. **Contract only, not yet served (ENT-200)** — see List processing activities.\n\n**The empty fields are the point, not a gap.** `approve_finding` creates the row from a finding that knows the activity exists and not what is in it, so `purpose`, `legalBasis` and `retentionPeriod` come back empty on an Executor-created stub. Article 30 is a record of fact, and a plausible guess in `legalBasis` is the single worst thing this product could write.\n\n`sourceFindingId` is set when the Executor created this and empty when a human did. \"Why is this here\" is the first question an auditor asks of a compliance record."
          }
        },
        {
          "name": "List AI systems",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"pageSize\": 20\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/ListAiSystems",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "ListAiSystems"]
            },
            "description": "The AI Act system register. Required scope: `records:read`. **Contract only, not yet served (ENT-200)** — see List processing activities. Approving an `ai-act-annex-iii-high-risk-systems` or `ai-act-art-26-deployer-obligations` finding already writes a row here (00009).\n\n**`riskClassification` defaults to `unclassified`, deliberately not `minimal`.** Article 6 classification is a judgement with consequences, and a register defaulting to the lowest tier tells a customer they are fine about a system nobody has assessed. Same reasoning as `not_assessed` on the dashboard posture, applied to a record instead of a summary. Values: `unacceptable`, `high`, `limited`, `minimal`, `unclassified`.\n\n`documentationStatus` is `missing`, `in_progress` or `complete`: how far the Article 11 technical documentation has got.\n\n`lastReviewedAt` absent means never reviewed, which for a register driving high-risk obligations is worth showing rather than hiding behind a created date.\n\n**Measured, and worth knowing before you profile this:** `ai_systems` carried only an `(org_id)` index from 00002 while its two sibling registers had the composite. Migration 00011 adds `(org_id, created_at desc)` and drops the redundant single-column index, because a keyset page over an unsupported ordering sorts the whole tenant's rows on every request."
          }
        },
        {
          "name": "Get AI system",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"aiSystemId\": \"{{ai_system_id}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/GetAiSystem",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "GetAiSystem"]
            },
            "description": "One system in the AI Act register. Required scope: `records:read`. **Contract only, not yet served (ENT-200)** — see List processing activities.\n\n`vendor` empty means built in house, which is a meaningful empty rather than a missing one: the AI Act's provider and deployer duties differ, and an organisation that built the system is generally both."
          }
        },
        {
          "name": "List DSARs",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"pageSize\": 20\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/ListDsars",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "ListDsars"]
            },
            "description": "Data-subject access requests and the statutory clock on each. Required scope: `records:read`. **Contract only, not yet served (ENT-200)** — see List processing activities.\n\n**Ordered by `responseDueAt` ascending, soonest first**, not by creation, because the only question anyone asks of a DSAR log is which one runs out next. That means a `pageToken` from this list is **not** interchangeable with the ones from the other two registers. Migration 00011 adds the `(org_id, response_due_at)` index this needs; the pre-existing `dsars_due_idx` looks like the right index and is not, because it does not lead with `org_id` and is partial on the two unfinished statuses.\n\n**Nothing in this register arrives from an approval, unlike the other two.** 00009 classified three obligations and deliberately mapped none to `create_dsar`, and 00010 asserts that: a data-subject request arrives from a person, so an obligation that manufactured one would be inventing the requester. Every row here was logged by a human, and `sourceFindingId` is therefore always empty today. It is carried rather than omitted so that going from always-empty to sometimes-set is not a contract change.\n\n**`urgency` is computed server-side**, one of `overdue`, `due_soon`, `on_track`, `answered`, with `due_soon` meaning ten days or fewer remaining. That window is a regulatory escalation threshold, and a client re-deriving it from `responseDueAt` would be a second implementation of a deadline that would eventually disagree with this one. `daysUntilDue` is carried alongside it because \"4 days left\" and \"11 days overdue\" are the words a handler acts on.\n\n`status` filters to `open`, `in_progress`, `responded` or `closed`. Omit it for every status: a closed request is still part of the record an auditor asks to see."
          }
        },
        {
          "name": "Get DSAR",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"dsarId\": \"{{dsar_id}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/GetDsar",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "GetDsar"]
            },
            "description": "One data-subject request. Required scope: `records:read`. **Contract only, not yet served (ENT-200)** — see List processing activities.\n\n**`receivedAt` is not the row's creation timestamp and the two routinely differ (ENT-224).** A request that arrived by post on the 1st and was logged on the 8th is due one month from the 1st. Article 12(3) runs from receipt, so a clock started at data entry silently grants the organisation the days it took them to notice, which is exactly backwards. `00010_dsar_clock_from_receipt.sql` made the executor path refuse a missing, non-timestamp or future `received_at` rather than defaulting to `now()`.\n\n`responseDueAt` is stored rather than computed on read, so an extension is recorded as a fact about this request instead of being re-derived from a rule that may since have changed. The Article 12(3) two-month extension for complex requests is **not yet modelled**; 00010 left it open as a schema question.\n\n`requestType` is free text rather than an enum on purpose: Article 15-22 requests arrive worded by the person making them, and forcing them into a closed list at intake loses what was actually asked for."
          }
        }
      ]
    }
  ]
}
