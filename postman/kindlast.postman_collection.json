{
  "info": {
    "name": "Kindlast",
    "description": "The Kindlast HTTP surface, against the local compose stack (deploy/compose.yaml).\n\nThe collection is committed and reviewed alongside the code it describes, so it grows with the build order rather than drifting in one person's workspace.\n\n**Two halves, with different futures.** The `auth` folder describes endpoints Zitadel serves and `web` redirects to. Those will never appear in a proto file, so this collection stays their source of truth. The `Core API v1` folder describes the proto surface, and once buf emits OpenAPI (design doc §23.2) those requests should be generated rather than hand-written, or the two will disagree.\n\n**Requests marked NOT YET IMPLEMENTED** are here on purpose. They document the contract that the issue named in each description will deliver, so the collection tracks the plan rather than only the past.\n\nNo secrets live in this file. The client secret is generated by the seed job and belongs in a Postman environment or a vault, never in the repo.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "auth_base_url",
      "value": "http://localhost:8300",
      "description": "Zitadel. Published by compose on KINDLAST_AUTH_PORT, default 8300."
    },
    {
      "key": "edge_base_url",
      "value": "http://localhost:8000",
      "description": "Caddy edge. Published on KINDLAST_EDGE_PORT, default 8000."
    },
    {
      "key": "web_base_url",
      "value": "http://localhost:3000",
      "description": "The Next.js app, run with `bun run dev`."
    },
    {
      "key": "api_base_url",
      "value": "http://localhost:8000",
      "description": "core-api, through the Caddy edge (ENT-197). It still binds no published port of its own, so the edge is the only way in: the same door from the host as from another container, with no development-only shortcut. Paths are the Connect procedures, which are fully qualified proto names."
    },
    {
      "key": "client_id",
      "value": "",
      "description": "The web OIDC client, created by the seed job. Read it with:\n\ndocker run --rm -v kindlast_zitadel-machinekey:/k alpine cat /k/web-client.json"
    },
    {
      "key": "client_secret",
      "value": "",
      "description": "Seed-generated. Keep it in an environment, never in this file."
    },
    {
      "key": "access_token",
      "value": "",
      "description": "Set by the token request's test script."
    },
    {
      "key": "org_id",
      "value": "a0000000-0000-4000-8000-000000000001",
      "description": "Alpha Compliance GmbH, from deploy/seed/seed.sql."
    }
  ],
  "item": [
    {
      "name": "Stack health",
      "description": "The two checks that tell you whether the stack is actually up, and the ones CI runs.",
      "item": [
        {
          "name": "Edge healthz",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{edge_base_url}}/healthz",
              "host": ["{{edge_base_url}}"],
              "path": ["healthz"]
            },
            "description": "Returns `ok`. The edge also routes `/kindlast.core.v1.*` to core-api as of ENT-197; web claims its own routes later in build-order step 1."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('edge is up', () => pm.response.to.have.status(200));",
                  "pm.test('returns ok', () => pm.expect(pm.response.text()).to.eql('ok'));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "Auth (Zitadel)",
      "description": "Consumed through discovery, never by hard-coded path (§18.2), so a self-hoster can point at their own provider. Every URL below is taken from the discovery document rather than assumed.",
      "item": [
        {
          "name": "OIDC discovery",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{auth_base_url}}/.well-known/openid-configuration",
              "host": ["{{auth_base_url}}"],
              "path": [".well-known", "openid-configuration"]
            },
            "description": "The document every other request here is derived from. Run this first: if it does not answer, nothing else will."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('discovery answers', () => pm.response.to.have.status(200));",
                  "const doc = pm.response.json();",
                  "pm.test('issuer matches the configured base url', () => {",
                  "  pm.expect(doc.issuer).to.eql(pm.variables.get('auth_base_url'));",
                  "});",
                  "// Store the endpoints so later requests use the advertised paths",
                  "// rather than paths copied into this file by hand.",
                  "pm.collectionVariables.set('token_endpoint', doc.token_endpoint);",
                  "pm.collectionVariables.set('userinfo_endpoint', doc.userinfo_endpoint);",
                  "pm.collectionVariables.set('jwks_uri', doc.jwks_uri);",
                  "pm.collectionVariables.set('revocation_endpoint', doc.revocation_endpoint);",
                  "pm.collectionVariables.set('end_session_endpoint', doc.end_session_endpoint);"
                ]
              }
            }
          ]
        },
        {
          "name": "JWKS",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{auth_base_url}}/oauth/v2/keys",
              "host": ["{{auth_base_url}}"],
              "path": ["oauth", "v2", "keys"]
            },
            "description": "The signing keys core-api verifies against, locally and cached, rather than introspecting per request (§1.4).\n\n**This is `{\"keys\": []}` on a freshly seeded stack.** Zitadel generates a signing key lazily, on the first token it issues, so an empty set is correct rather than broken. It matters for ENT-195: a JWKS cache populated at boot would cache nothing and then reject every token, which is exactly why the acceptance criterion is that an unknown `kid` triggers a refetch."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('serves a jwks document', () => {",
                  "  pm.response.to.have.status(200);",
                  "  pm.expect(pm.response.json().keys).to.be.an('array');",
                  "});",
                  "// Deliberately not asserting a non-empty set: see the description.",
                  "if (pm.response.json().keys.length === 0) {",
                  "  console.log('JWKS is empty, which is expected until the first token is issued.');",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Token, client credentials",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/x-www-form-urlencoded" }
            ],
            "body": {
              "mode": "urlencoded",
              "urlencoded": [
                { "key": "grant_type", "value": "client_credentials" },
                { "key": "client_id", "value": "{{client_id}}" },
                { "key": "client_secret", "value": "{{client_secret}}" },
                { "key": "scope", "value": "openid profile urn:zitadel:iam:org:project:id:{{project_id}}:aud" }
              ]
            },
            "url": {
              "raw": "{{auth_base_url}}/oauth/v2/token",
              "host": ["{{auth_base_url}}"],
              "path": ["oauth", "v2", "token"]
            },
            "description": "A machine token, for exercising the API without a browser. The seed job creates the `core-api-client` service user this needs (ENT-195), so it works against any stack brought up with `docker compose -f deploy/compose.yaml up -d`.\n\nFill `client_id`, `client_secret` and `project_id` from the shared volume rather than guessing:\n\n```bash\ndocker run --rm -v kindlast_zitadel-machinekey:/k alpine cat /k/core-api-client.json\ndocker run --rm -v kindlast_zitadel-machinekey:/k alpine cat /k/core-api-audience.txt\n```\n\nTwo details were measured against this stack rather than taken from a specification, and both cost an afternoon to rediscover.\n\n**`client_id` is the service user's username, not its id.** The secret response carries its own `clientId`, and that is the one to send. Passing the numeric user id returns `invalid_client: client not found`.\n\n**The audience is Zitadel's project id**, which is why the reserved scope `urn:zitadel:iam:org:project:id:{project}:aud` is in the scope list. Without it the token comes back scoped to the client itself, and core-api refuses it, correctly, as minted for someone else.\n\nThe scope story, which changed in ENT-197 and is worth reading before a `permission_denied` sends you looking in the wrong place. Zitadel's access tokens carry no `scope` and no `scp` claim whatever is requested here. With role assertion enabled they carry project roles under `urn:zitadel:iam:org:project:roles`, which `KINDLAST_OIDC_SCOPE_CLAIMS` points at, and that is what satisfies an endpoint declaring a real permission. `openid` is the exception: it is not a permission, nothing grants it, and verification asserts it, so an endpoint declaring it is reachable by any valid token. See `docs/core-api-configuration.md`.\n\nThe **user** flow is authorization code with PKCE and cannot be driven from Postman end to end, because it involves a login form and a redirect back to web. Use a browser for that."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// The service user is seeded (ENT-195), so a failure here is a",
                  "// real failure rather than an expected gap, and is asserted.",
                  "pm.test('token issued', () => pm.response.to.have.status(200));",
                  "",
                  "if (pm.response.code === 200) {",
                  "  const body = pm.response.json();",
                  "  pm.collectionVariables.set('access_token', body.access_token);",
                  "",
                  "  const claims = JSON.parse(atob(body.access_token.split('.')[1]",
                  "    .replace(/-/g, '+').replace(/_/g, '/')));",
                  "",
                  "  // core-api accepts one audience and only one. If this fails, the",
                  "  // reserved aud scope is missing or project_id is unset, and every",
                  "  // core-api call will read as an audience mismatch.",
                  "  pm.test('aud carries the project id', () =>",
                  "    pm.expect(String(claims.aud)).to.include(pm.environment.get('project_id')));",
                  "",
                  "  // Recorded, not asserted: this is the known gap, and failing here",
                  "  // would report a design decision as a defect.",
                  "  if (!claims.scope && !claims.scp) {",
                  "    console.log('No scope claim, as measured. core-api refuses this at the',",
                  "      'scope stage until ENT-197 enables role assertion.');",
                  "  }",
                  "}"
                ]
              }
            }
          ]
        },
        {
          "name": "Userinfo",
          "request": {
            "method": "GET",
            "header": [
              { "key": "Authorization", "value": "Bearer {{access_token}}" }
            ],
            "url": {
              "raw": "{{auth_base_url}}/oidc/v1/userinfo",
              "host": ["{{auth_base_url}}"],
              "path": ["oidc", "v1", "userinfo"]
            },
            "description": "The IdP's view of the subject. Note that core-api does not call this per request: it verifies the token locally (§1.4), so auth being down does not take the API down with it."
          }
        },
        {
          "name": "Revoke token",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/x-www-form-urlencoded" }
            ],
            "body": {
              "mode": "urlencoded",
              "urlencoded": [
                { "key": "token", "value": "{{access_token}}" },
                { "key": "client_id", "value": "{{client_id}}" },
                { "key": "client_secret", "value": "{{client_secret}}" }
              ]
            },
            "url": {
              "raw": "{{auth_base_url}}/oauth/v2/revoke",
              "host": ["{{auth_base_url}}"],
              "path": ["oauth", "v2", "revoke"]
            },
            "description": "Revocation reaches the refresh token. Access tokens already issued stay valid until they expire, which is why sign-out also adds the `jti` to a Redis deny-list (§1.7). Without that second step, sign-out looks like it works and does not, for up to ten minutes."
          }
        }
      ]
    },
    {
      "name": "web (OAuth client)",
      "description": "Browser-facing redirect endpoints on the Next.js app, not `/api/v1`. They are listed for completeness: most cannot be exercised from Postman, because they end in a redirect to a login form.\n\nNOT YET IMPLEMENTED: ENT-197.",
      "item": [
        {
          "name": "Login (NOT YET IMPLEMENTED, ENT-197)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{web_base_url}}/auth/login",
              "host": ["{{web_base_url}}"],
              "path": ["auth", "login"]
            },
            "description": "Builds the authorization URL with a PKCE verifier and state, then 302s. `?idp=google` adds an `idp_hint`.\n\nFollow redirects off to see the 302 rather than the login page."
          }
        },
        {
          "name": "Logout (NOT YET IMPLEMENTED, ENT-197)",
          "request": {
            "method": "POST",
            "header": [],
            "url": {
              "raw": "{{web_base_url}}/auth/logout",
              "host": ["{{web_base_url}}"],
              "path": ["auth", "logout"]
            },
            "description": "**POST with a CSRF token, never GET.** A GET logout can be triggered by a prefetch or an image tag, which signs the user out without them acting. ENT-197 asserts a GET is rejected."
          }
        }
      ]
    },
    {
      "name": "Core API v1",
      "description": "The proto surface, reachable from here as of ENT-197. core-api still binds no published port of its own, and CI asserts that absence rather than trusting it; the Caddy edge routes `/kindlast.core.v1.*` to it instead. That is the same door a container uses, which is the point: there is no development-only shortcut that stops existing in production.\n\nThese are Connect calls. Every one is a POST with a JSON body, including the ones that only read, and the path is the fully qualified proto name rather than a REST-style noun. An empty body is a parse error; the empty request message is `{}`.\n\nThe interceptor chain in front of these endpoints is live and tested as of ENT-195, and it is client-agnostic by construction: it verifies every token itself rather than trusting which door a request arrived through. `./scripts/validate-core-api-auth.sh` drives the whole chain and explains what each result proves.\n\nOne asymmetry to expect between this folder and a browser. The client-credentials token from the Auth folder is a machine token: it carries the project roles, so it passes endpoints declaring a real permission. A human token from the authorization-code flow cannot be obtained here at all, because that flow involves a login form and a redirect back to web. Use a browser for that, or `bun run --cwd apps/web test:e2e`, which drives it end to end.\n\nOnce buf emits OpenAPI (§23.2), these requests should be generated from it rather than maintained here by hand.",
      "item": [
        {
          "name": "Me (GetCurrentUser)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              {
                "key": "Kindlast-Org-Id",
                "value": "{{org_id}}",
                "description": "The active organisation travels per request rather than in the token, so switching organisation needs no re-minting (§20.1).\n\nDisable this header to exercise the bootstrap path: a caller who has never signed in has no organisation id to send, and the call has to work without one or a new user could never obtain one."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.SessionService/GetCurrentUser",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.SessionService", "GetCurrentUser"]
            },
            "description": "The console bootstrap call: user, memberships with roles, active organisation, plan.\n\nOn a subject it has never seen it provisions a personal organisation and an owner membership in one transaction, idempotent on the `sub` claim. Required scope: `openid`, declared on the proto method.\n\n**Answerable from here as of ENT-197**, now that the edge routes `/kindlast.core.v1.*` to core-api. It is a Connect call, so POST with a JSON body even though it reads: `{}` is the empty request message, and an empty body is a parse error rather than the same thing.\n\nWhat `openid` means here is worth knowing before it surprises you. It is not a permission and no authorization server grants it: verification asserts it, so a token that passed signature, issuer, audience and expiry carries it whether or not the IdP said anything about scopes. Measured on this stack, a real Zitadel access token carries seven project roles and no `openid` at all. That is why this endpoint is reachable by a caller holding no grants, which it has to be, because this is where their first grant comes from.\n\nTwo behaviours worth knowing before you read a response. **The organisation header is optional on the first call** and must be: a client that has never signed in cannot know an organisation id, because none exists yet, so omitting it resolves the caller's default. And a brand-new organisation has no subscription row, so `plan` comes back `free` rather than empty.\n\nHow the personal organisation gets its name is worth knowing, because it involves a call you cannot see from here. It is named from the token's display name or email, and Zitadel puts neither in an **access** token, so core-api asks the provider's userinfo endpoint with your own access token and names it from that. Only on the request that provisions, never on later calls. If userinfo is unreachable or the provider declares none, the name falls back to the raw subject id, which looks like `386250729179840515` and is the symptom to recognise.\n\nEach membership carries an `orgSlug` as of ENT-198, and that is what the console routes on: every authenticated page lives under `/o/{orgSlug}/`. It is derived from the name when the organisation is created and never recomputed, so it does not follow a rename. Use `orgName` for a label and `orgSlug` for a link; the two drift apart the moment anyone renames an organisation, and that is by design, because slugs live in bookmarks and in emailed approval links.\n\n**`user` carries two identifiers as of ENT-220, and they are not interchangeable.** `user.id` is the IdP subject claim, a Zitadel snowflake like `386250729179840515`. `user.userId` is core-api's own key for the same human, a version 5 uuid derived from (issuer, subject), and it is what `memberships`, `created_by`, `approved_by` and **`Member.userId` from ListMembers** all store.\n\nCompare `user.userId` when you want to know which row in a member list is the caller. Comparing `user.id` matches nothing and does so silently, which is the bug this field exists to close: before it, a console could list an organisation's members and not work out which one was the person reading the page, so an owner could not leave an organisation the API was perfectly willing to let them leave. The derivation is one-way, so you cannot compute one from the other client-side."
          }
        },
        {
          "name": "Create organisation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Acme Compliance GmbH\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/CreateOrganisation",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "CreateOrganisation"]
            },
            "description": "Creates an organisation and makes you its owner. **Implemented as of ENT-202.** Required scope: `org:manage`.\n\n**No organisation header, deliberately.** This is one of two calls that carry none, the other being Accept invitation: in both you are establishing membership rather than exercising it, so there is no active organisation to name. Omit the header rather than sending it empty (§1.8) — an empty value is a client bug and should be refused, while its absence is a statement.\n\nThe slug is derived from the name by `org_slug()` at insert time and is immutable from then on. Send a name with umlauts to watch the transliteration: `Müller GmbH` becomes `mueller-gmbh`, not `m-ller-gmbh`. If the slug is already taken, the collision retry appends an ordinal and you get `acme-compliance-gmbh-2`, which is the correct answer rather than an error.\n\nCopy the `orgId` from the response into the `org_id` environment variable to act inside the new organisation with the requests below."
          }
        },
        {
          "name": "Rename organisation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Acme Holdings GmbH\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/UpdateOrganisation",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "UpdateOrganisation"]
            },
            "description": "Renames the organisation the header names. **Implemented as of ENT-202.** Required scope: `org:manage`, and owner-only on top of that.\n\n**The slug does not follow the rename**, and the response returns it so you can see that rather than assume it. This is a product decision, not an oversight: slugs live in bookmarks and in emailed capability-token links, which are exactly the links a compliance product has to keep working. Rename a customer after an acquisition and every existing link still resolves.\n\nUse `name` for a label and `slug` for a link. They drift apart the moment anyone renames, by design.\n\nA member or viewer gets `permission_denied` rather than a call that appears to succeed and changes nothing."
          }
        },
        {
          "name": "List members",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/ListMembers",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "ListMembers"]
            },
            "description": "Everyone in the organisation the header names. **Implemented as of ENT-202.** Required scope: `org:read`, not `org:manage` — seeing who your colleagues are is not an administrative act, and requiring the management scope would mean a viewer could not render a page they are entitled to see.\n\n`displayName` and `email` are readable by every member regardless of role, which is a decision the product owner took explicitly on 2026-08-15 rather than a default nobody examined. Before that, `user_identities` was self-only and this endpoint could only have returned uuids. The reversal is narrow: it grants nothing outside a shared membership, and nothing outside the organisation the header names. See `db/migrations/00005_co_member_identity.sql`.\n\nBoth fields can be empty and a client must cope. `displayName` is absent when the authorization server returned no name claim, and both are absent for someone invited who has not yet signed in.\n\nCopy a `userId` from the response into the `member_user_id` environment variable for the two requests below."
          }
        },
        {
          "name": "Update member role",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"userId\": \"{{member_user_id}}\",\n  \"role\": \"member\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/UpdateMemberRole",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "UpdateMemberRole"]
            },
            "description": "Changes one member's role. **Implemented as of ENT-202.** Required scope: `org:manage`, owner-only.\n\nThree roles exist and no more: `owner`, `member`, `viewer`. Anything else answers `invalid_argument` naming the three, rather than letting the database's check constraint reply with `memberships_role_check`, which is a correct answer to the wrong audience.\n\n**Demoting the last owner is refused** with `failed_precondition`, not only removing them. Guarding removal alone would be theatre: a last owner who cannot be removed could be demoted to viewer and then removed, reaching the same ownerless organisation in two steps. An organisation with no owner has nobody who can invite, change roles or manage billing, and no way back short of an operator opening a database session.\n\nThe response re-reads the member rather than echoing what was sent, so it describes what the database holds."
          }
        },
        {
          "name": "Remove member",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"userId\": \"{{member_user_id}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/RemoveMember",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "RemoveMember"]
            },
            "description": "Removes someone from the organisation the header names. **Implemented as of ENT-202.** Required scope: `org:manage`, owner-only.\n\nRemoving the last owner answers `failed_precondition`; see Update member role for why that rule covers demotion too.\n\nA user id that is not a member of this organisation answers `not_found`, and so does one that does not exist at all. That ambiguity is deliberate: distinguishing them would turn this endpoint into a way of asking whether a given person belongs to a given organisation."
          }
        },
        {
          "name": "Invite member",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"email\": \"colleague@example.com\",\n  \"role\": \"member\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/InviteMember",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "InviteMember"]
            },
            "description": "Creates an invitation to the organisation the header names. **Implemented as of ENT-202.** Required scope: `org:manage`, owner-only. Invitations expire after seven days.\n\n**The response does not contain the token, and never will.** It carries `invitationId` and `expiresAt` only. The raw token exists in exactly two places: the email sent to the invitee, and a SHA-256 hash stored against the row. Returning it here would let anyone who can invite also redeem on the invitee's behalf, which defeats the point of a capability sent to an address.\n\nA practical consequence for anyone testing from here: you cannot chain this into Accept invitation. To exercise acceptance, write a fixture invitation with a token you already know, which is what the Go tests do via the store's `CreateInvitation`.\n\n**Email delivery works as of ENT-219.** The invitation and the email carrying it are written in one transaction: the raw token exists only inside this handler, so a row committed without its message would be permanently undeliverable rather than merely unsent. The message lands in `transactional_outbox` and a dispatcher drains it onto SMTP, which on the local stack is Mailpit at `localhost:8025`. Expect it within about ten seconds, which is the dispatcher's interval.\n\n**This can now answer `failed_precondition`,** and the message says so: a deployment with no `KINDLAST_APP_BASE_URL` refuses to invite rather than minting an invitation whose link cannot be built. That is not a caller error to retry. The alternative is worse than refusing, because the invitation would be stored, listed as pending, and impossible either to accept or to repair.\n\nA deployment with no `KINDLAST_SMTP_ADDR` still accepts the call. The message queues as `pending` and delivers whenever SMTP is configured, which is the outbox working rather than failing.\n\nEmail validation is deliberately shallow — non-empty and containing an `@`. Anything stricter rejects addresses that are valid under RFC 5321, and the authoritative test of an address is whether mail to it arrives."
          }
        },
        {
          "name": "Accept invitation",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"token\": \"{{invitation_token}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.OrgService/AcceptInvitation",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.OrgService", "AcceptInvitation"]
            },
            "description": "Ordering matters and is easy to get wrong: this must run **before** the first GetCurrentUser. Get it backwards and every invited user ends up owning a personal organisation they never asked for, alongside the one they were invited to. `web`'s callback enforces that ordering (ENT-197); a direct caller has to keep it themselves.\n\n**Implemented as of ENT-196**, and reachable from here as of ENT-197. Required scope is `openid`, not `org:manage`: the caller is redeeming a capability they were sent and holds no role anywhere until it succeeds, so an organisation scope would make a first invitation impossible to accept.\n\nThe token is sent in the clear here and stored hashed, so the value in the database never matches this one. **Issuing invitations exists as of ENT-202** (see Invite member above), and that endpoint still deliberately never returns the token, so you cannot chain the two from the response alone.\n\nOn a local stack you no longer need a hand-written fixture though: as of ENT-219 the invitation email is actually delivered, so call Invite member, open Mailpit at `http://localhost:8025`, and copy the token out of the link in the message. That is the same path a real invitee takes, which makes it a better test than a fixture. The Go tests still write a known token directly, because they must not depend on a mail server.\n\nExpired, already accepted and never existed all answer `not_found` alike, deliberately, so this cannot be used to discover which tokens are real.\n\nThe response carries `orgSlug` as of ENT-198, so a client can redirect straight into `/o/{orgSlug}/` rather than making a second call to find out where the organisation it just joined actually lives."
          }
        },
        {
          "name": "List findings",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"pageSize\": 20\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.FindingsService/ListFindings",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.FindingsService", "ListFindings"]
            },
            "description": "The feed: findings for the organisation the header names, newest first. Required scope: `findings:read`.\n\n**Implemented as of ENT-203.**\n\n**A signed-in human reaches this. The client-credentials token above does not, and that is correct rather than a gap.**\n\nENT-221 resolved in two parts. #134 granted the project roles, which the seed had been creating and giving to nobody. #135 went further: core-api now derives a human's scope set from the token's OAuth client rather than from granted roles, so every signed-in person holds the same fifteen scopes by construction. Verified in a browser, not inferred.\n\n**Machine clients are deliberately unchanged.** They still read granted scopes, which is what keeps the scope layer a real boundary for them, and `core-api-client` holds only `internal:*`. So the token this collection mints answers `permission_denied` here, from the scope interceptor rather than from the handler. Measured, not assumed.\n\nTo exercise this request as-is you would have to grant the service user `findings:read` in Zitadel. That is deliberately not seeded: `internal:*` is the vocabulary a machine is meant to hold, and granting it human scopes would blur the one distinction the client-class mechanism exists to draw. Reaching it as a human means going through `web`.\n\nPagination is keyset, not offset, over the `(org_id, created_at desc)` index 00002 built for this query. `pageToken` is opaque: do not parse or construct one. Send `pageSize: 0` (or omit it) to let the server choose. Treat a non-empty `nextPageToken` as the only signal that more exist — a full last page is ordinary and would otherwise read as another page waiting.\n\n`status` filters to one of `pending`, `approved`, `rejected`, `snoozed`. Omit it for every status, which is what the feed's default view wants: a finding somebody already dealt with is still part of the compliance record."
          }
        },
        {
          "name": "Get finding",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"findingId\": \"{{finding_id}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.FindingsService/GetFinding",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.FindingsService", "GetFinding"]
            },
            "description": "One finding plus the quoted regulatory text behind it. Required scope: `findings:read`. **Implemented as of ENT-203, and reachable by a signed-in human as of ENT-221.** See List findings for why this collection's client-credentials token still is not the way to call it.\n\nThe `supporting` array comes from `finding_supporting_chunks`, which is org-gated in the database as well as in the handler and returns no rows rather than raising when the finding belongs to someone else.\n\n**An empty `supporting` array is normal and is not an error.** The corpus does not cover every instrument an obligation can cite. Empty means \"we cannot show you the text here\", which is honest; it is not the same as the citation being absent, and a client renders the citation either way.\n\n**On the `citation` object, which is the part worth reading carefully.** Every field in it is a value the database already stored, assembled by `analyst_citation_label` and `analyst_citation_url` when the finding was created. Render `label` and link `url`. Do **not** rebuild the label from `celex` and `article`: a second assembler is a second thing that can be wrong, and the product's whole value is that a human can check the claim against the law."
          }
        },
        {
          "name": "Approve finding",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"findingId\": \"{{finding_id}}\",\n  \"reviewed\": true\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.FindingsService/ApproveFinding",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.FindingsService", "ApproveFinding"]
            },
            "description": "The act path. Required scope: `findings:act`, which is deliberately **not** `findings:read`: rendering the console and approving on a human's behalf are separately dangerous, and a display-only integration should not be one credential leak away from the second.\n\n**Implemented as of ENT-203**, database half included (`00006_act_path_audit.sql`, which is what makes the audit row exist at all). Reachable by a signed-in human as of ENT-221; see List findings for why this collection's client-credentials token still is not the way to call it.\n\n**Plan gating.** With `KINDLAST_BILLING_ENABLED` set, an organisation not on an active `pro` subscription gets `resource_exhausted` here rather than `permission_denied`: the two mean different things to the person reading the console, and only one of them has a button under it. Unset, which is the self-hosted default, the act path is ungated (\u00a718.1).\n\n**There is no actor field, and there will not be one.** The approving human is read from the session GUC the tenancy interceptor set, so a caller cannot name someone else as the approver. `approve_finding` lost its acting-user parameter in the ENT-192 rewrite for exactly this reason.\n\n**Idempotent.** Approving an already-approved finding returns `applied: false`, writes nothing and writes no second audit row. The usual cause is a double submit or a retry, neither of which is a problem worth showing a customer.\n\n`applied: false` also covers a finding that is unknown or belongs to another organisation. All three answer alike on purpose — telling them apart is what probing for a tenancy leak looks like.\n\n**`createdRecordId` is populated as of ENT-203, and which findings populate it is a data question rather than a code one.** 00007 gave obligations an `action_type` and made `analyst_convert_signal` copy it onto the finding; 00009 classified the three obligations where approving means something other than \"a human read it\". So approving a `gdpr-art-30-ropa` finding returns a `processing_activities` id, an Annex III or Article 26 finding returns an `ai_systems` id, and the other twelve obligations stay `review` and create nothing — which is the right outcome for an obligation whose remedy is not a row. Verified end to end through the console, not inferred from the migrations.\n\n**No obligation maps to `create_dsar`, and 00009 asserts that rather than leaving it to chance:** a data-subject request arrives from a person, so an obligation that manufactured one would be inventing the requester.\n\n`createdRecordId` is empty on a repeat approval even though the record exists, because nothing was created by that call. This field reports what this request did; `RecordsService` answers what is on file.\n\n`reviewed` records whether a human actually read the supporting text rather than approving from the feed card. \"Approved\" and \"approved having read the regulation\" are different claims and the audit trail can tell them apart."
          }
        },
        {
          "name": "Reject finding",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"findingId\": \"{{finding_id}}\",\n  \"reason\": \"Not applicable: we process no special-category data\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.FindingsService/RejectFinding",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.FindingsService", "RejectFinding"]
            },
            "description": "Rejects a finding, optionally saying why. Required scope: `findings:act`. **Implemented as of ENT-203**, and reachable by a signed-in human as of ENT-221; see List findings for why this collection's own token is not.\n\n**The reason is worth sending even though it is optional.** Three rejections of the same obligation on the same profile raise a `product_review_flags` row, carrying the distinct reasons given. That is the mechanism by which a bad rule gets noticed rather than being quietly dismissed by every customer in turn, and it only works if the reasons are there.\n\nIdempotent in the same way as approve: rejecting an already-rejected finding returns `applied: false`, keeps the first reason, and writes no second audit row."
          }
        },
        {
          "name": "Snooze finding",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"findingId\": \"{{finding_id}}\",\n  \"days\": 14\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.FindingsService/SnoozeFinding",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.FindingsService", "SnoozeFinding"]
            },
            "description": "Defers a finding. Required scope: `findings:act`. **Implemented as of ENT-203**, and reachable by a signed-in human as of ENT-221; see List findings for why this collection's own token is not.\n\n`days` is clamped to 1..365 by the database; `0` or an omitted value means the default of 7. A snoozed finding returns to `pending` when `expire_snoozed_findings()` next runs, and nothing schedules that yet — the pg_cron jobs were dropped in 00001 and the schedules move to Temporal at build-order step 8.\n\n**Unlike approve and reject, this is deliberately not idempotent.** Snoozing an already-snoozed finding writes a second audit row, because deferring again is a second decision with a new date. A trail that recorded only the first would say a finding was deferred once when a human deferred it four times, which is precisely what an auditor is looking for."
          }
        },
        {
          "name": "Run sweep",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"detectOnly\": false\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.platform.v1.SweepService/RunSweep",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.platform.v1.SweepService", "RunSweep"]
            },
            "description": "Runs the Watcher and then the Analyst for the organisation the header names. **Implemented as of ENT-203.** Required scope: `internal:ingest`.\n\n**This is the one request here the client-credentials token above is actually for.** Every other Core API request needs a human scope, which a machine client does not hold; this one needs `internal:ingest`, which is a machine scope and nothing else is meant to have.\n\n**The seed grants it as of ENT-221**, so this works from here where the others do not. That asymmetry is the design rather than an accident: `internal:*` is issued to service users and never to a browser client, and the human scope set in core-api deliberately contains none of it.\n\n**Why this endpoint exists at all.** Nothing runs the sweeps on a schedule. 00001 dropped the three pg_cron jobs when the schema moved off Supabase, and the schedules return with Temporal at build-order step 8. Between those points a perfectly correct feed shows an empty state forever, because nothing has ever looked. This is how you make something happen now, and when Temporal lands it becomes the thing Temporal calls rather than being replaced.\n\n**It runs as a different database role.** `core-api` serves requests as `kindlast_app`, which deliberately cannot create findings: the thing exposed to the world should not be able to fabricate a claim about a customer's legal exposure. Sweeps run on a second pool as `kindlast_agent` (00008). If `KINDLAST_AGENT_DATABASE_URL` is unset the service is not served at all and this answers `unimplemented`, which is a supported configuration rather than a fault.\n\n**Per-organisation, never global.** A request with no `Kindlast-Org-Id` is refused with `invalid_argument` rather than defaulting. Doing nothing would look like success, and sweeping everyone has every customer in its blast radius, so sweeping the estate is a loop somebody writes deliberately.\n\n`detectOnly` runs the Watcher and skips the Analyst. Use it when the feed is empty and you need to know which half is quiet: the Watcher finding nothing wrong is a different failure from the Analyst failing to resolve an obligation.\n\n`findings` lower than `signals` is normal, not an error. A signal whose obligation slug resolves to nothing is skipped and logged, because a finding with no obligation behind it could not cite anything."
          }
        },
        {
          "name": "Dashboard",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.DashboardService/GetDashboard",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.DashboardService", "GetDashboard"]
            },
            "description": "Posture, open counts by severity, and when the agents last ran. Required scope: `dashboard:read`, kept separate from `findings:read` so a viewer can be shown the summary without being entitled to every finding behind it. **Implemented as of ENT-203**, and reachable by a signed-in human as of ENT-221; see List findings for why this collection's own token is not.\n\n**`posture` has four values, and the fourth is the point:** `green`, `amber`, `red`, `not_assessed`. Before the Watcher has ever run there are no findings, and a posture derived by counting them reports green — telling a customer they are on track when the product has not looked (ENT-161). `not_assessed` is the only honest answer at that point.\n\n`pipeline` is what the agent rail (ENT-222) renders. `watcherLastRunAt` is absent when the Watcher has never run for this organisation, read from `compliance_profiles.watcher_last_run_at`. `profileExists` separates \"the agents have not run\" from \"there is nothing for them to run against\", which are different problems with different fixes.\n\n**Nothing currently runs the sweeps**, and that is deliberate rather than broken: 00001 dropped the three pg_cron jobs and the schedules move to Temporal at build-order step 8. So even a correct dashboard shows `not_assessed` until something invokes `run_watcher()` explicitly."
          }
        },
        {
          "name": "Notification preferences (read)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.NotificationService/GetNotificationPreferences",
              "host": ["{{api_base_url}}"],
              "path": [
                "kindlast.core.v1.NotificationService",
                "GetNotificationPreferences"
              ]
            },
            "description": "The caller's own notification settings for the organisation the header names. **Implemented as of ENT-209.** Required scope: `notifications:read`.\n\n**There is no user id in the request, and there will not be one.** `notification_preferences` is keyed `(org_id, user_id)` and its policies are the only ones in the schema that pin `user_id` to `app.current_user_id`, so the row is the caller's by policy rather than by argument. A parameter would invite a client to pass somebody else's and make the handler the thing that refuses, when the policy already does.\n\nPreferences are **personal within an organisation**. Somebody in two organisations holds two sets and can want a weekly briefing from one and silence from the other.\n\n**Never answers not-found.** Somebody who has never opened the settings page has no row and is nonetheless subscribed, because the product default is that a member is told: the opposite default means a critical finding sits unread until each person individually opts in. So this returns defaults (`medium` and above, briefing on, deadline alerts on, `Europe/Tallinn`) rather than an error, and \"I have not changed anything\" stays distinguishable from a fault."
          }
        },
        {
          "name": "Notification preferences (write)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"preferences\": {\n    \"email\": \"compliance@example.com\",\n    \"minSeverityForEmail\": \"medium\",\n    \"weeklyBriefingEnabled\": true,\n    \"deadlineAlertsEnabled\": true,\n    \"timezone\": \"Europe/Tallinn\",\n    \"quietHoursStart\": \"22:00\",\n    \"quietHoursEnd\": \"07:00\"\n  }\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.NotificationService/UpdateNotificationPreferences",
              "host": ["{{api_base_url}}"],
              "path": [
                "kindlast.core.v1.NotificationService",
                "UpdateNotificationPreferences"
              ]
            },
            "description": "Replace the caller's own settings. **Implemented as of ENT-209.** Required scope: `notifications:write`, which `notifications:read` does not satisfy: scope matching is exact, never by prefix.\n\n**A whole-object write, not a patch.** The settings surface renders every field at once, and a partial update would mean deciding what an absent field means, where \"unset\" and \"set to false\" are different intentions a proto3 scalar cannot tell apart. The REST alias is `PUT /api/v1/notification-preferences` for the same reason, unlike the records surface which is `PATCH`: here the body genuinely is the resource, with no server-owned columns to preserve.\n\n`minSeverityForEmail` is a **floor**, one of `low`, `medium`, `high`, `critical`. Not a switch, because \"tell me everything\" trains people to ignore the mail and \"tell me nothing\" is how a critical finding goes unread.\n\n`email` empty means \"use the address I sign in with\". It is a separate field because a compliance contact is often a shared mailbox nobody signs in as.\n\nQuiet hours are local `HH:MM` and **may wrap midnight**: `22:00` to `07:00` means what it looks like. Both empty means no quiet window; one alone is refused, because \"quiet from 22:00\" with no end reads to a person as overnight and would behave as no quiet hours at all.\n\nAnswers `invalid_argument` for an unknown severity, an IANA timezone that will not load, a time that is not `HH:MM`, or half a quiet window. The timezone is checked here rather than at send time, because an unloadable zone at send time is a notification that fails hours later for a reason nobody connects to a settings page.\n\nThe response is re-read from the database rather than echoed, so it describes what is stored."
          }
        },
        {
          "name": "Notification capabilities",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.NotificationService/GetNotificationCapabilities",
              "host": ["{{api_base_url}}"],
              "path": [
                "kindlast.core.v1.NotificationService",
                "GetNotificationCapabilities"
              ]
            },
            "description": "Which channels this deployment can actually deliver on (§18.3). **Implemented as of ENT-209.** Required scope: `notifications:read`.\n\nExists so a console never renders a channel it cannot deliver on. Telegram, Slack and WhatsApp are in the design and are not built, and they are **absent from this response rather than listed as unavailable**: a settings page offering a switch that silently does nothing is indistinguishable from a broken integration, and generates support instead of value.\n\n`email` is present always and `available` is false on a deployment with no `KINDLAST_SMTP_ADDR`, carrying a plain-language `unavailableReason` for the console to show. Read from configuration rather than probed, because a mail server that happens to be down is not the same as a deployment that was never told where to submit mail, and only the second is worth telling a person about.\n\nDeployment-wide rather than tenant-scoped: the answer is the same for every organisation in an installation. The organisation header is still sent because the chain requires it."
          }
        },
        {
          "name": "Unsubscribe (no session)",
          "request": {
            "method": "POST",
            "header": [{ "key": "Content-Type", "value": "application/json" }],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"token\": \"paste-the-token-from-the-unsubscribe-link\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/api/v1/unsubscribe",
              "host": ["{{api_base_url}}"],
              "path": ["api", "v1", "unsubscribe"]
            },
            "description": "Spend an unsubscribe token. **Implemented as of ENT-209.**\n\n**The only endpoint in this collection with no Authorization header and no organisation header, and that is the whole point.** The person clicking an unsubscribe link is reading an email, may never have signed in, and requiring them to authenticate in order to stop mail they did not ask for is how a product earns a spam complaint instead of an unsubscribe. So the token is the only identity claim, which is why it is stored hashed, expires after 30 days, and is spent once.\n\nIt is therefore **not on the Connect surface**: every method there runs behind authentication, a scope check and tenancy, and this caller has none of them. It is a plain route on core-api's mux beside `/healthz`.\n\n**POST rather than GET, deliberately.** Corporate mail gateways and link scanners follow every URL in a message before a human sees it, so under a GET the act of delivering the email would unsubscribe the recipient. `web` renders a confirmation page at `/unsubscribe/{token}` and posts here.\n\n**404 for every unusable token**, whether expired, already redeemed, of the wrong kind, or never real. Distinguishing them would make this an oracle for which tokens exist, to a caller who has proved nothing. 404 rather than 401 for the same reason: 401 invites a client to authenticate and there is nothing to authenticate as.\n\nOn success it returns the organisation the token named, and turns off the weekly briefing and deadline alerts and raises the email floor to `critical` for that person in that organisation alone.\n\nTo try it on the local stack: trigger a finding, read the message in Mailpit at `http://localhost:8025`, and copy the token out of the unsubscribe link."
          }
        },
        {
          "name": "List processing activities (ROPA)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"pageSize\": 20\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/ListProcessingActivities",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "ListProcessingActivities"]
            },
            "description": "The Article 30 record of processing activities. Required scope: `records:read`.\n\n**Served as of ENT-200.** Approving a `create_ropa` finding already writes a row here, and the console renders this register at /o/{slug}/records.\n\nPagination is keyset over `(org_id, created_at desc)`, not offset, for the reason List findings gives. `pageToken` is opaque: do not parse or construct one. Treat a non-empty `nextPageToken` as the only signal that more exist.\n\n**Ordered by creation, not by `updated_at`**, which is a deliberate change from the legacy console. Ordering a paginated list by a column that changes under it lets a row edited between two page requests be skipped or returned twice. \"What did I touch last\" is a sort a client can offer within a page.\n\n**`completeness` is computed server-side**, one of `complete`, `incomplete`, `review_needed`. `review_needed` means the Executor pre-filled the entry and no human has edited it since, which is a different state from incomplete and wants a different prompt. `complete` additionally requires the row to have been updated after it was created, which is what stops a stub from ever reading as complete without a human having looked.\n\n**`manualQuota`** carries the Free-tier cap on manually-created activities (ENT-84). `limit: 0` means unlimited, which is what a paid plan and a billing-disabled self-hosted deployment both report — check for zero rather than rendering \"3 of 0 used\". Executor-created rows never count against it: a record created from an approved finding is part of the compliance record and is not withheld behind a plan."
          }
        },
        {
          "name": "Get processing activity",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"processingActivityId\": \"{{processing_activity_id}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/GetProcessingActivity",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "GetProcessingActivity"]
            },
            "description": "One entry in the Article 30 record. Required scope: `records:read`. **Served as of ENT-200.**\n\n**The empty fields are the point, not a gap.** `approve_finding` creates the row from a finding that knows the activity exists and not what is in it, so `purpose`, `legalBasis` and `retentionPeriod` come back empty on an Executor-created stub. Article 30 is a record of fact, and a plausible guess in `legalBasis` is the single worst thing this product could write.\n\n`sourceFindingId` is set when the Executor created this and empty when a human did. \"Why is this here\" is the first question an auditor asks of a compliance record."
          }
        },
        {
          "name": "List AI systems",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"pageSize\": 20\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/ListAiSystems",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "ListAiSystems"]
            },
            "description": "The AI Act system register. Required scope: `records:read`. **Served as of ENT-200.** Approving an `ai-act-annex-iii-high-risk-systems` or `ai-act-art-26-deployer-obligations` finding already writes a row here (00009).\n\n**`riskClassification` defaults to `unclassified`, deliberately not `minimal`.** Article 6 classification is a judgement with consequences, and a register defaulting to the lowest tier tells a customer they are fine about a system nobody has assessed. Same reasoning as `not_assessed` on the dashboard posture, applied to a record instead of a summary. Values: `unacceptable`, `high`, `limited`, `minimal`, `unclassified`.\n\n`documentationStatus` is `missing`, `in_progress` or `complete`: how far the Article 11 technical documentation has got.\n\n`lastReviewedAt` absent means never reviewed, which for a register driving high-risk obligations is worth showing rather than hiding behind a created date.\n\n**Measured, and worth knowing before you profile this:** `ai_systems` carried only an `(org_id)` index from 00002 while its two sibling registers had the composite. Migration 00011 adds `(org_id, created_at desc)` and drops the redundant single-column index, because a keyset page over an unsupported ordering sorts the whole tenant's rows on every request."
          }
        },
        {
          "name": "Get AI system",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"aiSystemId\": \"{{ai_system_id}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/GetAiSystem",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "GetAiSystem"]
            },
            "description": "One system in the AI Act register. Required scope: `records:read`. **Served as of ENT-200.**\n\n`vendor` empty means built in house, which is a meaningful empty rather than a missing one: the AI Act's provider and deployer duties differ, and an organisation that built the system is generally both."
          }
        },
        {
          "name": "List DSARs",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"pageSize\": 20\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/ListDsars",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "ListDsars"]
            },
            "description": "Data-subject access requests and the statutory clock on each. Required scope: `records:read`. **Served as of ENT-200.**\n\n**Ordered by `responseDueAt` ascending, soonest first**, not by creation, because the only question anyone asks of a DSAR log is which one runs out next. That means a `pageToken` from this list is **not** interchangeable with the ones from the other two registers. Migration 00011 adds the `(org_id, response_due_at)` index this needs; the pre-existing `dsars_due_idx` looks like the right index and is not, because it does not lead with `org_id` and is partial on the two unfinished statuses.\n\n**Nothing in this register arrives from an approval, unlike the other two.** 00009 classified three obligations and deliberately mapped none to `create_dsar`, and 00010 asserts that: a data-subject request arrives from a person, so an obligation that manufactured one would be inventing the requester. Every row here was logged by a human, and `sourceFindingId` is therefore always empty today. It is carried rather than omitted so that going from always-empty to sometimes-set is not a contract change.\n\n**`urgency` is computed server-side**, one of `overdue`, `due_soon`, `on_track`, `answered`, with `due_soon` meaning ten days or fewer remaining. That window is a regulatory escalation threshold, and a client re-deriving it from `responseDueAt` would be a second implementation of a deadline that would eventually disagree with this one. `daysUntilDue` is carried alongside it because \"4 days left\" and \"11 days overdue\" are the words a handler acts on.\n\n`status` filters to `open`, `in_progress`, `responded` or `closed`. Omit it for every status: a closed request is still part of the record an auditor asks to see."
          }
        },
        {
          "name": "Get DSAR",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"dsarId\": \"{{dsar_id}}\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/GetDsar",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "GetDsar"]
            },
            "description": "One data-subject request. Required scope: `records:read`. **Served as of ENT-200.**\n\n**`receivedAt` is not the row's creation timestamp and the two routinely differ (ENT-224).** A request that arrived by post on the 1st and was logged on the 8th is due one month from the 1st. Article 12(3) runs from receipt, so a clock started at data entry silently grants the organisation the days it took them to notice, which is exactly backwards. `00010_dsar_clock_from_receipt.sql` made the executor path refuse a missing, non-timestamp or future `received_at` rather than defaulting to `now()`.\n\n`responseDueAt` is stored rather than computed on read, so an extension is recorded as a fact about this request instead of being re-derived from a rule that may since have changed. The Article 12(3) two-month extension for complex requests is **not yet modelled**; 00010 left it open as a schema question.\n\n`requestType` is free text rather than an enum on purpose: Article 15-22 requests arrive worded by the person making them, and forcing them into a closed list at intake loses what was actually asked for."
          }
        },
        {
          "name": "Create processing activity",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"fields\": {\n    \"name\": \"Payroll\",\n    \"purpose\": \"Paying staff and meeting tax obligations\",\n    \"legalBasis\": \"Article 6(1)(b), performance of a contract\",\n    \"dataCategories\": [\"name\", \"bank details\"],\n    \"recipients\": [\"our accountant\"],\n    \"retentionPeriod\": \"7 years after employment ends\"\n  }\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/CreateProcessingActivity",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "CreateProcessingActivity"]
            },
            "description": "Adds an Article 30 entry the Executor has not seen. Required scope: `records:ropa:write`, which is deliberately not `records:read`. **Served as of ENT-200.**\n\n**Capped on the free plan, and over the cap answers `resource_exhausted`, not `permission_denied`.** The caller is entitled to do this and has run out of allowance, which is a distinction with a button under it: one has an upgrade, the other sends somebody to an owner who cannot help. The cap counts only manually created rows, because a record created from an approved finding is part of the compliance record and is never withheld behind a plan.\n\n**`failed_precondition` here means the organisation has no compliance profile**, so there is nothing for a record to hang off. Finish onboarding first.\n\nThere is no actor field and there will not be one. `The store reads the acting human from the session GUC, enforces the cap, and writes the audit row in the same transaction as the insert. A handler writing the table directly would have to reimplement all three."
          }
        },
        {
          "name": "Update processing activity",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"processingActivityId\": \"{{processing_activity_id}}\",\n  \"fields\": {\n    \"name\": \"Payroll\",\n    \"purpose\": \"Paying staff\",\n    \"legalBasis\": \"Article 6(1)(b)\",\n    \"dataCategories\": [\"name\", \"bank details\"],\n    \"recipients\": [\"our accountant\"],\n    \"retentionPeriod\": \"7 years\"\n  }\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/UpdateProcessingActivity",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "UpdateProcessingActivity"]
            },
            "description": "Replaces an entry's fields. Required scope: `records:ropa:write`. **Served as of ENT-200.**\n\n**A full replacement, not a patch, and the field mask that would make it a patch is deliberately absent.** Article 30 is a record of fact whose gaps are meaningful: clearing a legal basis is a real edit somebody makes when they realise the one recorded was wrong, and a partial update in which an omitted field means \"leave it alone\" gives a client no way to express that. Send every field you want the record to have.\n\nAn unknown id and one belonging to another organisation both answer `not_found`, because the database function's lookup is org-scoped and cannot tell them apart either."
          }
        },
        {
          "name": "Create AI system",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"fields\": {\n    \"name\": \"CV ranking model\",\n    \"vendor\": \"\",\n    \"purpose\": \"Ranks applicants for shortlisting\",\n    \"riskClassification\": \"high\",\n    \"documentationStatus\": \"missing\"\n  },\n  \"reviewed\": true\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/CreateAiSystem",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "CreateAiSystem"]
            },
            "description": "Registers a system nobody has approved a finding about. Required scope: `records:ai-systems:write`. **Served as of ENT-200.**\n\nUseful mostly for shadow AI: the system somebody adopted without telling anyone is exactly the one no finding will ever raise.\n\n**`reviewed` is required when `riskClassification` is `high`**, and the body above will answer `failed_precondition` if you set it to false. See Update AI system for why.\n\nAn empty `riskClassification` means `unclassified`, which is the honest default and deliberately not `minimal`: a register that defaulted to the lowest tier would tell a customer they are fine about a system nobody has assessed."
          }
        },
        {
          "name": "Update AI system",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"aiSystemId\": \"{{ai_system_id}}\",\n  \"fields\": {\n    \"name\": \"CV ranking model\",\n    \"vendor\": \"\",\n    \"purpose\": \"Ranks applicants for shortlisting\",\n    \"riskClassification\": \"limited\",\n    \"documentationStatus\": \"in_progress\"\n  },\n  \"reviewed\": true\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/UpdateAiSystem",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "UpdateAiSystem"]
            },
            "description": "Replaces a system's fields. Required scope: `records:ai-systems:write`. **Served as of ENT-200.**\n\n**ANY change to `riskClassification` is refused unless `reviewed` is true**, and the refusal is `failed_precondition` rather than `permission_denied`: the caller may do this and must confirm first, so a console shows a confirm step rather than an upgrade prompt or an owner's name.\n\nThe reason is that an Article 6 classification is the determination the whole high-risk obligation stack hangs off. Moving a system out of `high` in a form somebody was tabbing through would quietly retire Articles 9 to 17 for it, and the record would show a change with nobody's deliberate decision behind it. `reviewed` says a human meant it, and the audit row records that they did.\n\n**An edit that leaves the classification alone needs no confirmation**, which is what keeps the gate from being a nuisance on every save. **The gate lives in Go as of ENT-225**, and this sentence used to say the opposite. It read: \"the gate lives in `update_ai_system`, not in the handler: a check in Go would be a second implementation, and the copy furthest from the data is the one that drifts.\" That was a reasonable argument and it lost. A classification change requiring a reviewed approval is a decision, not an invariant, and 00016 dropped the function. There is exactly one implementation either way; what changed is which language it is in and whether it can be table-tested without a database."
          }
        },
        {
          "name": "Log DSAR",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"subjectName\": \"M. Laurent\",\n  \"requestType\": \"erasure\",\n  \"handler\": \"Privacy team\",\n  \"receivedAt\": \"2026-08-01T00:00:00Z\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/LogDsar",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "LogDsar"]
            },
            "description": "Records a request that arrived. Required scope: `records:dsar:write`. **Served as of ENT-200.**\n\n**This is the only way a DSAR enters the register.** Nothing creates one from an approval: 00009 classified three obligations and mapped none to `create_dsar`, because a request comes from a person and an obligation that manufactured one would be inventing the requester.\n\n**`receivedAt` decides the deadline, and omitting it means today (ENT-224).** Before migration 00012 there was no such field: `log_dsar` stamped `now()`, so a request that came by post on the 1st and was logged on the 8th was recorded as due a month from the 8th. Article 12(3) runs from receipt, so a clock started at data entry silently grants the organisation the days it took them to notice, and the slower they are the longer they appear to have.\n\nA **future date is refused** with `invalid_argument` rather than clamped: a request cannot have arrived tomorrow, and the likeliest cause is a typo that would extend a statutory deadline nobody chose to extend. `invalid_argument` rather than `failed_precondition` because the fix is a different value, not a condition to satisfy and retry.\n\nThe response carries the computed `responseDueAt`, which is the field the person who logged it actually needs to know.\n\n`subjectName` is optional: a request can arrive pseudonymously, through a form or an inbox, before anyone has identified the person behind it. `handler` is optional and free text, because it is often a role or an external DPO rather than a member of the workspace."
          }
        },
        {
          "name": "Mark DSAR responded",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"dsarId\": \"{{dsar_id}}\",\n  \"reviewed\": true\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.RecordsService/MarkDsarResponded",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.RecordsService", "MarkDsarResponded"]
            },
            "description": "Records that a response went out, which stops the statutory clock. Required scope: `records:dsar:write`. **Served as of ENT-200.**\n\n**`reviewed` must be true**, for the same class of reason Update AI system needs it: this is the assertion that the organisation met an Article 12(3) deadline, and it is the single most consequential row in this register to get wrong. A regulator reading the log later is reading this field. Without it, `failed_precondition`.\n\n**Idempotent.** Marking an already-answered request returns `applied: false` and writes nothing, rather than overwriting the date a response actually went out with the date somebody clicked the button twice. The status is read BEFORE the call rather than after, which matters: reading it afterwards reports `applied` on every repeat, and that is a bug the findings act path shipped with and had to fix.\n\n**Worth knowing if you script against this:** every gate here is a `raise exception`, and a raise aborts the whole Postgres transaction. Each request gets its own transaction so a refusal costs nothing, but two writes in one request will see the second fail with `25P02` for a reason unrelated to it."
          }
        },
        {
          "name": "Billing (GetBilling)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.BillingService/GetBilling",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.BillingService", "GetBilling"]
            },
            "description": "What this organisation is on, and what this deployment can offer it. Required scope: `billing:read`. **Served as of ENT-210.**\n\n**Read only, and BillingService has no write RPC at all.** A plan changes because a payment provider's signed webhook said so. An endpoint that could change one from a session would be a way to grant yourself a paid entitlement with a request, so it does not exist rather than existing behind a check.\n\n**Owner-only, refused with `permission_denied` for a member.** The scope says what a token may attempt and the role says who inside the organisation may; a member holds `billing:read` like everybody else, so the handler checks the role as well. The console renders 404 rather than the refusal, because \"you are not allowed\" is itself information about the organisation.\n\n**`plan` and `status` are different questions and both are returned.** `plan` is the entitlement in force, so a `pro` subscription that is `past_due` or `canceled` reports `plan: \"free\"` here. A client that rendered only `plan` would tell a paying customer whose card was declined that they had downgraded themselves, which is why `status` is carried separately.\n\n**`billingConfigured` is read from configuration, not inferred from the absence of a subscription row.** A hosted customer between provisioning and their first row looks identical to a self-hoster, and would otherwise be shown a page saying the product cannot be bought. `gatingEnabled` is separate again, because an operator may wire a provider before turning gating on, and then nothing is withheld and nothing should be advertised as withheld.\n\nAn organisation that has never bought anything has no subscription row and reports `plan: \"free\"` with an empty `status`."
          }
        },
        {
          "name": "Billing webhook (no session, signed)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              {
                "key": "Stripe-Signature",
                "value": "t={{$timestamp}},v1=compute-this-yourself-see-description"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"id\": \"evt_local_001\",\n  \"type\": \"customer.subscription.updated\",\n  \"data\": {\n    \"object\": {\n      \"customer\": \"cus_local_001\",\n      \"status\": \"past_due\",\n      \"current_period_end\": 1789000000,\n      \"items\": {\n        \"data\": [{ \"price\": { \"lookup_key\": \"pro\" } }]\n      }\n    }\n  }\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/api/v1/billing/webhook",
              "host": ["{{api_base_url}}"],
              "path": ["api", "v1", "billing", "webhook"]
            },
            "description": "Applies a payment provider's subscription event. **Implemented as of ENT-210.**\n\n**Unauthenticated, and it changes what a customer is entitled to.** No session, no token, no caller identity: anybody on the internet can POST here. The only thing separating a real event from an invented one is the HMAC in `Stripe-Signature`, which is why it is verified **before the body is parsed** and before anything is read out of it. A handler that unmarshals first has already let the caller choose what its parser sees.\n\nNot on the Connect surface for the same reason as Unsubscribe: every method there runs behind authentication, a scope check and tenancy, and this caller has none of them. It is a plain route on core-api's mux, and the edge routes exactly this one path to core-api rather than the whole `/api/v1/*` prefix.\n\n**Signing a request by hand**, which you must do because Postman cannot compute it for you:\n\n```\nt=$(date +%s)\nbody='{\"id\":\"evt_local_001\",...}'   # byte for byte what you send\nprintf '%s.%s' \"$t\" \"$body\" \\\n  | openssl dgst -sha256 -hmac \"$KINDLAST_BILLING_WEBHOOK_SECRET\" -hex\n```\n\nThen send `Stripe-Signature: t=<t>,v1=<hex>`. The signed payload is `timestamp + \".\" + raw body`, so any reformatting of the JSON invalidates it. The header may carry several `v1=` values and any one matching is accepted, which is what makes a secret rotation not an outage.\n\n**The timestamp is checked in both directions**, five minutes either side. A signature would otherwise be valid forever, and a timestamp far in the future is as much a forgery signal as one far in the past.\n\n**Every refusal is a bare 4xx with no body.** No signature, a bad signature and an expired one are indistinguishable to the caller on purpose: an endpoint that explains why it refused is one an attacker can use to search for a working forgery.\n\n**Replays return 200 and change nothing.** The event id is claimed in the same transaction as the subscription write, so a retried delivery hits the primary key and the whole transaction rolls back. Providers retry on any non-2xx and on timeouts, so a 4xx here would be retried forever and a 500 would be retried on something already applied.\n\n**An unknown customer is also 200 and also records nothing**, deliberately: a customer this deployment has not seen may be one whose checkout has not landed yet, and burning the event id would make the provider's retry a permanent no-op.\n\n**Only three event types do anything**: `customer.subscription.created`, `.updated` and `.deleted`. An allow-list rather than a switch with a default, so a provider adding a type cannot silently start moving plans. Everything else is acknowledged and ignored.\n\nThe plan comes from the price's **`lookup_key`**, not its id: a price id is generated per environment and would make this deployment-specific. Anything other than `pro` reads as `free`, and any status this code does not recognise reads as `canceled`, because an unknown must never grant.\n\nThe request runs on its own Postgres role, `kindlast_billing`, which holds grants on two tables and nothing else. It resolves the customer to an organisation through its one unscoped select, sets `app.current_org_id` to what it resolved, and only then writes: a crafted payload cannot turn one event into an upgrade for a different tenant, because the write matches zero rows rather than the wrong ones.\n\nTo try it locally, set `KINDLAST_BILLING_ENABLED=true` and a `KINDLAST_BILLING_WEBHOOK_SECRET` in the compose environment, and give an organisation a `provider_customer_id` to resolve against."
          }
        },
        {
          "name": "List audit entries",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"filter\": {\n    \"since\": \"2026-08-01T00:00:00Z\",\n    \"until\": \"2026-09-01T00:00:00Z\",\n    \"actionTypes\": [\"approve_finding\", \"reject_finding\", \"snooze_finding\"]\n  },\n  \"pageSize\": 50\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.AuditService/ListAuditEntries",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.AuditService", "ListAuditEntries"]
            },
            "description": "What was decided, by whom, and when. Required scope: `audit:read`. **Served as of ENT-223.**\n\n**This surface reads `audit_log` and nothing else.** Not traces, not model calls, not anything an observability tool holds (§7.2). That firewall is the product rather than an implementation detail: an auditor is buying a record a regulator can be shown, and a record assembled partly from a vendor's telemetry has completeness that depends on that vendor's retention settings. `audit_log` is append-only by trigger, lives in the customer's own database, and is written in the same transaction as the act it describes.\n\n**Read only, and structurally so.** AuditService has no write RPC and will not get one. The table carries an append-only trigger, `kindlast_app` holds no update or delete grant on it, and the only insert path is bound by policy to the human in the GUC.\n\n**No role gate**, unlike BillingService. Every member sees this, viewers included: a plan and a renewal date are commercial facts about the company, but \"who approved this finding\" is the shared record of the work these people did together. The organisation boundary is still absolute and supplied by RLS.\n\n**There is no failure dimension and no `level` field**, which is the one real difference from the infrastructure log views this is modelled on. A refused act writes nothing, because the gates are constraints and policies and a refusal aborts the transaction the row would have been written in. Every row here is a thing that happened.\n\n**`filter.until` is EXCLUSIVE.** Pulling January and then February with inclusive bounds returns any boundary row twice, and a duplicated decision in an audit file is a question nobody wants to have to answer. A range whose end is at or before its start is `invalid_argument` rather than swapped or ignored: both of those look like they worked, and in an export the difference is a month of decisions versus all of them.\n\n**`filter.query` does not search `before` and `after`.** Those hold whatever the acted-on row contained, which for a DSAR includes a data subject's name, and a substring search across them would turn the audit log into a search engine over the personal data it exists to account for. It searches the action type, the target table, and the actor's name and email. The payloads are still returned in full: it is the search that stops at the boundary, not the record.\n\n**`actor.actorRole` is the role held AT THE TIME**, snapshotted into the row rather than resolved now. A client that looked it up would relabel a past act every time somebody's role changed. `actor.displayName` and `actor.email` are empty for an actor who has left, and the row is still returned: a log that dropped entries when somebody was offboarded would be defeatable by offboarding somebody.\n\n**`agentRunId` and `actor.kind` are declared and always unpopulated.** `agent_runs` arrives with ENT-218 (§26). They exist now so the day an agent produces an act is a server change rather than a contract change, and so nothing gets written on the assumption that every actor is a human.\n\nPagination is keyset over `(occurred_at desc, id desc)`. The id tie-break matters more here than in the other registers: an act writes the decision and the record it created in ONE transaction, so both rows share `occurred_at` exactly, and without it a cursor skips a row or repeats one forever. `pageToken` is opaque; `pageSize` defaults to 50 and caps at 200.\n\n`availableActionTypes` lists every action type present in this organisation's log, ignoring the filter, so a console can offer only values that would return something."
          }
        },
        {
          "name": "Export audit entries (CSV)",
          "request": {
            "method": "POST",
            "header": [
              { "key": "Content-Type", "value": "application/json" },
              { "key": "Authorization", "value": "Bearer {{access_token}}" },
              { "key": "Kindlast-Org-Id", "value": "{{org_id}}" }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"filter\": {\n    \"since\": \"2026-08-01T00:00:00Z\",\n    \"until\": \"2026-09-01T00:00:00Z\"\n  },\n  \"format\": \"EXPORT_FORMAT_CSV\"\n}"
            },
            "url": {
              "raw": "{{api_base_url}}/kindlast.core.v1.AuditService/ExportAuditEntries",
              "host": ["{{api_base_url}}"],
              "path": ["kindlast.core.v1.AuditService", "ExportAuditEntries"]
            },
            "description": "The same filtered set, as a file an auditor can attach to a report. Required scope: `audit:read`. **Served as of ENT-223.**\n\n**A separate RPC rather than a flag on the list**, because the two answer different questions. The list answers \"what is on this page\" and is paged by construction. This answers \"everything that matched\", and an export that silently stopped at a page boundary is worse than none: the auditor cannot see the truncation, and the file is the artefact that leaves the building.\n\n**Check `truncated` on every response.** It is true when the export hit its 50,000 row cap, and it is the only signal there is: a truncated CSV is a valid CSV that simply stops. `rowCount` says how many rows the file holds. Narrow the range and export again.\n\n**The response is a `oneof` and today it is always `content`.** §7 designs the export as a request returning a signed download URL and §25 leaves synchronous-versus-job open. A synchronous body is right at today's volumes; the shape that grows into a URL is decided now, so the day a customer's log outgrows one response it becomes `download` with no contract break and no second RPC. **The synchronous path is the interim, not the decision.**\n\n`content.data` is base64 over Connect's JSON codec, which is how proto `bytes` travel. Decode it before writing the file.\n\n**The file carries a UTF-8 BOM**, for the unglamorous reason that Excel on Windows otherwise reads UTF-8 as the local code page and mangles every non-ASCII name. The names that get mangled are disproportionately not English ones, and this is a record of who did what.\n\n**Spreadsheet formula injection is defused.** Any cell opening with `= + - @` or a leading tab is prefixed with an apostrophe, OWASP's mitigation. Several columns carry text a user chose (a display name, and the `before`/`after` payloads), the person opening the file is an auditor on a corporate laptop, and spreadsheets evaluate those cells. **This changes the exported bytes deliberately**: the record of truth is `audit_log` in the customer's own database, untouched and append-only, and a rendering that can execute is not a safer record. A parser consuming these files should strip a single leading apostrophe.\n\n**The column order is a contract.** Somebody will point a spreadsheet at column C. Append, never rearrange: `occurred_at, action_type, actor_user_id, actor_name, actor_email, actor_role, finding_id, target_table, target_id, before, after, agent_run_id, audit_id`.\n\nTimestamps are RFC 3339 in UTC, not a friendly local format. This file is read in other timezones and possibly years later, and `14:32` with no offset is a fact nobody can verify.\n\n**`format` may be omitted**, and UNSPECIFIED is accepted as CSV. It is the only format there is, so refusing an unset enum would fail every caller written against the obvious reading of a one-value enum. A second format changes that.\n\nA zero-row export is a real answer and still returns a file with its header row: a completely empty file reads as a broken download."
          }
        },
        {
          "name": "Download the audit export (web)",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{web_base_url}}/o/{{org_slug}}/logs/export?since=2026-08-01&until=2026-08-31",
              "host": ["{{web_base_url}}"],
              "path": ["o", "{{org_slug}}", "logs", "export"],
              "query": [
                { "key": "since", "value": "2026-08-01" },
                { "key": "until", "value": "2026-08-31" }
              ]
            },
            "description": "`web`'s download endpoint, in front of ExportAuditEntries. **Implemented as of ENT-223.**\n\nIn the collection because it is one of the halves that will never appear in a proto file: a browser-facing route with its own status codes and its own headers, which is exactly what this collection is the source of truth for.\n\n**Authenticated by the session cookie, not a bearer token**, so this request only works from a browser that has signed in. There is nothing to paste into Postman here; it is documented so the contract is reviewable.\n\n**A route handler rather than a server action**, because the result is a file and a download has to survive being a plain link. A server action returning bytes needs client-side JavaScript to turn them into a saved file, and the artefact an auditor's whole workflow ends in should not depend on a hydration step.\n\nIt reads the SAME query string the page does, through the same `toFilter`, which is what makes \"export what I am looking at\" true by construction rather than because two implementations agree. `page` is deliberately dropped: an export is the whole matching set, and carrying a cursor into it would be asking for \"everything, starting from page three\".\n\n**The filename carries the truncation.** When core-api reports the cap was hit, the file is named `-partial`. That is the only signal that survives the trip: the file gets attached to a report and emailed onwards long after any banner in the console is gone, and a name is the one piece of context that travels with it. `X-Kindlast-Truncated` and `X-Kindlast-Row-Count` carry the same facts for a script.\n\nStatus codes worth knowing: **401** with no session, and deliberately not a redirect, because redirecting a file endpoint into a sign-in page saves an HTML document called `audit.csv`. **404** for a slug the caller does not belong to, matching the rest of the console, so this cannot be used to discover which organisations exist. **400** when core-api refused the filter (a backwards range). **502** when core-api was unreachable or returned no file; a response with no content is a fault rather than an empty export, because an export of nothing still carries a header row and a zero-byte file looks like an answer.\n\n`Cache-Control: no-store, private`. This is a per-organisation record behind a session, and a shared proxy holding one tenant's audit log is the worst cache bug available in this product."
          }
        }
      ]
    }
  ]
}
