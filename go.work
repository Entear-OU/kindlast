// A Go module per service, joined here for local development
// (core-api-surface §21.4). One module would mean one dependency set, so an
// upgrade for core-api would silently upgrade workers, and nothing would stop
// a careless import from crossing a service boundary.
//
// apps/workers joined at ENT-231, when the integrations gateway gave it
// something to do. apps/intelligence is Python and never joins.
//
// This number is a floor for the whole workspace, so it has to be at least the
// highest `go` any joined module declares. Raise a module past it and go.work
// stops loading, which fails every command in every module at once rather than
// only in the module that moved.
go 1.25.4

use (
	./apps/core-api
	./apps/workers
	./gen/go
	./libs/chassis
)
