// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kindlast/core/v1/audit.proto

package corev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Entear-OU/kindlast/gen/go/kindlast/core/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// AuditServiceName is the fully-qualified name of the AuditService service.
	AuditServiceName = "kindlast.core.v1.AuditService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// AuditServiceListAuditEntriesProcedure is the fully-qualified name of the AuditService's
	// ListAuditEntries RPC.
	AuditServiceListAuditEntriesProcedure = "/kindlast.core.v1.AuditService/ListAuditEntries"
	// AuditServiceExportAuditEntriesProcedure is the fully-qualified name of the AuditService's
	// ExportAuditEntries RPC.
	AuditServiceExportAuditEntriesProcedure = "/kindlast.core.v1.AuditService/ExportAuditEntries"
)

// AuditServiceClient is a client for the kindlast.core.v1.AuditService service.
type AuditServiceClient interface {
	// The decisions, filtered and paged.
	//
	// Keyset over `(occurred_at desc, id desc)`, matching the
	// `audit_log_org_occurred_idx` index. Offset pagination would let a row
	// written between two page requests shift the window, and this is the one
	// register where a skipped row is a compliance problem rather than a
	// cosmetic one.
	ListAuditEntries(context.Context, *connect.Request[v1.ListAuditEntriesRequest]) (*connect.Response[v1.ListAuditEntriesResponse], error)
	// The same filtered set, as a file an auditor can attach to a report.
	//
	// # WHY THIS IS A SEPARATE RPC RATHER THAN A FLAG ON THE LIST
	//
	// Because the two answer different questions. The list answers "what is on
	// this page", and is paged by construction. An export answers "everything
	// that matched", and an export that silently stopped at a page boundary is
	// worse than no export: the auditor cannot see the truncation, and the file
	// is the artefact that leaves the building.
	//
	// # WHY THE RESPONSE IS A ONEOF
	//
	// §7 designs the export as a request that returns a signed download URL, and
	// §25 leaves synchronous-versus-job open. At today's volumes a synchronous
	// body is right and a job queue would be machinery for nobody. But the shape
	// that grows into a URL and the shape that does not are decided now, so the
	// response carries a `oneof`: today it is always `content`, and the day a
	// customer's log outgrows one response it becomes `download`, with no
	// contract break and no second RPC.
	ExportAuditEntries(context.Context, *connect.Request[v1.ExportAuditEntriesRequest]) (*connect.Response[v1.ExportAuditEntriesResponse], error)
}

// NewAuditServiceClient constructs a client for the kindlast.core.v1.AuditService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewAuditServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) AuditServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	auditServiceMethods := v1.File_kindlast_core_v1_audit_proto.Services().ByName("AuditService").Methods()
	return &auditServiceClient{
		listAuditEntries: connect.NewClient[v1.ListAuditEntriesRequest, v1.ListAuditEntriesResponse](
			httpClient,
			baseURL+AuditServiceListAuditEntriesProcedure,
			connect.WithSchema(auditServiceMethods.ByName("ListAuditEntries")),
			connect.WithClientOptions(opts...),
		),
		exportAuditEntries: connect.NewClient[v1.ExportAuditEntriesRequest, v1.ExportAuditEntriesResponse](
			httpClient,
			baseURL+AuditServiceExportAuditEntriesProcedure,
			connect.WithSchema(auditServiceMethods.ByName("ExportAuditEntries")),
			connect.WithClientOptions(opts...),
		),
	}
}

// auditServiceClient implements AuditServiceClient.
type auditServiceClient struct {
	listAuditEntries   *connect.Client[v1.ListAuditEntriesRequest, v1.ListAuditEntriesResponse]
	exportAuditEntries *connect.Client[v1.ExportAuditEntriesRequest, v1.ExportAuditEntriesResponse]
}

// ListAuditEntries calls kindlast.core.v1.AuditService.ListAuditEntries.
func (c *auditServiceClient) ListAuditEntries(ctx context.Context, req *connect.Request[v1.ListAuditEntriesRequest]) (*connect.Response[v1.ListAuditEntriesResponse], error) {
	return c.listAuditEntries.CallUnary(ctx, req)
}

// ExportAuditEntries calls kindlast.core.v1.AuditService.ExportAuditEntries.
func (c *auditServiceClient) ExportAuditEntries(ctx context.Context, req *connect.Request[v1.ExportAuditEntriesRequest]) (*connect.Response[v1.ExportAuditEntriesResponse], error) {
	return c.exportAuditEntries.CallUnary(ctx, req)
}

// AuditServiceHandler is an implementation of the kindlast.core.v1.AuditService service.
type AuditServiceHandler interface {
	// The decisions, filtered and paged.
	//
	// Keyset over `(occurred_at desc, id desc)`, matching the
	// `audit_log_org_occurred_idx` index. Offset pagination would let a row
	// written between two page requests shift the window, and this is the one
	// register where a skipped row is a compliance problem rather than a
	// cosmetic one.
	ListAuditEntries(context.Context, *connect.Request[v1.ListAuditEntriesRequest]) (*connect.Response[v1.ListAuditEntriesResponse], error)
	// The same filtered set, as a file an auditor can attach to a report.
	//
	// # WHY THIS IS A SEPARATE RPC RATHER THAN A FLAG ON THE LIST
	//
	// Because the two answer different questions. The list answers "what is on
	// this page", and is paged by construction. An export answers "everything
	// that matched", and an export that silently stopped at a page boundary is
	// worse than no export: the auditor cannot see the truncation, and the file
	// is the artefact that leaves the building.
	//
	// # WHY THE RESPONSE IS A ONEOF
	//
	// §7 designs the export as a request that returns a signed download URL, and
	// §25 leaves synchronous-versus-job open. At today's volumes a synchronous
	// body is right and a job queue would be machinery for nobody. But the shape
	// that grows into a URL and the shape that does not are decided now, so the
	// response carries a `oneof`: today it is always `content`, and the day a
	// customer's log outgrows one response it becomes `download`, with no
	// contract break and no second RPC.
	ExportAuditEntries(context.Context, *connect.Request[v1.ExportAuditEntriesRequest]) (*connect.Response[v1.ExportAuditEntriesResponse], error)
}

// NewAuditServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewAuditServiceHandler(svc AuditServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	auditServiceMethods := v1.File_kindlast_core_v1_audit_proto.Services().ByName("AuditService").Methods()
	auditServiceListAuditEntriesHandler := connect.NewUnaryHandler(
		AuditServiceListAuditEntriesProcedure,
		svc.ListAuditEntries,
		connect.WithSchema(auditServiceMethods.ByName("ListAuditEntries")),
		connect.WithHandlerOptions(opts...),
	)
	auditServiceExportAuditEntriesHandler := connect.NewUnaryHandler(
		AuditServiceExportAuditEntriesProcedure,
		svc.ExportAuditEntries,
		connect.WithSchema(auditServiceMethods.ByName("ExportAuditEntries")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kindlast.core.v1.AuditService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case AuditServiceListAuditEntriesProcedure:
			auditServiceListAuditEntriesHandler.ServeHTTP(w, r)
		case AuditServiceExportAuditEntriesProcedure:
			auditServiceExportAuditEntriesHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedAuditServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedAuditServiceHandler struct{}

func (UnimplementedAuditServiceHandler) ListAuditEntries(context.Context, *connect.Request[v1.ListAuditEntriesRequest]) (*connect.Response[v1.ListAuditEntriesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.AuditService.ListAuditEntries is not implemented"))
}

func (UnimplementedAuditServiceHandler) ExportAuditEntries(context.Context, *connect.Request[v1.ExportAuditEntriesRequest]) (*connect.Response[v1.ExportAuditEntriesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.AuditService.ExportAuditEntries is not implemented"))
}
