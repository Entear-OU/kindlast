// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kindlast/core/v1/memory.proto

package corev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Entear-OU/kindlast/gen/go/kindlast/core/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// MemoryServiceName is the fully-qualified name of the MemoryService service.
	MemoryServiceName = "kindlast.core.v1.MemoryService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// MemoryServiceListProfileFactsProcedure is the fully-qualified name of the MemoryService's
	// ListProfileFacts RPC.
	MemoryServiceListProfileFactsProcedure = "/kindlast.core.v1.MemoryService/ListProfileFacts"
	// MemoryServiceGetFactHistoryProcedure is the fully-qualified name of the MemoryService's
	// GetFactHistory RPC.
	MemoryServiceGetFactHistoryProcedure = "/kindlast.core.v1.MemoryService/GetFactHistory"
	// MemoryServiceCorrectFactProcedure is the fully-qualified name of the MemoryService's CorrectFact
	// RPC.
	MemoryServiceCorrectFactProcedure = "/kindlast.core.v1.MemoryService/CorrectFact"
	// MemoryServiceListEvidenceProcedure is the fully-qualified name of the MemoryService's
	// ListEvidence RPC.
	MemoryServiceListEvidenceProcedure = "/kindlast.core.v1.MemoryService/ListEvidence"
)

// MemoryServiceClient is a client for the kindlast.core.v1.MemoryService service.
type MemoryServiceClient interface {
	// What we currently believe, one row per fact.
	//
	// Not paged. The profile is a few dozen facts by construction, and a person
	// asking what a product knows about them wants the answer rather than a
	// window onto it.
	ListProfileFacts(context.Context, *connect.Request[v1.ListProfileFactsRequest]) (*connect.Response[v1.ListProfileFactsResponse], error)
	// Every value one fact has ever had, newest first.
	//
	// THE READ THAT MAKES CORRECTION MEANINGFUL. Without it, correcting a fact
	// is indistinguishable from us having always thought the new thing, and the
	// question somebody checking an old finding is asking is precisely what we
	// believed at the time.
	GetFactHistory(context.Context, *connect.Request[v1.GetFactHistoryRequest]) (*connect.Response[v1.GetFactHistoryResponse], error)
	// Correct a fact, as a typed patch.
	//
	// Closes the current value and records a new one, in one transaction. It is
	// never an update in place: `kindlast_app` holds `update (valid_to)` and not
	// `update (value)`, so an in-place rewrite is refused by the database rather
	// than avoided by convention.
	//
	// Idempotent in the way that matters: correcting a fact to the value it
	// already holds is a no-op rather than a second identical row, because a
	// history full of "changed from yes to yes" is a history nobody reads.
	CorrectFact(context.Context, *connect.Request[v1.CorrectFactRequest]) (*connect.Response[v1.CorrectFactResponse], error)
	// What we observed, and where each observation came from.
	//
	// Paged, because unlike the profile this grows without bound: every sweep of
	// every connected tool adds rows.
	ListEvidence(context.Context, *connect.Request[v1.ListEvidenceRequest]) (*connect.Response[v1.ListEvidenceResponse], error)
}

// NewMemoryServiceClient constructs a client for the kindlast.core.v1.MemoryService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewMemoryServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) MemoryServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	memoryServiceMethods := v1.File_kindlast_core_v1_memory_proto.Services().ByName("MemoryService").Methods()
	return &memoryServiceClient{
		listProfileFacts: connect.NewClient[v1.ListProfileFactsRequest, v1.ListProfileFactsResponse](
			httpClient,
			baseURL+MemoryServiceListProfileFactsProcedure,
			connect.WithSchema(memoryServiceMethods.ByName("ListProfileFacts")),
			connect.WithClientOptions(opts...),
		),
		getFactHistory: connect.NewClient[v1.GetFactHistoryRequest, v1.GetFactHistoryResponse](
			httpClient,
			baseURL+MemoryServiceGetFactHistoryProcedure,
			connect.WithSchema(memoryServiceMethods.ByName("GetFactHistory")),
			connect.WithClientOptions(opts...),
		),
		correctFact: connect.NewClient[v1.CorrectFactRequest, v1.CorrectFactResponse](
			httpClient,
			baseURL+MemoryServiceCorrectFactProcedure,
			connect.WithSchema(memoryServiceMethods.ByName("CorrectFact")),
			connect.WithClientOptions(opts...),
		),
		listEvidence: connect.NewClient[v1.ListEvidenceRequest, v1.ListEvidenceResponse](
			httpClient,
			baseURL+MemoryServiceListEvidenceProcedure,
			connect.WithSchema(memoryServiceMethods.ByName("ListEvidence")),
			connect.WithClientOptions(opts...),
		),
	}
}

// memoryServiceClient implements MemoryServiceClient.
type memoryServiceClient struct {
	listProfileFacts *connect.Client[v1.ListProfileFactsRequest, v1.ListProfileFactsResponse]
	getFactHistory   *connect.Client[v1.GetFactHistoryRequest, v1.GetFactHistoryResponse]
	correctFact      *connect.Client[v1.CorrectFactRequest, v1.CorrectFactResponse]
	listEvidence     *connect.Client[v1.ListEvidenceRequest, v1.ListEvidenceResponse]
}

// ListProfileFacts calls kindlast.core.v1.MemoryService.ListProfileFacts.
func (c *memoryServiceClient) ListProfileFacts(ctx context.Context, req *connect.Request[v1.ListProfileFactsRequest]) (*connect.Response[v1.ListProfileFactsResponse], error) {
	return c.listProfileFacts.CallUnary(ctx, req)
}

// GetFactHistory calls kindlast.core.v1.MemoryService.GetFactHistory.
func (c *memoryServiceClient) GetFactHistory(ctx context.Context, req *connect.Request[v1.GetFactHistoryRequest]) (*connect.Response[v1.GetFactHistoryResponse], error) {
	return c.getFactHistory.CallUnary(ctx, req)
}

// CorrectFact calls kindlast.core.v1.MemoryService.CorrectFact.
func (c *memoryServiceClient) CorrectFact(ctx context.Context, req *connect.Request[v1.CorrectFactRequest]) (*connect.Response[v1.CorrectFactResponse], error) {
	return c.correctFact.CallUnary(ctx, req)
}

// ListEvidence calls kindlast.core.v1.MemoryService.ListEvidence.
func (c *memoryServiceClient) ListEvidence(ctx context.Context, req *connect.Request[v1.ListEvidenceRequest]) (*connect.Response[v1.ListEvidenceResponse], error) {
	return c.listEvidence.CallUnary(ctx, req)
}

// MemoryServiceHandler is an implementation of the kindlast.core.v1.MemoryService service.
type MemoryServiceHandler interface {
	// What we currently believe, one row per fact.
	//
	// Not paged. The profile is a few dozen facts by construction, and a person
	// asking what a product knows about them wants the answer rather than a
	// window onto it.
	ListProfileFacts(context.Context, *connect.Request[v1.ListProfileFactsRequest]) (*connect.Response[v1.ListProfileFactsResponse], error)
	// Every value one fact has ever had, newest first.
	//
	// THE READ THAT MAKES CORRECTION MEANINGFUL. Without it, correcting a fact
	// is indistinguishable from us having always thought the new thing, and the
	// question somebody checking an old finding is asking is precisely what we
	// believed at the time.
	GetFactHistory(context.Context, *connect.Request[v1.GetFactHistoryRequest]) (*connect.Response[v1.GetFactHistoryResponse], error)
	// Correct a fact, as a typed patch.
	//
	// Closes the current value and records a new one, in one transaction. It is
	// never an update in place: `kindlast_app` holds `update (valid_to)` and not
	// `update (value)`, so an in-place rewrite is refused by the database rather
	// than avoided by convention.
	//
	// Idempotent in the way that matters: correcting a fact to the value it
	// already holds is a no-op rather than a second identical row, because a
	// history full of "changed from yes to yes" is a history nobody reads.
	CorrectFact(context.Context, *connect.Request[v1.CorrectFactRequest]) (*connect.Response[v1.CorrectFactResponse], error)
	// What we observed, and where each observation came from.
	//
	// Paged, because unlike the profile this grows without bound: every sweep of
	// every connected tool adds rows.
	ListEvidence(context.Context, *connect.Request[v1.ListEvidenceRequest]) (*connect.Response[v1.ListEvidenceResponse], error)
}

// NewMemoryServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewMemoryServiceHandler(svc MemoryServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	memoryServiceMethods := v1.File_kindlast_core_v1_memory_proto.Services().ByName("MemoryService").Methods()
	memoryServiceListProfileFactsHandler := connect.NewUnaryHandler(
		MemoryServiceListProfileFactsProcedure,
		svc.ListProfileFacts,
		connect.WithSchema(memoryServiceMethods.ByName("ListProfileFacts")),
		connect.WithHandlerOptions(opts...),
	)
	memoryServiceGetFactHistoryHandler := connect.NewUnaryHandler(
		MemoryServiceGetFactHistoryProcedure,
		svc.GetFactHistory,
		connect.WithSchema(memoryServiceMethods.ByName("GetFactHistory")),
		connect.WithHandlerOptions(opts...),
	)
	memoryServiceCorrectFactHandler := connect.NewUnaryHandler(
		MemoryServiceCorrectFactProcedure,
		svc.CorrectFact,
		connect.WithSchema(memoryServiceMethods.ByName("CorrectFact")),
		connect.WithHandlerOptions(opts...),
	)
	memoryServiceListEvidenceHandler := connect.NewUnaryHandler(
		MemoryServiceListEvidenceProcedure,
		svc.ListEvidence,
		connect.WithSchema(memoryServiceMethods.ByName("ListEvidence")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kindlast.core.v1.MemoryService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case MemoryServiceListProfileFactsProcedure:
			memoryServiceListProfileFactsHandler.ServeHTTP(w, r)
		case MemoryServiceGetFactHistoryProcedure:
			memoryServiceGetFactHistoryHandler.ServeHTTP(w, r)
		case MemoryServiceCorrectFactProcedure:
			memoryServiceCorrectFactHandler.ServeHTTP(w, r)
		case MemoryServiceListEvidenceProcedure:
			memoryServiceListEvidenceHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedMemoryServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedMemoryServiceHandler struct{}

func (UnimplementedMemoryServiceHandler) ListProfileFacts(context.Context, *connect.Request[v1.ListProfileFactsRequest]) (*connect.Response[v1.ListProfileFactsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.MemoryService.ListProfileFacts is not implemented"))
}

func (UnimplementedMemoryServiceHandler) GetFactHistory(context.Context, *connect.Request[v1.GetFactHistoryRequest]) (*connect.Response[v1.GetFactHistoryResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.MemoryService.GetFactHistory is not implemented"))
}

func (UnimplementedMemoryServiceHandler) CorrectFact(context.Context, *connect.Request[v1.CorrectFactRequest]) (*connect.Response[v1.CorrectFactResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.MemoryService.CorrectFact is not implemented"))
}

func (UnimplementedMemoryServiceHandler) ListEvidence(context.Context, *connect.Request[v1.ListEvidenceRequest]) (*connect.Response[v1.ListEvidenceResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.MemoryService.ListEvidence is not implemented"))
}
