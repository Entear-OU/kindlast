// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kindlast/core/v1/notifications.proto

package corev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Entear-OU/kindlast/gen/go/kindlast/core/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// NotificationServiceName is the fully-qualified name of the NotificationService service.
	NotificationServiceName = "kindlast.core.v1.NotificationService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// NotificationServiceGetNotificationPreferencesProcedure is the fully-qualified name of the
	// NotificationService's GetNotificationPreferences RPC.
	NotificationServiceGetNotificationPreferencesProcedure = "/kindlast.core.v1.NotificationService/GetNotificationPreferences"
	// NotificationServiceUpdateNotificationPreferencesProcedure is the fully-qualified name of the
	// NotificationService's UpdateNotificationPreferences RPC.
	NotificationServiceUpdateNotificationPreferencesProcedure = "/kindlast.core.v1.NotificationService/UpdateNotificationPreferences"
	// NotificationServiceGetNotificationCapabilitiesProcedure is the fully-qualified name of the
	// NotificationService's GetNotificationCapabilities RPC.
	NotificationServiceGetNotificationCapabilitiesProcedure = "/kindlast.core.v1.NotificationService/GetNotificationCapabilities"
)

// NotificationServiceClient is a client for the kindlast.core.v1.NotificationService service.
type NotificationServiceClient interface {
	// The caller's own preferences for the active organisation.
	//
	// Returns defaults rather than an error when no row exists, because somebody
	// who has never opened the settings page has no row and is nonetheless
	// subscribed: the product default is that a member is told. An error here
	// would make "I have not changed anything" indistinguishable from a fault.
	GetNotificationPreferences(context.Context, *connect.Request[v1.GetNotificationPreferencesRequest]) (*connect.Response[v1.GetNotificationPreferencesResponse], error)
	// Replace the caller's preferences for the active organisation.
	//
	// A whole-object write rather than a patch. The settings surface renders every
	// field at once, so a partial update would mean deciding what an absent field
	// means, and "unset" and "set to false" are different intentions that a proto3
	// scalar cannot tell apart.
	UpdateNotificationPreferences(context.Context, *connect.Request[v1.UpdateNotificationPreferencesRequest]) (*connect.Response[v1.UpdateNotificationPreferencesResponse], error)
	// Which notification channels this deployment can actually use (§18.3).
	//
	// Exists so a console never renders a channel it cannot deliver on. Telegram,
	// Slack and WhatsApp are in the design and are not built, and the failure
	// this prevents is a settings page offering a switch that silently does
	// nothing, which is indistinguishable from a broken integration and generates
	// support instead of value.
	//
	// Read from configuration rather than from a hard-coded array, because
	// "email" is only available on a deployment that has told core-api where to
	// submit mail. A self-hoster with no SMTP has no channels at all, and saying
	// so plainly is better than listing one that will queue forever.
	GetNotificationCapabilities(context.Context, *connect.Request[v1.GetNotificationCapabilitiesRequest]) (*connect.Response[v1.GetNotificationCapabilitiesResponse], error)
}

// NewNotificationServiceClient constructs a client for the kindlast.core.v1.NotificationService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewNotificationServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) NotificationServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	notificationServiceMethods := v1.File_kindlast_core_v1_notifications_proto.Services().ByName("NotificationService").Methods()
	return &notificationServiceClient{
		getNotificationPreferences: connect.NewClient[v1.GetNotificationPreferencesRequest, v1.GetNotificationPreferencesResponse](
			httpClient,
			baseURL+NotificationServiceGetNotificationPreferencesProcedure,
			connect.WithSchema(notificationServiceMethods.ByName("GetNotificationPreferences")),
			connect.WithClientOptions(opts...),
		),
		updateNotificationPreferences: connect.NewClient[v1.UpdateNotificationPreferencesRequest, v1.UpdateNotificationPreferencesResponse](
			httpClient,
			baseURL+NotificationServiceUpdateNotificationPreferencesProcedure,
			connect.WithSchema(notificationServiceMethods.ByName("UpdateNotificationPreferences")),
			connect.WithClientOptions(opts...),
		),
		getNotificationCapabilities: connect.NewClient[v1.GetNotificationCapabilitiesRequest, v1.GetNotificationCapabilitiesResponse](
			httpClient,
			baseURL+NotificationServiceGetNotificationCapabilitiesProcedure,
			connect.WithSchema(notificationServiceMethods.ByName("GetNotificationCapabilities")),
			connect.WithClientOptions(opts...),
		),
	}
}

// notificationServiceClient implements NotificationServiceClient.
type notificationServiceClient struct {
	getNotificationPreferences    *connect.Client[v1.GetNotificationPreferencesRequest, v1.GetNotificationPreferencesResponse]
	updateNotificationPreferences *connect.Client[v1.UpdateNotificationPreferencesRequest, v1.UpdateNotificationPreferencesResponse]
	getNotificationCapabilities   *connect.Client[v1.GetNotificationCapabilitiesRequest, v1.GetNotificationCapabilitiesResponse]
}

// GetNotificationPreferences calls kindlast.core.v1.NotificationService.GetNotificationPreferences.
func (c *notificationServiceClient) GetNotificationPreferences(ctx context.Context, req *connect.Request[v1.GetNotificationPreferencesRequest]) (*connect.Response[v1.GetNotificationPreferencesResponse], error) {
	return c.getNotificationPreferences.CallUnary(ctx, req)
}

// UpdateNotificationPreferences calls
// kindlast.core.v1.NotificationService.UpdateNotificationPreferences.
func (c *notificationServiceClient) UpdateNotificationPreferences(ctx context.Context, req *connect.Request[v1.UpdateNotificationPreferencesRequest]) (*connect.Response[v1.UpdateNotificationPreferencesResponse], error) {
	return c.updateNotificationPreferences.CallUnary(ctx, req)
}

// GetNotificationCapabilities calls
// kindlast.core.v1.NotificationService.GetNotificationCapabilities.
func (c *notificationServiceClient) GetNotificationCapabilities(ctx context.Context, req *connect.Request[v1.GetNotificationCapabilitiesRequest]) (*connect.Response[v1.GetNotificationCapabilitiesResponse], error) {
	return c.getNotificationCapabilities.CallUnary(ctx, req)
}

// NotificationServiceHandler is an implementation of the kindlast.core.v1.NotificationService
// service.
type NotificationServiceHandler interface {
	// The caller's own preferences for the active organisation.
	//
	// Returns defaults rather than an error when no row exists, because somebody
	// who has never opened the settings page has no row and is nonetheless
	// subscribed: the product default is that a member is told. An error here
	// would make "I have not changed anything" indistinguishable from a fault.
	GetNotificationPreferences(context.Context, *connect.Request[v1.GetNotificationPreferencesRequest]) (*connect.Response[v1.GetNotificationPreferencesResponse], error)
	// Replace the caller's preferences for the active organisation.
	//
	// A whole-object write rather than a patch. The settings surface renders every
	// field at once, so a partial update would mean deciding what an absent field
	// means, and "unset" and "set to false" are different intentions that a proto3
	// scalar cannot tell apart.
	UpdateNotificationPreferences(context.Context, *connect.Request[v1.UpdateNotificationPreferencesRequest]) (*connect.Response[v1.UpdateNotificationPreferencesResponse], error)
	// Which notification channels this deployment can actually use (§18.3).
	//
	// Exists so a console never renders a channel it cannot deliver on. Telegram,
	// Slack and WhatsApp are in the design and are not built, and the failure
	// this prevents is a settings page offering a switch that silently does
	// nothing, which is indistinguishable from a broken integration and generates
	// support instead of value.
	//
	// Read from configuration rather than from a hard-coded array, because
	// "email" is only available on a deployment that has told core-api where to
	// submit mail. A self-hoster with no SMTP has no channels at all, and saying
	// so plainly is better than listing one that will queue forever.
	GetNotificationCapabilities(context.Context, *connect.Request[v1.GetNotificationCapabilitiesRequest]) (*connect.Response[v1.GetNotificationCapabilitiesResponse], error)
}

// NewNotificationServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewNotificationServiceHandler(svc NotificationServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	notificationServiceMethods := v1.File_kindlast_core_v1_notifications_proto.Services().ByName("NotificationService").Methods()
	notificationServiceGetNotificationPreferencesHandler := connect.NewUnaryHandler(
		NotificationServiceGetNotificationPreferencesProcedure,
		svc.GetNotificationPreferences,
		connect.WithSchema(notificationServiceMethods.ByName("GetNotificationPreferences")),
		connect.WithHandlerOptions(opts...),
	)
	notificationServiceUpdateNotificationPreferencesHandler := connect.NewUnaryHandler(
		NotificationServiceUpdateNotificationPreferencesProcedure,
		svc.UpdateNotificationPreferences,
		connect.WithSchema(notificationServiceMethods.ByName("UpdateNotificationPreferences")),
		connect.WithHandlerOptions(opts...),
	)
	notificationServiceGetNotificationCapabilitiesHandler := connect.NewUnaryHandler(
		NotificationServiceGetNotificationCapabilitiesProcedure,
		svc.GetNotificationCapabilities,
		connect.WithSchema(notificationServiceMethods.ByName("GetNotificationCapabilities")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kindlast.core.v1.NotificationService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case NotificationServiceGetNotificationPreferencesProcedure:
			notificationServiceGetNotificationPreferencesHandler.ServeHTTP(w, r)
		case NotificationServiceUpdateNotificationPreferencesProcedure:
			notificationServiceUpdateNotificationPreferencesHandler.ServeHTTP(w, r)
		case NotificationServiceGetNotificationCapabilitiesProcedure:
			notificationServiceGetNotificationCapabilitiesHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedNotificationServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedNotificationServiceHandler struct{}

func (UnimplementedNotificationServiceHandler) GetNotificationPreferences(context.Context, *connect.Request[v1.GetNotificationPreferencesRequest]) (*connect.Response[v1.GetNotificationPreferencesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.NotificationService.GetNotificationPreferences is not implemented"))
}

func (UnimplementedNotificationServiceHandler) UpdateNotificationPreferences(context.Context, *connect.Request[v1.UpdateNotificationPreferencesRequest]) (*connect.Response[v1.UpdateNotificationPreferencesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.NotificationService.UpdateNotificationPreferences is not implemented"))
}

func (UnimplementedNotificationServiceHandler) GetNotificationCapabilities(context.Context, *connect.Request[v1.GetNotificationCapabilitiesRequest]) (*connect.Response[v1.GetNotificationCapabilitiesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.NotificationService.GetNotificationCapabilities is not implemented"))
}
