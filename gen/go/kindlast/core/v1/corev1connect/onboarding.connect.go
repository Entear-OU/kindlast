// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kindlast/core/v1/onboarding.proto

package corev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Entear-OU/kindlast/gen/go/kindlast/core/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// OnboardingServiceName is the fully-qualified name of the OnboardingService service.
	OnboardingServiceName = "kindlast.core.v1.OnboardingService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// OnboardingServiceGetOnboardingSessionProcedure is the fully-qualified name of the
	// OnboardingService's GetOnboardingSession RPC.
	OnboardingServiceGetOnboardingSessionProcedure = "/kindlast.core.v1.OnboardingService/GetOnboardingSession"
	// OnboardingServiceStartOnboardingProcedure is the fully-qualified name of the OnboardingService's
	// StartOnboarding RPC.
	OnboardingServiceStartOnboardingProcedure = "/kindlast.core.v1.OnboardingService/StartOnboarding"
	// OnboardingServiceAnswerQuestionProcedure is the fully-qualified name of the OnboardingService's
	// AnswerQuestion RPC.
	OnboardingServiceAnswerQuestionProcedure = "/kindlast.core.v1.OnboardingService/AnswerQuestion"
	// OnboardingServiceConfirmProfileProcedure is the fully-qualified name of the OnboardingService's
	// ConfirmProfile RPC.
	OnboardingServiceConfirmProfileProcedure = "/kindlast.core.v1.OnboardingService/ConfirmProfile"
)

// OnboardingServiceClient is a client for the kindlast.core.v1.OnboardingService service.
type OnboardingServiceClient interface {
	// Where this organisation has got to, without starting anything.
	//
	// A read that creates nothing, deliberately. Every authenticated route that
	// wants to know whether to route a person into onboarding asks this, and a
	// read that quietly opened a session would mean merely LOOKING at the console
	// started an interview.
	GetOnboardingSession(context.Context, *connect.Request[v1.GetOnboardingSessionRequest]) (*connect.Response[v1.GetOnboardingSessionResponse], error)
	// Open the interview, or resume the one already open.
	//
	// Idempotent by construction: an organisation with an unfinished session gets
	// that session back rather than a second one, so a refresh, a second tab and
	// a retry after a dropped connection all land in the same conversation.
	StartOnboarding(context.Context, *connect.Request[v1.StartOnboardingRequest]) (*connect.Response[v1.StartOnboardingResponse], error)
	// Answer the question that was asked, or decline it.
	//
	// The key travels in the request rather than being inferred from position,
	// because a person may go back and change an earlier answer and because a
	// retry must not be able to attach an answer to a different question than the
	// one it was typed under.
	AnswerQuestion(context.Context, *connect.Request[v1.AnswerQuestionRequest]) (*connect.Response[v1.AnswerQuestionResponse], error)
	// Record what was said as what the organisation believes.
	//
	// This is the only call in this service that writes a fact. Until it is made,
	// the interview is a conversation and nothing else, which is how "the person
	// sees and confirms the profile before it drives anything" is enforced rather
	// than promised.
	//
	// Idempotent: confirming twice records the same values, and a value equal to
	// what is already believed writes no history row.
	ConfirmProfile(context.Context, *connect.Request[v1.ConfirmProfileRequest]) (*connect.Response[v1.ConfirmProfileResponse], error)
}

// NewOnboardingServiceClient constructs a client for the kindlast.core.v1.OnboardingService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewOnboardingServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) OnboardingServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	onboardingServiceMethods := v1.File_kindlast_core_v1_onboarding_proto.Services().ByName("OnboardingService").Methods()
	return &onboardingServiceClient{
		getOnboardingSession: connect.NewClient[v1.GetOnboardingSessionRequest, v1.GetOnboardingSessionResponse](
			httpClient,
			baseURL+OnboardingServiceGetOnboardingSessionProcedure,
			connect.WithSchema(onboardingServiceMethods.ByName("GetOnboardingSession")),
			connect.WithClientOptions(opts...),
		),
		startOnboarding: connect.NewClient[v1.StartOnboardingRequest, v1.StartOnboardingResponse](
			httpClient,
			baseURL+OnboardingServiceStartOnboardingProcedure,
			connect.WithSchema(onboardingServiceMethods.ByName("StartOnboarding")),
			connect.WithClientOptions(opts...),
		),
		answerQuestion: connect.NewClient[v1.AnswerQuestionRequest, v1.AnswerQuestionResponse](
			httpClient,
			baseURL+OnboardingServiceAnswerQuestionProcedure,
			connect.WithSchema(onboardingServiceMethods.ByName("AnswerQuestion")),
			connect.WithClientOptions(opts...),
		),
		confirmProfile: connect.NewClient[v1.ConfirmProfileRequest, v1.ConfirmProfileResponse](
			httpClient,
			baseURL+OnboardingServiceConfirmProfileProcedure,
			connect.WithSchema(onboardingServiceMethods.ByName("ConfirmProfile")),
			connect.WithClientOptions(opts...),
		),
	}
}

// onboardingServiceClient implements OnboardingServiceClient.
type onboardingServiceClient struct {
	getOnboardingSession *connect.Client[v1.GetOnboardingSessionRequest, v1.GetOnboardingSessionResponse]
	startOnboarding      *connect.Client[v1.StartOnboardingRequest, v1.StartOnboardingResponse]
	answerQuestion       *connect.Client[v1.AnswerQuestionRequest, v1.AnswerQuestionResponse]
	confirmProfile       *connect.Client[v1.ConfirmProfileRequest, v1.ConfirmProfileResponse]
}

// GetOnboardingSession calls kindlast.core.v1.OnboardingService.GetOnboardingSession.
func (c *onboardingServiceClient) GetOnboardingSession(ctx context.Context, req *connect.Request[v1.GetOnboardingSessionRequest]) (*connect.Response[v1.GetOnboardingSessionResponse], error) {
	return c.getOnboardingSession.CallUnary(ctx, req)
}

// StartOnboarding calls kindlast.core.v1.OnboardingService.StartOnboarding.
func (c *onboardingServiceClient) StartOnboarding(ctx context.Context, req *connect.Request[v1.StartOnboardingRequest]) (*connect.Response[v1.StartOnboardingResponse], error) {
	return c.startOnboarding.CallUnary(ctx, req)
}

// AnswerQuestion calls kindlast.core.v1.OnboardingService.AnswerQuestion.
func (c *onboardingServiceClient) AnswerQuestion(ctx context.Context, req *connect.Request[v1.AnswerQuestionRequest]) (*connect.Response[v1.AnswerQuestionResponse], error) {
	return c.answerQuestion.CallUnary(ctx, req)
}

// ConfirmProfile calls kindlast.core.v1.OnboardingService.ConfirmProfile.
func (c *onboardingServiceClient) ConfirmProfile(ctx context.Context, req *connect.Request[v1.ConfirmProfileRequest]) (*connect.Response[v1.ConfirmProfileResponse], error) {
	return c.confirmProfile.CallUnary(ctx, req)
}

// OnboardingServiceHandler is an implementation of the kindlast.core.v1.OnboardingService service.
type OnboardingServiceHandler interface {
	// Where this organisation has got to, without starting anything.
	//
	// A read that creates nothing, deliberately. Every authenticated route that
	// wants to know whether to route a person into onboarding asks this, and a
	// read that quietly opened a session would mean merely LOOKING at the console
	// started an interview.
	GetOnboardingSession(context.Context, *connect.Request[v1.GetOnboardingSessionRequest]) (*connect.Response[v1.GetOnboardingSessionResponse], error)
	// Open the interview, or resume the one already open.
	//
	// Idempotent by construction: an organisation with an unfinished session gets
	// that session back rather than a second one, so a refresh, a second tab and
	// a retry after a dropped connection all land in the same conversation.
	StartOnboarding(context.Context, *connect.Request[v1.StartOnboardingRequest]) (*connect.Response[v1.StartOnboardingResponse], error)
	// Answer the question that was asked, or decline it.
	//
	// The key travels in the request rather than being inferred from position,
	// because a person may go back and change an earlier answer and because a
	// retry must not be able to attach an answer to a different question than the
	// one it was typed under.
	AnswerQuestion(context.Context, *connect.Request[v1.AnswerQuestionRequest]) (*connect.Response[v1.AnswerQuestionResponse], error)
	// Record what was said as what the organisation believes.
	//
	// This is the only call in this service that writes a fact. Until it is made,
	// the interview is a conversation and nothing else, which is how "the person
	// sees and confirms the profile before it drives anything" is enforced rather
	// than promised.
	//
	// Idempotent: confirming twice records the same values, and a value equal to
	// what is already believed writes no history row.
	ConfirmProfile(context.Context, *connect.Request[v1.ConfirmProfileRequest]) (*connect.Response[v1.ConfirmProfileResponse], error)
}

// NewOnboardingServiceHandler builds an HTTP handler from the service implementation. It returns
// the path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewOnboardingServiceHandler(svc OnboardingServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	onboardingServiceMethods := v1.File_kindlast_core_v1_onboarding_proto.Services().ByName("OnboardingService").Methods()
	onboardingServiceGetOnboardingSessionHandler := connect.NewUnaryHandler(
		OnboardingServiceGetOnboardingSessionProcedure,
		svc.GetOnboardingSession,
		connect.WithSchema(onboardingServiceMethods.ByName("GetOnboardingSession")),
		connect.WithHandlerOptions(opts...),
	)
	onboardingServiceStartOnboardingHandler := connect.NewUnaryHandler(
		OnboardingServiceStartOnboardingProcedure,
		svc.StartOnboarding,
		connect.WithSchema(onboardingServiceMethods.ByName("StartOnboarding")),
		connect.WithHandlerOptions(opts...),
	)
	onboardingServiceAnswerQuestionHandler := connect.NewUnaryHandler(
		OnboardingServiceAnswerQuestionProcedure,
		svc.AnswerQuestion,
		connect.WithSchema(onboardingServiceMethods.ByName("AnswerQuestion")),
		connect.WithHandlerOptions(opts...),
	)
	onboardingServiceConfirmProfileHandler := connect.NewUnaryHandler(
		OnboardingServiceConfirmProfileProcedure,
		svc.ConfirmProfile,
		connect.WithSchema(onboardingServiceMethods.ByName("ConfirmProfile")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kindlast.core.v1.OnboardingService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case OnboardingServiceGetOnboardingSessionProcedure:
			onboardingServiceGetOnboardingSessionHandler.ServeHTTP(w, r)
		case OnboardingServiceStartOnboardingProcedure:
			onboardingServiceStartOnboardingHandler.ServeHTTP(w, r)
		case OnboardingServiceAnswerQuestionProcedure:
			onboardingServiceAnswerQuestionHandler.ServeHTTP(w, r)
		case OnboardingServiceConfirmProfileProcedure:
			onboardingServiceConfirmProfileHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedOnboardingServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedOnboardingServiceHandler struct{}

func (UnimplementedOnboardingServiceHandler) GetOnboardingSession(context.Context, *connect.Request[v1.GetOnboardingSessionRequest]) (*connect.Response[v1.GetOnboardingSessionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.OnboardingService.GetOnboardingSession is not implemented"))
}

func (UnimplementedOnboardingServiceHandler) StartOnboarding(context.Context, *connect.Request[v1.StartOnboardingRequest]) (*connect.Response[v1.StartOnboardingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.OnboardingService.StartOnboarding is not implemented"))
}

func (UnimplementedOnboardingServiceHandler) AnswerQuestion(context.Context, *connect.Request[v1.AnswerQuestionRequest]) (*connect.Response[v1.AnswerQuestionResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.OnboardingService.AnswerQuestion is not implemented"))
}

func (UnimplementedOnboardingServiceHandler) ConfirmProfile(context.Context, *connect.Request[v1.ConfirmProfileRequest]) (*connect.Response[v1.ConfirmProfileResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.OnboardingService.ConfirmProfile is not implemented"))
}
