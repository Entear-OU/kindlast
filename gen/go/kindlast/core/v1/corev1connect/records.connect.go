// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kindlast/core/v1/records.proto

package corev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Entear-OU/kindlast/gen/go/kindlast/core/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RecordsServiceName is the fully-qualified name of the RecordsService service.
	RecordsServiceName = "kindlast.core.v1.RecordsService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RecordsServiceListProcessingActivitiesProcedure is the fully-qualified name of the
	// RecordsService's ListProcessingActivities RPC.
	RecordsServiceListProcessingActivitiesProcedure = "/kindlast.core.v1.RecordsService/ListProcessingActivities"
	// RecordsServiceGetProcessingActivityProcedure is the fully-qualified name of the RecordsService's
	// GetProcessingActivity RPC.
	RecordsServiceGetProcessingActivityProcedure = "/kindlast.core.v1.RecordsService/GetProcessingActivity"
	// RecordsServiceListAiSystemsProcedure is the fully-qualified name of the RecordsService's
	// ListAiSystems RPC.
	RecordsServiceListAiSystemsProcedure = "/kindlast.core.v1.RecordsService/ListAiSystems"
	// RecordsServiceGetAiSystemProcedure is the fully-qualified name of the RecordsService's
	// GetAiSystem RPC.
	RecordsServiceGetAiSystemProcedure = "/kindlast.core.v1.RecordsService/GetAiSystem"
	// RecordsServiceListDsarsProcedure is the fully-qualified name of the RecordsService's ListDsars
	// RPC.
	RecordsServiceListDsarsProcedure = "/kindlast.core.v1.RecordsService/ListDsars"
	// RecordsServiceGetDsarProcedure is the fully-qualified name of the RecordsService's GetDsar RPC.
	RecordsServiceGetDsarProcedure = "/kindlast.core.v1.RecordsService/GetDsar"
	// RecordsServiceCreateProcessingActivityProcedure is the fully-qualified name of the
	// RecordsService's CreateProcessingActivity RPC.
	RecordsServiceCreateProcessingActivityProcedure = "/kindlast.core.v1.RecordsService/CreateProcessingActivity"
	// RecordsServiceUpdateProcessingActivityProcedure is the fully-qualified name of the
	// RecordsService's UpdateProcessingActivity RPC.
	RecordsServiceUpdateProcessingActivityProcedure = "/kindlast.core.v1.RecordsService/UpdateProcessingActivity"
	// RecordsServiceCreateAiSystemProcedure is the fully-qualified name of the RecordsService's
	// CreateAiSystem RPC.
	RecordsServiceCreateAiSystemProcedure = "/kindlast.core.v1.RecordsService/CreateAiSystem"
	// RecordsServiceUpdateAiSystemProcedure is the fully-qualified name of the RecordsService's
	// UpdateAiSystem RPC.
	RecordsServiceUpdateAiSystemProcedure = "/kindlast.core.v1.RecordsService/UpdateAiSystem"
	// RecordsServiceLogDsarProcedure is the fully-qualified name of the RecordsService's LogDsar RPC.
	RecordsServiceLogDsarProcedure = "/kindlast.core.v1.RecordsService/LogDsar"
	// RecordsServiceListDsarTrailProcedure is the fully-qualified name of the RecordsService's
	// ListDsarTrail RPC.
	RecordsServiceListDsarTrailProcedure = "/kindlast.core.v1.RecordsService/ListDsarTrail"
	// RecordsServiceAddDsarTrailEntryProcedure is the fully-qualified name of the RecordsService's
	// AddDsarTrailEntry RPC.
	RecordsServiceAddDsarTrailEntryProcedure = "/kindlast.core.v1.RecordsService/AddDsarTrailEntry"
	// RecordsServiceMarkDsarRespondedProcedure is the fully-qualified name of the RecordsService's
	// MarkDsarResponded RPC.
	RecordsServiceMarkDsarRespondedProcedure = "/kindlast.core.v1.RecordsService/MarkDsarResponded"
)

// RecordsServiceClient is a client for the kindlast.core.v1.RecordsService service.
type RecordsServiceClient interface {
	// The Article 30 record of processing activities.
	//
	// Ordered and paginated over the `(org_id, created_at desc)` index that 00001
	// created. Keyset rather than offset, for the reason ListFindings gives: the
	// list is written to while it is read, and an offset page 2 silently skips a
	// row whenever one arrives between requests.
	//
	// Note this orders by creation, where the legacy register ordered by
	// `updated_at desc`. That was the better ordering for a register someone is
	// actively editing and it is the wrong one to paginate: a row edited between
	// two page requests moves position, so a keyset over `updated_at` can both
	// skip rows and return them twice. Creation order is stable, and "what did I
	// touch last" is a sort a client can offer within a page.
	ListProcessingActivities(context.Context, *connect.Request[v1.ListProcessingActivitiesRequest]) (*connect.Response[v1.ListProcessingActivitiesResponse], error)
	GetProcessingActivity(context.Context, *connect.Request[v1.GetProcessingActivityRequest]) (*connect.Response[v1.GetProcessingActivityResponse], error)
	// The AI system register, which the AI Act calls for and the GDPR does not.
	//
	// Paginated the same way, but note that `ai_systems` is indexed on `(org_id)`
	// alone rather than `(org_id, created_at desc)` like the other two. The
	// migration accompanying this surface adds the composite, because a keyset
	// page over an unsupported ordering sorts the whole tenant's rows on every
	// request: correct, and quietly quadratic.
	ListAiSystems(context.Context, *connect.Request[v1.ListAiSystemsRequest]) (*connect.Response[v1.ListAiSystemsResponse], error)
	GetAiSystem(context.Context, *connect.Request[v1.GetAiSystemRequest]) (*connect.Response[v1.GetAiSystemResponse], error)
	// Data-subject access requests, and the statutory clock on each.
	//
	// Ordered by `response_due_at` ascending rather than by creation, because the
	// only question anyone asks of this list is which one runs out first. That is
	// a different ordering from the other two lists on purpose, and it means a
	// page token from this list is not interchangeable with theirs.
	//
	// Unlike the other two registers, nothing in this one arrives from an
	// approval. 00009 classified three obligations and deliberately mapped none of
	// them to `create_dsar`, and 00010 asserts that, so every row here was logged
	// by a human. `source_finding_id` is therefore always empty today; it is
	// carried rather than omitted because the executor path exists and the field
	// going from always-empty to sometimes-set must not be a contract change.
	ListDsars(context.Context, *connect.Request[v1.ListDsarsRequest]) (*connect.Response[v1.ListDsarsResponse], error)
	GetDsar(context.Context, *connect.Request[v1.GetDsarRequest]) (*connect.Response[v1.GetDsarResponse], error)
	// Adds an activity the Executor has not seen.
	//
	// Capped on the free plan, and the cap counts only manually-created rows: a
	// record created from an approved finding is part of the compliance record and
	// is never withheld behind a plan. Over the cap answers `resource_exhausted`
	// rather than `permission_denied`, because the caller is entitled to do this
	// and simply cannot right now, which is a distinction with a button under it.
	CreateProcessingActivity(context.Context, *connect.Request[v1.CreateProcessingActivityRequest]) (*connect.Response[v1.CreateProcessingActivityResponse], error)
	// Replaces an activity's fields.
	//
	// A full replacement rather than a patch, and the field mask that would make
	// it a patch is deliberately absent. Article 30 is a record of fact whose gaps
	// are meaningful: clearing a legal basis is a real edit somebody makes when
	// they realise the one recorded was wrong, and a partial update in which an
	// omitted field means "leave it alone" gives a client no way to express that.
	UpdateProcessingActivity(context.Context, *connect.Request[v1.UpdateProcessingActivityRequest]) (*connect.Response[v1.UpdateProcessingActivityResponse], error)
	// Registers a system nobody has approved a finding about.
	//
	// Useful mostly for shadow AI: the system somebody adopted without telling
	// anyone is exactly the one no finding will ever raise.
	//
	// Classifying a new system as `high` requires `reviewed`. See UpdateAiSystem.
	CreateAiSystem(context.Context, *connect.Request[v1.CreateAiSystemRequest]) (*connect.Response[v1.CreateAiSystemResponse], error)
	// Replaces a system's fields.
	//
	// # CHANGING A CLASSIFICATION REQUIRES `reviewed`, AND THAT IS THE POINT
	//
	// Any change to `risk_classification` is refused unless the request carries
	// `reviewed: true`, and so is classifying a new system as `high`. The database
	// enforces it, not this layer.
	//
	// The reason is that an Article 6 classification is the determination the
	// whole high-risk obligation stack hangs off. Moving a system out of `high` in
	// a form somebody was tabbing through would quietly retire Articles 9 to 17
	// for it, and the record would show a change with nobody's deliberate decision
	// behind it. A caller sends `reviewed` to say a human meant it, and the audit
	// row records that they did.
	//
	// Refused answers `failed_precondition`: the caller may do this, and must
	// confirm first. That is not `permission_denied` and not `resource_exhausted`,
	// and a client shows a confirm step rather than an upgrade prompt.
	UpdateAiSystem(context.Context, *connect.Request[v1.UpdateAiSystemRequest]) (*connect.Response[v1.UpdateAiSystemResponse], error)
	// Logs a request that arrived.
	//
	// The only way a DSAR enters the register: nothing creates one from an
	// approval, because a request comes from a person and an obligation that
	// manufactured one would be inventing the requester.
	LogDsar(context.Context, *connect.Request[v1.LogDsarRequest]) (*connect.Response[v1.LogDsarResponse], error)
	// The trail a response was assembled from: which store was searched, when,
	// and what came back (ENT-226).
	//
	// Chronological, oldest first, which is the opposite of every other list on
	// this service. The other registers answer "what is outstanding" and lead
	// with the most pressing row; a trail answers "what did you do", and that
	// reads forward. A page token from this list is not interchangeable with any
	// other here.
	//
	// Ordered by `occurred_at` and not by when somebody typed it in, for the
	// reason DsarTrailEntry.occurred_at gives.
	ListDsarTrail(context.Context, *connect.Request[v1.ListDsarTrailRequest]) (*connect.Response[v1.ListDsarTrailResponse], error)
	// Adds one entry to that trail.
	//
	// # APPEND ONLY, AND THERE IS NO UPDATE OR DELETE RPC TO GO WITH IT
	//
	// Not an unfinished surface. An entry is evidence about how a response to a
	// statutory request was built, and evidence a producer can revise after the
	// fact is worth less than evidence it cannot. The database refuses an UPDATE
	// with a trigger that binds even the migrator, so an RPC to do it could not
	// be served whatever this contract said.
	//
	// Correcting a mistake means adding an entry that says so, which is how a
	// paper file works. `withheld` and `none_found` exist so that the correction
	// has somewhere honest to go.
	//
	// No `reviewed` gate, unlike MarkDsarResponded. Recording that you searched
	// the CRM is not an assertion a regulator reads as compliance; it is the
	// working note that makes the assertion checkable, and a confirm step on
	// every note would mean fewer notes.
	AddDsarTrailEntry(context.Context, *connect.Request[v1.AddDsarTrailEntryRequest]) (*connect.Response[v1.AddDsarTrailEntryResponse], error)
	// Records that a response went out, which stops the statutory clock.
	//
	// Requires `reviewed`, for the same class of reason UpdateAiSystem does: this
	// is the assertion that the organisation met an Article 12(3) deadline, and it
	// is the single most consequential row in this register to get wrong. A
	// regulator reading the log later is reading this field.
	//
	// Idempotent. Marking an already-answered request returns `applied: false` and
	// writes nothing, rather than overwriting the date a response actually went
	// out with the date somebody clicked the button twice.
	//
	// `:respond` rather than a status field on an update, per AIP-136: it is one
	// transition with a gate, not an arbitrary field change.
	MarkDsarResponded(context.Context, *connect.Request[v1.MarkDsarRespondedRequest]) (*connect.Response[v1.MarkDsarRespondedResponse], error)
}

// NewRecordsServiceClient constructs a client for the kindlast.core.v1.RecordsService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRecordsServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RecordsServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	recordsServiceMethods := v1.File_kindlast_core_v1_records_proto.Services().ByName("RecordsService").Methods()
	return &recordsServiceClient{
		listProcessingActivities: connect.NewClient[v1.ListProcessingActivitiesRequest, v1.ListProcessingActivitiesResponse](
			httpClient,
			baseURL+RecordsServiceListProcessingActivitiesProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("ListProcessingActivities")),
			connect.WithClientOptions(opts...),
		),
		getProcessingActivity: connect.NewClient[v1.GetProcessingActivityRequest, v1.GetProcessingActivityResponse](
			httpClient,
			baseURL+RecordsServiceGetProcessingActivityProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("GetProcessingActivity")),
			connect.WithClientOptions(opts...),
		),
		listAiSystems: connect.NewClient[v1.ListAiSystemsRequest, v1.ListAiSystemsResponse](
			httpClient,
			baseURL+RecordsServiceListAiSystemsProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("ListAiSystems")),
			connect.WithClientOptions(opts...),
		),
		getAiSystem: connect.NewClient[v1.GetAiSystemRequest, v1.GetAiSystemResponse](
			httpClient,
			baseURL+RecordsServiceGetAiSystemProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("GetAiSystem")),
			connect.WithClientOptions(opts...),
		),
		listDsars: connect.NewClient[v1.ListDsarsRequest, v1.ListDsarsResponse](
			httpClient,
			baseURL+RecordsServiceListDsarsProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("ListDsars")),
			connect.WithClientOptions(opts...),
		),
		getDsar: connect.NewClient[v1.GetDsarRequest, v1.GetDsarResponse](
			httpClient,
			baseURL+RecordsServiceGetDsarProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("GetDsar")),
			connect.WithClientOptions(opts...),
		),
		createProcessingActivity: connect.NewClient[v1.CreateProcessingActivityRequest, v1.CreateProcessingActivityResponse](
			httpClient,
			baseURL+RecordsServiceCreateProcessingActivityProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("CreateProcessingActivity")),
			connect.WithClientOptions(opts...),
		),
		updateProcessingActivity: connect.NewClient[v1.UpdateProcessingActivityRequest, v1.UpdateProcessingActivityResponse](
			httpClient,
			baseURL+RecordsServiceUpdateProcessingActivityProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("UpdateProcessingActivity")),
			connect.WithClientOptions(opts...),
		),
		createAiSystem: connect.NewClient[v1.CreateAiSystemRequest, v1.CreateAiSystemResponse](
			httpClient,
			baseURL+RecordsServiceCreateAiSystemProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("CreateAiSystem")),
			connect.WithClientOptions(opts...),
		),
		updateAiSystem: connect.NewClient[v1.UpdateAiSystemRequest, v1.UpdateAiSystemResponse](
			httpClient,
			baseURL+RecordsServiceUpdateAiSystemProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("UpdateAiSystem")),
			connect.WithClientOptions(opts...),
		),
		logDsar: connect.NewClient[v1.LogDsarRequest, v1.LogDsarResponse](
			httpClient,
			baseURL+RecordsServiceLogDsarProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("LogDsar")),
			connect.WithClientOptions(opts...),
		),
		listDsarTrail: connect.NewClient[v1.ListDsarTrailRequest, v1.ListDsarTrailResponse](
			httpClient,
			baseURL+RecordsServiceListDsarTrailProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("ListDsarTrail")),
			connect.WithClientOptions(opts...),
		),
		addDsarTrailEntry: connect.NewClient[v1.AddDsarTrailEntryRequest, v1.AddDsarTrailEntryResponse](
			httpClient,
			baseURL+RecordsServiceAddDsarTrailEntryProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("AddDsarTrailEntry")),
			connect.WithClientOptions(opts...),
		),
		markDsarResponded: connect.NewClient[v1.MarkDsarRespondedRequest, v1.MarkDsarRespondedResponse](
			httpClient,
			baseURL+RecordsServiceMarkDsarRespondedProcedure,
			connect.WithSchema(recordsServiceMethods.ByName("MarkDsarResponded")),
			connect.WithClientOptions(opts...),
		),
	}
}

// recordsServiceClient implements RecordsServiceClient.
type recordsServiceClient struct {
	listProcessingActivities *connect.Client[v1.ListProcessingActivitiesRequest, v1.ListProcessingActivitiesResponse]
	getProcessingActivity    *connect.Client[v1.GetProcessingActivityRequest, v1.GetProcessingActivityResponse]
	listAiSystems            *connect.Client[v1.ListAiSystemsRequest, v1.ListAiSystemsResponse]
	getAiSystem              *connect.Client[v1.GetAiSystemRequest, v1.GetAiSystemResponse]
	listDsars                *connect.Client[v1.ListDsarsRequest, v1.ListDsarsResponse]
	getDsar                  *connect.Client[v1.GetDsarRequest, v1.GetDsarResponse]
	createProcessingActivity *connect.Client[v1.CreateProcessingActivityRequest, v1.CreateProcessingActivityResponse]
	updateProcessingActivity *connect.Client[v1.UpdateProcessingActivityRequest, v1.UpdateProcessingActivityResponse]
	createAiSystem           *connect.Client[v1.CreateAiSystemRequest, v1.CreateAiSystemResponse]
	updateAiSystem           *connect.Client[v1.UpdateAiSystemRequest, v1.UpdateAiSystemResponse]
	logDsar                  *connect.Client[v1.LogDsarRequest, v1.LogDsarResponse]
	listDsarTrail            *connect.Client[v1.ListDsarTrailRequest, v1.ListDsarTrailResponse]
	addDsarTrailEntry        *connect.Client[v1.AddDsarTrailEntryRequest, v1.AddDsarTrailEntryResponse]
	markDsarResponded        *connect.Client[v1.MarkDsarRespondedRequest, v1.MarkDsarRespondedResponse]
}

// ListProcessingActivities calls kindlast.core.v1.RecordsService.ListProcessingActivities.
func (c *recordsServiceClient) ListProcessingActivities(ctx context.Context, req *connect.Request[v1.ListProcessingActivitiesRequest]) (*connect.Response[v1.ListProcessingActivitiesResponse], error) {
	return c.listProcessingActivities.CallUnary(ctx, req)
}

// GetProcessingActivity calls kindlast.core.v1.RecordsService.GetProcessingActivity.
func (c *recordsServiceClient) GetProcessingActivity(ctx context.Context, req *connect.Request[v1.GetProcessingActivityRequest]) (*connect.Response[v1.GetProcessingActivityResponse], error) {
	return c.getProcessingActivity.CallUnary(ctx, req)
}

// ListAiSystems calls kindlast.core.v1.RecordsService.ListAiSystems.
func (c *recordsServiceClient) ListAiSystems(ctx context.Context, req *connect.Request[v1.ListAiSystemsRequest]) (*connect.Response[v1.ListAiSystemsResponse], error) {
	return c.listAiSystems.CallUnary(ctx, req)
}

// GetAiSystem calls kindlast.core.v1.RecordsService.GetAiSystem.
func (c *recordsServiceClient) GetAiSystem(ctx context.Context, req *connect.Request[v1.GetAiSystemRequest]) (*connect.Response[v1.GetAiSystemResponse], error) {
	return c.getAiSystem.CallUnary(ctx, req)
}

// ListDsars calls kindlast.core.v1.RecordsService.ListDsars.
func (c *recordsServiceClient) ListDsars(ctx context.Context, req *connect.Request[v1.ListDsarsRequest]) (*connect.Response[v1.ListDsarsResponse], error) {
	return c.listDsars.CallUnary(ctx, req)
}

// GetDsar calls kindlast.core.v1.RecordsService.GetDsar.
func (c *recordsServiceClient) GetDsar(ctx context.Context, req *connect.Request[v1.GetDsarRequest]) (*connect.Response[v1.GetDsarResponse], error) {
	return c.getDsar.CallUnary(ctx, req)
}

// CreateProcessingActivity calls kindlast.core.v1.RecordsService.CreateProcessingActivity.
func (c *recordsServiceClient) CreateProcessingActivity(ctx context.Context, req *connect.Request[v1.CreateProcessingActivityRequest]) (*connect.Response[v1.CreateProcessingActivityResponse], error) {
	return c.createProcessingActivity.CallUnary(ctx, req)
}

// UpdateProcessingActivity calls kindlast.core.v1.RecordsService.UpdateProcessingActivity.
func (c *recordsServiceClient) UpdateProcessingActivity(ctx context.Context, req *connect.Request[v1.UpdateProcessingActivityRequest]) (*connect.Response[v1.UpdateProcessingActivityResponse], error) {
	return c.updateProcessingActivity.CallUnary(ctx, req)
}

// CreateAiSystem calls kindlast.core.v1.RecordsService.CreateAiSystem.
func (c *recordsServiceClient) CreateAiSystem(ctx context.Context, req *connect.Request[v1.CreateAiSystemRequest]) (*connect.Response[v1.CreateAiSystemResponse], error) {
	return c.createAiSystem.CallUnary(ctx, req)
}

// UpdateAiSystem calls kindlast.core.v1.RecordsService.UpdateAiSystem.
func (c *recordsServiceClient) UpdateAiSystem(ctx context.Context, req *connect.Request[v1.UpdateAiSystemRequest]) (*connect.Response[v1.UpdateAiSystemResponse], error) {
	return c.updateAiSystem.CallUnary(ctx, req)
}

// LogDsar calls kindlast.core.v1.RecordsService.LogDsar.
func (c *recordsServiceClient) LogDsar(ctx context.Context, req *connect.Request[v1.LogDsarRequest]) (*connect.Response[v1.LogDsarResponse], error) {
	return c.logDsar.CallUnary(ctx, req)
}

// ListDsarTrail calls kindlast.core.v1.RecordsService.ListDsarTrail.
func (c *recordsServiceClient) ListDsarTrail(ctx context.Context, req *connect.Request[v1.ListDsarTrailRequest]) (*connect.Response[v1.ListDsarTrailResponse], error) {
	return c.listDsarTrail.CallUnary(ctx, req)
}

// AddDsarTrailEntry calls kindlast.core.v1.RecordsService.AddDsarTrailEntry.
func (c *recordsServiceClient) AddDsarTrailEntry(ctx context.Context, req *connect.Request[v1.AddDsarTrailEntryRequest]) (*connect.Response[v1.AddDsarTrailEntryResponse], error) {
	return c.addDsarTrailEntry.CallUnary(ctx, req)
}

// MarkDsarResponded calls kindlast.core.v1.RecordsService.MarkDsarResponded.
func (c *recordsServiceClient) MarkDsarResponded(ctx context.Context, req *connect.Request[v1.MarkDsarRespondedRequest]) (*connect.Response[v1.MarkDsarRespondedResponse], error) {
	return c.markDsarResponded.CallUnary(ctx, req)
}

// RecordsServiceHandler is an implementation of the kindlast.core.v1.RecordsService service.
type RecordsServiceHandler interface {
	// The Article 30 record of processing activities.
	//
	// Ordered and paginated over the `(org_id, created_at desc)` index that 00001
	// created. Keyset rather than offset, for the reason ListFindings gives: the
	// list is written to while it is read, and an offset page 2 silently skips a
	// row whenever one arrives between requests.
	//
	// Note this orders by creation, where the legacy register ordered by
	// `updated_at desc`. That was the better ordering for a register someone is
	// actively editing and it is the wrong one to paginate: a row edited between
	// two page requests moves position, so a keyset over `updated_at` can both
	// skip rows and return them twice. Creation order is stable, and "what did I
	// touch last" is a sort a client can offer within a page.
	ListProcessingActivities(context.Context, *connect.Request[v1.ListProcessingActivitiesRequest]) (*connect.Response[v1.ListProcessingActivitiesResponse], error)
	GetProcessingActivity(context.Context, *connect.Request[v1.GetProcessingActivityRequest]) (*connect.Response[v1.GetProcessingActivityResponse], error)
	// The AI system register, which the AI Act calls for and the GDPR does not.
	//
	// Paginated the same way, but note that `ai_systems` is indexed on `(org_id)`
	// alone rather than `(org_id, created_at desc)` like the other two. The
	// migration accompanying this surface adds the composite, because a keyset
	// page over an unsupported ordering sorts the whole tenant's rows on every
	// request: correct, and quietly quadratic.
	ListAiSystems(context.Context, *connect.Request[v1.ListAiSystemsRequest]) (*connect.Response[v1.ListAiSystemsResponse], error)
	GetAiSystem(context.Context, *connect.Request[v1.GetAiSystemRequest]) (*connect.Response[v1.GetAiSystemResponse], error)
	// Data-subject access requests, and the statutory clock on each.
	//
	// Ordered by `response_due_at` ascending rather than by creation, because the
	// only question anyone asks of this list is which one runs out first. That is
	// a different ordering from the other two lists on purpose, and it means a
	// page token from this list is not interchangeable with theirs.
	//
	// Unlike the other two registers, nothing in this one arrives from an
	// approval. 00009 classified three obligations and deliberately mapped none of
	// them to `create_dsar`, and 00010 asserts that, so every row here was logged
	// by a human. `source_finding_id` is therefore always empty today; it is
	// carried rather than omitted because the executor path exists and the field
	// going from always-empty to sometimes-set must not be a contract change.
	ListDsars(context.Context, *connect.Request[v1.ListDsarsRequest]) (*connect.Response[v1.ListDsarsResponse], error)
	GetDsar(context.Context, *connect.Request[v1.GetDsarRequest]) (*connect.Response[v1.GetDsarResponse], error)
	// Adds an activity the Executor has not seen.
	//
	// Capped on the free plan, and the cap counts only manually-created rows: a
	// record created from an approved finding is part of the compliance record and
	// is never withheld behind a plan. Over the cap answers `resource_exhausted`
	// rather than `permission_denied`, because the caller is entitled to do this
	// and simply cannot right now, which is a distinction with a button under it.
	CreateProcessingActivity(context.Context, *connect.Request[v1.CreateProcessingActivityRequest]) (*connect.Response[v1.CreateProcessingActivityResponse], error)
	// Replaces an activity's fields.
	//
	// A full replacement rather than a patch, and the field mask that would make
	// it a patch is deliberately absent. Article 30 is a record of fact whose gaps
	// are meaningful: clearing a legal basis is a real edit somebody makes when
	// they realise the one recorded was wrong, and a partial update in which an
	// omitted field means "leave it alone" gives a client no way to express that.
	UpdateProcessingActivity(context.Context, *connect.Request[v1.UpdateProcessingActivityRequest]) (*connect.Response[v1.UpdateProcessingActivityResponse], error)
	// Registers a system nobody has approved a finding about.
	//
	// Useful mostly for shadow AI: the system somebody adopted without telling
	// anyone is exactly the one no finding will ever raise.
	//
	// Classifying a new system as `high` requires `reviewed`. See UpdateAiSystem.
	CreateAiSystem(context.Context, *connect.Request[v1.CreateAiSystemRequest]) (*connect.Response[v1.CreateAiSystemResponse], error)
	// Replaces a system's fields.
	//
	// # CHANGING A CLASSIFICATION REQUIRES `reviewed`, AND THAT IS THE POINT
	//
	// Any change to `risk_classification` is refused unless the request carries
	// `reviewed: true`, and so is classifying a new system as `high`. The database
	// enforces it, not this layer.
	//
	// The reason is that an Article 6 classification is the determination the
	// whole high-risk obligation stack hangs off. Moving a system out of `high` in
	// a form somebody was tabbing through would quietly retire Articles 9 to 17
	// for it, and the record would show a change with nobody's deliberate decision
	// behind it. A caller sends `reviewed` to say a human meant it, and the audit
	// row records that they did.
	//
	// Refused answers `failed_precondition`: the caller may do this, and must
	// confirm first. That is not `permission_denied` and not `resource_exhausted`,
	// and a client shows a confirm step rather than an upgrade prompt.
	UpdateAiSystem(context.Context, *connect.Request[v1.UpdateAiSystemRequest]) (*connect.Response[v1.UpdateAiSystemResponse], error)
	// Logs a request that arrived.
	//
	// The only way a DSAR enters the register: nothing creates one from an
	// approval, because a request comes from a person and an obligation that
	// manufactured one would be inventing the requester.
	LogDsar(context.Context, *connect.Request[v1.LogDsarRequest]) (*connect.Response[v1.LogDsarResponse], error)
	// The trail a response was assembled from: which store was searched, when,
	// and what came back (ENT-226).
	//
	// Chronological, oldest first, which is the opposite of every other list on
	// this service. The other registers answer "what is outstanding" and lead
	// with the most pressing row; a trail answers "what did you do", and that
	// reads forward. A page token from this list is not interchangeable with any
	// other here.
	//
	// Ordered by `occurred_at` and not by when somebody typed it in, for the
	// reason DsarTrailEntry.occurred_at gives.
	ListDsarTrail(context.Context, *connect.Request[v1.ListDsarTrailRequest]) (*connect.Response[v1.ListDsarTrailResponse], error)
	// Adds one entry to that trail.
	//
	// # APPEND ONLY, AND THERE IS NO UPDATE OR DELETE RPC TO GO WITH IT
	//
	// Not an unfinished surface. An entry is evidence about how a response to a
	// statutory request was built, and evidence a producer can revise after the
	// fact is worth less than evidence it cannot. The database refuses an UPDATE
	// with a trigger that binds even the migrator, so an RPC to do it could not
	// be served whatever this contract said.
	//
	// Correcting a mistake means adding an entry that says so, which is how a
	// paper file works. `withheld` and `none_found` exist so that the correction
	// has somewhere honest to go.
	//
	// No `reviewed` gate, unlike MarkDsarResponded. Recording that you searched
	// the CRM is not an assertion a regulator reads as compliance; it is the
	// working note that makes the assertion checkable, and a confirm step on
	// every note would mean fewer notes.
	AddDsarTrailEntry(context.Context, *connect.Request[v1.AddDsarTrailEntryRequest]) (*connect.Response[v1.AddDsarTrailEntryResponse], error)
	// Records that a response went out, which stops the statutory clock.
	//
	// Requires `reviewed`, for the same class of reason UpdateAiSystem does: this
	// is the assertion that the organisation met an Article 12(3) deadline, and it
	// is the single most consequential row in this register to get wrong. A
	// regulator reading the log later is reading this field.
	//
	// Idempotent. Marking an already-answered request returns `applied: false` and
	// writes nothing, rather than overwriting the date a response actually went
	// out with the date somebody clicked the button twice.
	//
	// `:respond` rather than a status field on an update, per AIP-136: it is one
	// transition with a gate, not an arbitrary field change.
	MarkDsarResponded(context.Context, *connect.Request[v1.MarkDsarRespondedRequest]) (*connect.Response[v1.MarkDsarRespondedResponse], error)
}

// NewRecordsServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRecordsServiceHandler(svc RecordsServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	recordsServiceMethods := v1.File_kindlast_core_v1_records_proto.Services().ByName("RecordsService").Methods()
	recordsServiceListProcessingActivitiesHandler := connect.NewUnaryHandler(
		RecordsServiceListProcessingActivitiesProcedure,
		svc.ListProcessingActivities,
		connect.WithSchema(recordsServiceMethods.ByName("ListProcessingActivities")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceGetProcessingActivityHandler := connect.NewUnaryHandler(
		RecordsServiceGetProcessingActivityProcedure,
		svc.GetProcessingActivity,
		connect.WithSchema(recordsServiceMethods.ByName("GetProcessingActivity")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceListAiSystemsHandler := connect.NewUnaryHandler(
		RecordsServiceListAiSystemsProcedure,
		svc.ListAiSystems,
		connect.WithSchema(recordsServiceMethods.ByName("ListAiSystems")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceGetAiSystemHandler := connect.NewUnaryHandler(
		RecordsServiceGetAiSystemProcedure,
		svc.GetAiSystem,
		connect.WithSchema(recordsServiceMethods.ByName("GetAiSystem")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceListDsarsHandler := connect.NewUnaryHandler(
		RecordsServiceListDsarsProcedure,
		svc.ListDsars,
		connect.WithSchema(recordsServiceMethods.ByName("ListDsars")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceGetDsarHandler := connect.NewUnaryHandler(
		RecordsServiceGetDsarProcedure,
		svc.GetDsar,
		connect.WithSchema(recordsServiceMethods.ByName("GetDsar")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceCreateProcessingActivityHandler := connect.NewUnaryHandler(
		RecordsServiceCreateProcessingActivityProcedure,
		svc.CreateProcessingActivity,
		connect.WithSchema(recordsServiceMethods.ByName("CreateProcessingActivity")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceUpdateProcessingActivityHandler := connect.NewUnaryHandler(
		RecordsServiceUpdateProcessingActivityProcedure,
		svc.UpdateProcessingActivity,
		connect.WithSchema(recordsServiceMethods.ByName("UpdateProcessingActivity")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceCreateAiSystemHandler := connect.NewUnaryHandler(
		RecordsServiceCreateAiSystemProcedure,
		svc.CreateAiSystem,
		connect.WithSchema(recordsServiceMethods.ByName("CreateAiSystem")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceUpdateAiSystemHandler := connect.NewUnaryHandler(
		RecordsServiceUpdateAiSystemProcedure,
		svc.UpdateAiSystem,
		connect.WithSchema(recordsServiceMethods.ByName("UpdateAiSystem")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceLogDsarHandler := connect.NewUnaryHandler(
		RecordsServiceLogDsarProcedure,
		svc.LogDsar,
		connect.WithSchema(recordsServiceMethods.ByName("LogDsar")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceListDsarTrailHandler := connect.NewUnaryHandler(
		RecordsServiceListDsarTrailProcedure,
		svc.ListDsarTrail,
		connect.WithSchema(recordsServiceMethods.ByName("ListDsarTrail")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceAddDsarTrailEntryHandler := connect.NewUnaryHandler(
		RecordsServiceAddDsarTrailEntryProcedure,
		svc.AddDsarTrailEntry,
		connect.WithSchema(recordsServiceMethods.ByName("AddDsarTrailEntry")),
		connect.WithHandlerOptions(opts...),
	)
	recordsServiceMarkDsarRespondedHandler := connect.NewUnaryHandler(
		RecordsServiceMarkDsarRespondedProcedure,
		svc.MarkDsarResponded,
		connect.WithSchema(recordsServiceMethods.ByName("MarkDsarResponded")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kindlast.core.v1.RecordsService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RecordsServiceListProcessingActivitiesProcedure:
			recordsServiceListProcessingActivitiesHandler.ServeHTTP(w, r)
		case RecordsServiceGetProcessingActivityProcedure:
			recordsServiceGetProcessingActivityHandler.ServeHTTP(w, r)
		case RecordsServiceListAiSystemsProcedure:
			recordsServiceListAiSystemsHandler.ServeHTTP(w, r)
		case RecordsServiceGetAiSystemProcedure:
			recordsServiceGetAiSystemHandler.ServeHTTP(w, r)
		case RecordsServiceListDsarsProcedure:
			recordsServiceListDsarsHandler.ServeHTTP(w, r)
		case RecordsServiceGetDsarProcedure:
			recordsServiceGetDsarHandler.ServeHTTP(w, r)
		case RecordsServiceCreateProcessingActivityProcedure:
			recordsServiceCreateProcessingActivityHandler.ServeHTTP(w, r)
		case RecordsServiceUpdateProcessingActivityProcedure:
			recordsServiceUpdateProcessingActivityHandler.ServeHTTP(w, r)
		case RecordsServiceCreateAiSystemProcedure:
			recordsServiceCreateAiSystemHandler.ServeHTTP(w, r)
		case RecordsServiceUpdateAiSystemProcedure:
			recordsServiceUpdateAiSystemHandler.ServeHTTP(w, r)
		case RecordsServiceLogDsarProcedure:
			recordsServiceLogDsarHandler.ServeHTTP(w, r)
		case RecordsServiceListDsarTrailProcedure:
			recordsServiceListDsarTrailHandler.ServeHTTP(w, r)
		case RecordsServiceAddDsarTrailEntryProcedure:
			recordsServiceAddDsarTrailEntryHandler.ServeHTTP(w, r)
		case RecordsServiceMarkDsarRespondedProcedure:
			recordsServiceMarkDsarRespondedHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRecordsServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedRecordsServiceHandler struct{}

func (UnimplementedRecordsServiceHandler) ListProcessingActivities(context.Context, *connect.Request[v1.ListProcessingActivitiesRequest]) (*connect.Response[v1.ListProcessingActivitiesResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.ListProcessingActivities is not implemented"))
}

func (UnimplementedRecordsServiceHandler) GetProcessingActivity(context.Context, *connect.Request[v1.GetProcessingActivityRequest]) (*connect.Response[v1.GetProcessingActivityResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.GetProcessingActivity is not implemented"))
}

func (UnimplementedRecordsServiceHandler) ListAiSystems(context.Context, *connect.Request[v1.ListAiSystemsRequest]) (*connect.Response[v1.ListAiSystemsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.ListAiSystems is not implemented"))
}

func (UnimplementedRecordsServiceHandler) GetAiSystem(context.Context, *connect.Request[v1.GetAiSystemRequest]) (*connect.Response[v1.GetAiSystemResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.GetAiSystem is not implemented"))
}

func (UnimplementedRecordsServiceHandler) ListDsars(context.Context, *connect.Request[v1.ListDsarsRequest]) (*connect.Response[v1.ListDsarsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.ListDsars is not implemented"))
}

func (UnimplementedRecordsServiceHandler) GetDsar(context.Context, *connect.Request[v1.GetDsarRequest]) (*connect.Response[v1.GetDsarResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.GetDsar is not implemented"))
}

func (UnimplementedRecordsServiceHandler) CreateProcessingActivity(context.Context, *connect.Request[v1.CreateProcessingActivityRequest]) (*connect.Response[v1.CreateProcessingActivityResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.CreateProcessingActivity is not implemented"))
}

func (UnimplementedRecordsServiceHandler) UpdateProcessingActivity(context.Context, *connect.Request[v1.UpdateProcessingActivityRequest]) (*connect.Response[v1.UpdateProcessingActivityResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.UpdateProcessingActivity is not implemented"))
}

func (UnimplementedRecordsServiceHandler) CreateAiSystem(context.Context, *connect.Request[v1.CreateAiSystemRequest]) (*connect.Response[v1.CreateAiSystemResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.CreateAiSystem is not implemented"))
}

func (UnimplementedRecordsServiceHandler) UpdateAiSystem(context.Context, *connect.Request[v1.UpdateAiSystemRequest]) (*connect.Response[v1.UpdateAiSystemResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.UpdateAiSystem is not implemented"))
}

func (UnimplementedRecordsServiceHandler) LogDsar(context.Context, *connect.Request[v1.LogDsarRequest]) (*connect.Response[v1.LogDsarResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.LogDsar is not implemented"))
}

func (UnimplementedRecordsServiceHandler) ListDsarTrail(context.Context, *connect.Request[v1.ListDsarTrailRequest]) (*connect.Response[v1.ListDsarTrailResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.ListDsarTrail is not implemented"))
}

func (UnimplementedRecordsServiceHandler) AddDsarTrailEntry(context.Context, *connect.Request[v1.AddDsarTrailEntryRequest]) (*connect.Response[v1.AddDsarTrailEntryResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.AddDsarTrailEntry is not implemented"))
}

func (UnimplementedRecordsServiceHandler) MarkDsarResponded(context.Context, *connect.Request[v1.MarkDsarRespondedRequest]) (*connect.Response[v1.MarkDsarRespondedResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.RecordsService.MarkDsarResponded is not implemented"))
}
