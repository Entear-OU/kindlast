// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kindlast/core/v1/billing.proto

package corev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Entear-OU/kindlast/gen/go/kindlast/core/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// BillingServiceName is the fully-qualified name of the BillingService service.
	BillingServiceName = "kindlast.core.v1.BillingService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// BillingServiceGetBillingProcedure is the fully-qualified name of the BillingService's GetBilling
	// RPC.
	BillingServiceGetBillingProcedure = "/kindlast.core.v1.BillingService/GetBilling"
)

// BillingServiceClient is a client for the kindlast.core.v1.BillingService service.
type BillingServiceClient interface {
	// What this organisation is on, and what the console may offer.
	//
	// Owner-only, per §20.1. A member cannot see it: billing is not secret from
	// colleagues in a moral sense, but a plan and a renewal date are commercial
	// facts about the company, and the role that manages them is the one that
	// signed for them.
	GetBilling(context.Context, *connect.Request[v1.GetBillingRequest]) (*connect.Response[v1.GetBillingResponse], error)
}

// NewBillingServiceClient constructs a client for the kindlast.core.v1.BillingService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewBillingServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) BillingServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	billingServiceMethods := v1.File_kindlast_core_v1_billing_proto.Services().ByName("BillingService").Methods()
	return &billingServiceClient{
		getBilling: connect.NewClient[v1.GetBillingRequest, v1.GetBillingResponse](
			httpClient,
			baseURL+BillingServiceGetBillingProcedure,
			connect.WithSchema(billingServiceMethods.ByName("GetBilling")),
			connect.WithClientOptions(opts...),
		),
	}
}

// billingServiceClient implements BillingServiceClient.
type billingServiceClient struct {
	getBilling *connect.Client[v1.GetBillingRequest, v1.GetBillingResponse]
}

// GetBilling calls kindlast.core.v1.BillingService.GetBilling.
func (c *billingServiceClient) GetBilling(ctx context.Context, req *connect.Request[v1.GetBillingRequest]) (*connect.Response[v1.GetBillingResponse], error) {
	return c.getBilling.CallUnary(ctx, req)
}

// BillingServiceHandler is an implementation of the kindlast.core.v1.BillingService service.
type BillingServiceHandler interface {
	// What this organisation is on, and what the console may offer.
	//
	// Owner-only, per §20.1. A member cannot see it: billing is not secret from
	// colleagues in a moral sense, but a plan and a renewal date are commercial
	// facts about the company, and the role that manages them is the one that
	// signed for them.
	GetBilling(context.Context, *connect.Request[v1.GetBillingRequest]) (*connect.Response[v1.GetBillingResponse], error)
}

// NewBillingServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewBillingServiceHandler(svc BillingServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	billingServiceMethods := v1.File_kindlast_core_v1_billing_proto.Services().ByName("BillingService").Methods()
	billingServiceGetBillingHandler := connect.NewUnaryHandler(
		BillingServiceGetBillingProcedure,
		svc.GetBilling,
		connect.WithSchema(billingServiceMethods.ByName("GetBilling")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kindlast.core.v1.BillingService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case BillingServiceGetBillingProcedure:
			billingServiceGetBillingHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedBillingServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedBillingServiceHandler struct{}

func (UnimplementedBillingServiceHandler) GetBilling(context.Context, *connect.Request[v1.GetBillingRequest]) (*connect.Response[v1.GetBillingResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.BillingService.GetBilling is not implemented"))
}
