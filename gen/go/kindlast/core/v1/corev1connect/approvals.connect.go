// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kindlast/core/v1/approvals.proto

package corev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Entear-OU/kindlast/gen/go/kindlast/core/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ApprovalServiceName is the fully-qualified name of the ApprovalService service.
	ApprovalServiceName = "kindlast.core.v1.ApprovalService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ApprovalServiceExplainApprovalProcedure is the fully-qualified name of the ApprovalService's
	// ExplainApproval RPC.
	ApprovalServiceExplainApprovalProcedure = "/kindlast.core.v1.ApprovalService/ExplainApproval"
)

// ApprovalServiceClient is a client for the kindlast.core.v1.ApprovalService service.
type ApprovalServiceClient interface {
	// What approving this finding will do for this organisation, and the record
	// it prepares.
	//
	// # `agents:ask` FOR THE REASON ASKING THE ANALYST NEEDS IT
	//
	// Reading a finding and running a model over it are separately dangerous.
	// Reading discloses a compliance gap; this spends a model budget, sends the
	// organisation's own memory to whichever provider it chose, and writes a
	// proposed payload onto the finding. A credential issued to render the feed
	// should not thereby be one that can run the Hands in a loop at somebody
	// else's cost.
	//
	// # SYNCHRONOUS, BECAUSE SOMEBODY IS WAITING
	//
	// Same as `AskAboutFinding` and unlike narration: the person is on the
	// finding page with the decision panel under this. A deployment with no
	// model answers `intelligence_available: false` at once rather than holding
	// the request open.
	//
	//	not_found            no such finding, and a finding in another
	//	                     organisation, which answer alike
	//	failed_precondition  approving this finding creates no record, so there
	//	                     is nothing to prepare, or this organisation chose a
	//	                     model that cannot be honoured
	//	unavailable          the Hands could not be reached
	ExplainApproval(context.Context, *connect.Request[v1.ExplainApprovalRequest]) (*connect.Response[v1.ExplainApprovalResponse], error)
}

// NewApprovalServiceClient constructs a client for the kindlast.core.v1.ApprovalService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewApprovalServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ApprovalServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	approvalServiceMethods := v1.File_kindlast_core_v1_approvals_proto.Services().ByName("ApprovalService").Methods()
	return &approvalServiceClient{
		explainApproval: connect.NewClient[v1.ExplainApprovalRequest, v1.ExplainApprovalResponse](
			httpClient,
			baseURL+ApprovalServiceExplainApprovalProcedure,
			connect.WithSchema(approvalServiceMethods.ByName("ExplainApproval")),
			connect.WithClientOptions(opts...),
		),
	}
}

// approvalServiceClient implements ApprovalServiceClient.
type approvalServiceClient struct {
	explainApproval *connect.Client[v1.ExplainApprovalRequest, v1.ExplainApprovalResponse]
}

// ExplainApproval calls kindlast.core.v1.ApprovalService.ExplainApproval.
func (c *approvalServiceClient) ExplainApproval(ctx context.Context, req *connect.Request[v1.ExplainApprovalRequest]) (*connect.Response[v1.ExplainApprovalResponse], error) {
	return c.explainApproval.CallUnary(ctx, req)
}

// ApprovalServiceHandler is an implementation of the kindlast.core.v1.ApprovalService service.
type ApprovalServiceHandler interface {
	// What approving this finding will do for this organisation, and the record
	// it prepares.
	//
	// # `agents:ask` FOR THE REASON ASKING THE ANALYST NEEDS IT
	//
	// Reading a finding and running a model over it are separately dangerous.
	// Reading discloses a compliance gap; this spends a model budget, sends the
	// organisation's own memory to whichever provider it chose, and writes a
	// proposed payload onto the finding. A credential issued to render the feed
	// should not thereby be one that can run the Hands in a loop at somebody
	// else's cost.
	//
	// # SYNCHRONOUS, BECAUSE SOMEBODY IS WAITING
	//
	// Same as `AskAboutFinding` and unlike narration: the person is on the
	// finding page with the decision panel under this. A deployment with no
	// model answers `intelligence_available: false` at once rather than holding
	// the request open.
	//
	//	not_found            no such finding, and a finding in another
	//	                     organisation, which answer alike
	//	failed_precondition  approving this finding creates no record, so there
	//	                     is nothing to prepare, or this organisation chose a
	//	                     model that cannot be honoured
	//	unavailable          the Hands could not be reached
	ExplainApproval(context.Context, *connect.Request[v1.ExplainApprovalRequest]) (*connect.Response[v1.ExplainApprovalResponse], error)
}

// NewApprovalServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewApprovalServiceHandler(svc ApprovalServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	approvalServiceMethods := v1.File_kindlast_core_v1_approvals_proto.Services().ByName("ApprovalService").Methods()
	approvalServiceExplainApprovalHandler := connect.NewUnaryHandler(
		ApprovalServiceExplainApprovalProcedure,
		svc.ExplainApproval,
		connect.WithSchema(approvalServiceMethods.ByName("ExplainApproval")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kindlast.core.v1.ApprovalService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ApprovalServiceExplainApprovalProcedure:
			approvalServiceExplainApprovalHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedApprovalServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedApprovalServiceHandler struct{}

func (UnimplementedApprovalServiceHandler) ExplainApproval(context.Context, *connect.Request[v1.ExplainApprovalRequest]) (*connect.Response[v1.ExplainApprovalResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.ApprovalService.ExplainApproval is not implemented"))
}
