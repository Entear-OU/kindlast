// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kindlast/core/v1/session.proto

package corev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Entear-OU/kindlast/gen/go/kindlast/core/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// SessionServiceName is the fully-qualified name of the SessionService service.
	SessionServiceName = "kindlast.core.v1.SessionService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// SessionServiceGetCurrentUserProcedure is the fully-qualified name of the SessionService's
	// GetCurrentUser RPC.
	SessionServiceGetCurrentUserProcedure = "/kindlast.core.v1.SessionService/GetCurrentUser"
)

// SessionServiceClient is a client for the kindlast.core.v1.SessionService service.
type SessionServiceClient interface {
	// Returns the caller, their memberships with roles, and the active
	// organisation. On a subject it has never seen, it provisions a personal
	// organisation and an owner membership in one transaction before returning
	// (ENT-196), which is why this is a plain unary call rather than a read.
	GetCurrentUser(context.Context, *connect.Request[v1.GetCurrentUserRequest]) (*connect.Response[v1.GetCurrentUserResponse], error)
}

// NewSessionServiceClient constructs a client for the kindlast.core.v1.SessionService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewSessionServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) SessionServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	sessionServiceMethods := v1.File_kindlast_core_v1_session_proto.Services().ByName("SessionService").Methods()
	return &sessionServiceClient{
		getCurrentUser: connect.NewClient[v1.GetCurrentUserRequest, v1.GetCurrentUserResponse](
			httpClient,
			baseURL+SessionServiceGetCurrentUserProcedure,
			connect.WithSchema(sessionServiceMethods.ByName("GetCurrentUser")),
			connect.WithClientOptions(opts...),
		),
	}
}

// sessionServiceClient implements SessionServiceClient.
type sessionServiceClient struct {
	getCurrentUser *connect.Client[v1.GetCurrentUserRequest, v1.GetCurrentUserResponse]
}

// GetCurrentUser calls kindlast.core.v1.SessionService.GetCurrentUser.
func (c *sessionServiceClient) GetCurrentUser(ctx context.Context, req *connect.Request[v1.GetCurrentUserRequest]) (*connect.Response[v1.GetCurrentUserResponse], error) {
	return c.getCurrentUser.CallUnary(ctx, req)
}

// SessionServiceHandler is an implementation of the kindlast.core.v1.SessionService service.
type SessionServiceHandler interface {
	// Returns the caller, their memberships with roles, and the active
	// organisation. On a subject it has never seen, it provisions a personal
	// organisation and an owner membership in one transaction before returning
	// (ENT-196), which is why this is a plain unary call rather than a read.
	GetCurrentUser(context.Context, *connect.Request[v1.GetCurrentUserRequest]) (*connect.Response[v1.GetCurrentUserResponse], error)
}

// NewSessionServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewSessionServiceHandler(svc SessionServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	sessionServiceMethods := v1.File_kindlast_core_v1_session_proto.Services().ByName("SessionService").Methods()
	sessionServiceGetCurrentUserHandler := connect.NewUnaryHandler(
		SessionServiceGetCurrentUserProcedure,
		svc.GetCurrentUser,
		connect.WithSchema(sessionServiceMethods.ByName("GetCurrentUser")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kindlast.core.v1.SessionService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case SessionServiceGetCurrentUserProcedure:
			sessionServiceGetCurrentUserHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedSessionServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedSessionServiceHandler struct{}

func (UnimplementedSessionServiceHandler) GetCurrentUser(context.Context, *connect.Request[v1.GetCurrentUserRequest]) (*connect.Response[v1.GetCurrentUserResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.core.v1.SessionService.GetCurrentUser is not implemented"))
}
