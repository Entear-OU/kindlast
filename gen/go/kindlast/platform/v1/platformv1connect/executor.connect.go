// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kindlast/platform/v1/executor.proto

package platformv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Entear-OU/kindlast/gen/go/kindlast/platform/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ExecutorServiceName is the fully-qualified name of the ExecutorService service.
	ExecutorServiceName = "kindlast.platform.v1.ExecutorService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ExecutorServiceListPendingJobsProcedure is the fully-qualified name of the ExecutorService's
	// ListPendingJobs RPC.
	ExecutorServiceListPendingJobsProcedure = "/kindlast.platform.v1.ExecutorService/ListPendingJobs"
	// ExecutorServiceExecuteJobProcedure is the fully-qualified name of the ExecutorService's
	// ExecuteJob RPC.
	ExecutorServiceExecuteJobProcedure = "/kindlast.platform.v1.ExecutorService/ExecuteJob"
)

// ExecutorServiceClient is a client for the kindlast.platform.v1.ExecutorService service.
type ExecutorServiceClient interface {
	// Lists the approvals whose record has not been created yet: job id,
	// organisation, finding and action, oldest first. The relay's question,
	// asked every few seconds; no organisation header, because the list is
	// every organisation's, and no payload, because the finding's proposed
	// record stays in the database until the execution reads it.
	ListPendingJobs(context.Context, *connect.Request[v1.ListPendingJobsRequest]) (*connect.Response[v1.ListPendingJobsResponse], error)
	// Creates the record one approved finding asked for, and settles the job,
	// in one transaction as the approver.
	//
	// Idempotent in the two ways that matter: the job row is claimed `for
	// update` and re-read as pending, so two runs produce one execution; and
	// the record is guarded on the finding, the same guard the trigger had, so
	// an execution that created a record and failed to commit its settle finds
	// that record on the retry rather than creating a second.
	//
	//	invalid_argument   the job id is not a uuid
	//	unavailable        the execution failed and was recorded on the row;
	//	                   the job stays pending and is offered again
	ExecuteJob(context.Context, *connect.Request[v1.ExecuteJobRequest]) (*connect.Response[v1.ExecuteJobResponse], error)
}

// NewExecutorServiceClient constructs a client for the kindlast.platform.v1.ExecutorService
// service. By default, it uses the Connect protocol with the binary Protobuf Codec, asks for
// gzipped responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply
// the connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewExecutorServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ExecutorServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	executorServiceMethods := v1.File_kindlast_platform_v1_executor_proto.Services().ByName("ExecutorService").Methods()
	return &executorServiceClient{
		listPendingJobs: connect.NewClient[v1.ListPendingJobsRequest, v1.ListPendingJobsResponse](
			httpClient,
			baseURL+ExecutorServiceListPendingJobsProcedure,
			connect.WithSchema(executorServiceMethods.ByName("ListPendingJobs")),
			connect.WithClientOptions(opts...),
		),
		executeJob: connect.NewClient[v1.ExecuteJobRequest, v1.ExecuteJobResponse](
			httpClient,
			baseURL+ExecutorServiceExecuteJobProcedure,
			connect.WithSchema(executorServiceMethods.ByName("ExecuteJob")),
			connect.WithClientOptions(opts...),
		),
	}
}

// executorServiceClient implements ExecutorServiceClient.
type executorServiceClient struct {
	listPendingJobs *connect.Client[v1.ListPendingJobsRequest, v1.ListPendingJobsResponse]
	executeJob      *connect.Client[v1.ExecuteJobRequest, v1.ExecuteJobResponse]
}

// ListPendingJobs calls kindlast.platform.v1.ExecutorService.ListPendingJobs.
func (c *executorServiceClient) ListPendingJobs(ctx context.Context, req *connect.Request[v1.ListPendingJobsRequest]) (*connect.Response[v1.ListPendingJobsResponse], error) {
	return c.listPendingJobs.CallUnary(ctx, req)
}

// ExecuteJob calls kindlast.platform.v1.ExecutorService.ExecuteJob.
func (c *executorServiceClient) ExecuteJob(ctx context.Context, req *connect.Request[v1.ExecuteJobRequest]) (*connect.Response[v1.ExecuteJobResponse], error) {
	return c.executeJob.CallUnary(ctx, req)
}

// ExecutorServiceHandler is an implementation of the kindlast.platform.v1.ExecutorService service.
type ExecutorServiceHandler interface {
	// Lists the approvals whose record has not been created yet: job id,
	// organisation, finding and action, oldest first. The relay's question,
	// asked every few seconds; no organisation header, because the list is
	// every organisation's, and no payload, because the finding's proposed
	// record stays in the database until the execution reads it.
	ListPendingJobs(context.Context, *connect.Request[v1.ListPendingJobsRequest]) (*connect.Response[v1.ListPendingJobsResponse], error)
	// Creates the record one approved finding asked for, and settles the job,
	// in one transaction as the approver.
	//
	// Idempotent in the two ways that matter: the job row is claimed `for
	// update` and re-read as pending, so two runs produce one execution; and
	// the record is guarded on the finding, the same guard the trigger had, so
	// an execution that created a record and failed to commit its settle finds
	// that record on the retry rather than creating a second.
	//
	//	invalid_argument   the job id is not a uuid
	//	unavailable        the execution failed and was recorded on the row;
	//	                   the job stays pending and is offered again
	ExecuteJob(context.Context, *connect.Request[v1.ExecuteJobRequest]) (*connect.Response[v1.ExecuteJobResponse], error)
}

// NewExecutorServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewExecutorServiceHandler(svc ExecutorServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	executorServiceMethods := v1.File_kindlast_platform_v1_executor_proto.Services().ByName("ExecutorService").Methods()
	executorServiceListPendingJobsHandler := connect.NewUnaryHandler(
		ExecutorServiceListPendingJobsProcedure,
		svc.ListPendingJobs,
		connect.WithSchema(executorServiceMethods.ByName("ListPendingJobs")),
		connect.WithHandlerOptions(opts...),
	)
	executorServiceExecuteJobHandler := connect.NewUnaryHandler(
		ExecutorServiceExecuteJobProcedure,
		svc.ExecuteJob,
		connect.WithSchema(executorServiceMethods.ByName("ExecuteJob")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kindlast.platform.v1.ExecutorService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ExecutorServiceListPendingJobsProcedure:
			executorServiceListPendingJobsHandler.ServeHTTP(w, r)
		case ExecutorServiceExecuteJobProcedure:
			executorServiceExecuteJobHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedExecutorServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedExecutorServiceHandler struct{}

func (UnimplementedExecutorServiceHandler) ListPendingJobs(context.Context, *connect.Request[v1.ListPendingJobsRequest]) (*connect.Response[v1.ListPendingJobsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.platform.v1.ExecutorService.ListPendingJobs is not implemented"))
}

func (UnimplementedExecutorServiceHandler) ExecuteJob(context.Context, *connect.Request[v1.ExecuteJobRequest]) (*connect.Response[v1.ExecuteJobResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.platform.v1.ExecutorService.ExecuteJob is not implemented"))
}
