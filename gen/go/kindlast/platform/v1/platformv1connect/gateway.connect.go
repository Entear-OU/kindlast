// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: kindlast/platform/v1/gateway.proto

package platformv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Entear-OU/kindlast/gen/go/kindlast/platform/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// GatewayServiceName is the fully-qualified name of the GatewayService service.
	GatewayServiceName = "kindlast.platform.v1.GatewayService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// GatewayServiceListToolsProcedure is the fully-qualified name of the GatewayService's ListTools
	// RPC.
	GatewayServiceListToolsProcedure = "/kindlast.platform.v1.GatewayService/ListTools"
	// GatewayServiceCallToolProcedure is the fully-qualified name of the GatewayService's CallTool RPC.
	GatewayServiceCallToolProcedure = "/kindlast.platform.v1.GatewayService/CallTool"
)

// GatewayServiceClient is a client for the kindlast.platform.v1.GatewayService service.
type GatewayServiceClient interface {
	// Ask an endpoint what tools it has.
	//
	// Reads nothing and stores nothing. It exists so the consent screen can list
	// the exact tools a connection exposes, and so an endpoint outside the
	// egress allow-list is refused at the moment somebody tries to connect it
	// rather than the first time somebody uses it.
	ListTools(context.Context, *connect.Request[v1.ListToolsRequest]) (*connect.Response[v1.ListToolsResponse], error)
	// Call one tool, on one connection, under one connection's policy.
	//
	// The policy travels in the request rather than being looked up here,
	// because the gateway holds no database handle. That makes the caller
	// responsible for sending the truth, and the gateway responsible for
	// refusing anything the policy does not permit, which is the right split:
	// core-api cannot be relied on to be free of bugs, so the gateway checks
	// what it was sent rather than assuming it was filtered.
	CallTool(context.Context, *connect.Request[v1.CallToolRequest]) (*connect.Response[v1.CallToolResponse], error)
}

// NewGatewayServiceClient constructs a client for the kindlast.platform.v1.GatewayService service.
// By default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped
// responses, and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewGatewayServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) GatewayServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	gatewayServiceMethods := v1.File_kindlast_platform_v1_gateway_proto.Services().ByName("GatewayService").Methods()
	return &gatewayServiceClient{
		listTools: connect.NewClient[v1.ListToolsRequest, v1.ListToolsResponse](
			httpClient,
			baseURL+GatewayServiceListToolsProcedure,
			connect.WithSchema(gatewayServiceMethods.ByName("ListTools")),
			connect.WithClientOptions(opts...),
		),
		callTool: connect.NewClient[v1.CallToolRequest, v1.CallToolResponse](
			httpClient,
			baseURL+GatewayServiceCallToolProcedure,
			connect.WithSchema(gatewayServiceMethods.ByName("CallTool")),
			connect.WithClientOptions(opts...),
		),
	}
}

// gatewayServiceClient implements GatewayServiceClient.
type gatewayServiceClient struct {
	listTools *connect.Client[v1.ListToolsRequest, v1.ListToolsResponse]
	callTool  *connect.Client[v1.CallToolRequest, v1.CallToolResponse]
}

// ListTools calls kindlast.platform.v1.GatewayService.ListTools.
func (c *gatewayServiceClient) ListTools(ctx context.Context, req *connect.Request[v1.ListToolsRequest]) (*connect.Response[v1.ListToolsResponse], error) {
	return c.listTools.CallUnary(ctx, req)
}

// CallTool calls kindlast.platform.v1.GatewayService.CallTool.
func (c *gatewayServiceClient) CallTool(ctx context.Context, req *connect.Request[v1.CallToolRequest]) (*connect.Response[v1.CallToolResponse], error) {
	return c.callTool.CallUnary(ctx, req)
}

// GatewayServiceHandler is an implementation of the kindlast.platform.v1.GatewayService service.
type GatewayServiceHandler interface {
	// Ask an endpoint what tools it has.
	//
	// Reads nothing and stores nothing. It exists so the consent screen can list
	// the exact tools a connection exposes, and so an endpoint outside the
	// egress allow-list is refused at the moment somebody tries to connect it
	// rather than the first time somebody uses it.
	ListTools(context.Context, *connect.Request[v1.ListToolsRequest]) (*connect.Response[v1.ListToolsResponse], error)
	// Call one tool, on one connection, under one connection's policy.
	//
	// The policy travels in the request rather than being looked up here,
	// because the gateway holds no database handle. That makes the caller
	// responsible for sending the truth, and the gateway responsible for
	// refusing anything the policy does not permit, which is the right split:
	// core-api cannot be relied on to be free of bugs, so the gateway checks
	// what it was sent rather than assuming it was filtered.
	CallTool(context.Context, *connect.Request[v1.CallToolRequest]) (*connect.Response[v1.CallToolResponse], error)
}

// NewGatewayServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewGatewayServiceHandler(svc GatewayServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	gatewayServiceMethods := v1.File_kindlast_platform_v1_gateway_proto.Services().ByName("GatewayService").Methods()
	gatewayServiceListToolsHandler := connect.NewUnaryHandler(
		GatewayServiceListToolsProcedure,
		svc.ListTools,
		connect.WithSchema(gatewayServiceMethods.ByName("ListTools")),
		connect.WithHandlerOptions(opts...),
	)
	gatewayServiceCallToolHandler := connect.NewUnaryHandler(
		GatewayServiceCallToolProcedure,
		svc.CallTool,
		connect.WithSchema(gatewayServiceMethods.ByName("CallTool")),
		connect.WithHandlerOptions(opts...),
	)
	return "/kindlast.platform.v1.GatewayService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case GatewayServiceListToolsProcedure:
			gatewayServiceListToolsHandler.ServeHTTP(w, r)
		case GatewayServiceCallToolProcedure:
			gatewayServiceCallToolHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedGatewayServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedGatewayServiceHandler struct{}

func (UnimplementedGatewayServiceHandler) ListTools(context.Context, *connect.Request[v1.ListToolsRequest]) (*connect.Response[v1.ListToolsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.platform.v1.GatewayService.ListTools is not implemented"))
}

func (UnimplementedGatewayServiceHandler) CallTool(context.Context, *connect.Request[v1.CallToolRequest]) (*connect.Response[v1.CallToolResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("kindlast.platform.v1.GatewayService.CallTool is not implemented"))
}
