// The resource server. Owns the domain schema and is the only writer to it
// (core-api-surface §4.1, §21.6).
module github.com/Entear-OU/kindlast/apps/core-api

go 1.24

require google.golang.org/protobuf v1.36.11
